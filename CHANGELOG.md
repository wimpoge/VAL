# Changelog

Reverse-chronological log of significant changes to VAL — Visual AI Learning. The roadmap proper (`AI_ENGINEER_ROADMAP.md`) holds the curriculum spec; this file holds the implementation history.

---

## 2026-05-30

### Day 20 — Capstone: Build Your AI App (N96–N100) · roadmap complete

- **Tenth advanced day, last day in the roadmap. 20/20 days · 100/100 nodes live.** Lives at [frontend/app/advanced-ai/day-20/page.tsx](frontend/app/advanced-ai/day-20/page.tsx), backend at [backend/routers/day20.py](backend/routers/day20.py).
- **Backend — two endpoints.**
  - `POST /ask` — JSON `{question, provider?}`. Topic context is forward-looking: "Today is the capstone day — the learner has completed Days 1-19 covering [full track list]. Be encouraging and forward-looking. Help them plan their first real project." Single-language plain-text answer.
  - `POST /stack-picker` — JSON `{needs_multimodal, needs_own_docs, needs_agents, needs_local, needs_scale, provider?}`. Five booleans → full stack recommendation. System prompt biases the model "toward boring proven choices — Next.js + FastAPI + a managed provider beats clever frameworks for v1." Returns `{architecture: simple|rag|agent, frontend, backend, vector_db, llm_provider, local_runtime, reason, days_to_revisit: int[], estimated_complexity: weekend|1-week|1-month}`. `response_format={"type": "json_object"}`.
  - **Deterministic post-processing is doing real work:** `_expected_architecture()` and `_expected_complexity()` compute the rule-correct answer from the booleans (agent if needs_agents; rag if needs_own_docs; weekend ≤ 1 flag, 1-week 2-3 flags, 1-month 4-5 flags). The model's pick is `_clamp_enum`d into the allowed values, then **the rule wins on conflict** — if needs_agents=true but the model picks rag, it gets force-corrected to agent. Same for the optional fields: `vector_db` is null-ed when architecture='simple' or needs_own_docs=false; `local_runtime` is null-ed when needs_local=false (regardless of what the model returned). `_clamp_revisit()` enforces days_to_revisit are unique ints in [1, 20] with max 6 entries. Sanity-tested via inline harness across all-false / own_docs-only / agents+docs / all-five cases — all four returned correct architecture and complexity (simple/weekend, rag/weekend, agent/1-week, agent/1-month).
- **[backend/main.py](backend/main.py)** — `day20` added to imports + `_day_routers` + `DAY_TOPICS[20] = "Capstone — Build Your AI App"`. Routes verified: `/day20/ask` + `/day20/stack-picker`.
- **Frontend — five node tabs N96–N100.** Per-tab palette: Stack green, Patterns violet, RAG E2E violet, Flywheel coral, What Next green.
  - **N96 Choosing Your Stack + Picker** — the day's headline. Five toggle cards in a 1/2/5 column grid: `📷 Needs multimodal`, `📄 Needs my own docs`, `🤖 Needs agents`, `🏠 Needs to run locally`, `📈 Needs to scale`. Each card has the emoji, an `aria-pressed` state, a serif label and a small hint. Selected cards swap to `bg-accent text-background`; off cards stay on `border-rule`. Submit → `POST /day20/stack-picker`. Result renders as: (1) an architecture pill (simple=blue / rag=violet / agent=amber) + complexity pill (weekend=green / 1-week=amber / 1-month=red), (2) a **StackTable** with 5 rows (`Frontend / Backend / Vector DB / LLM Provider / Local Runtime`) each as a colored technology badge — when the field is null (vector DB on a simple app, local runtime when not needed) it renders `— not needed` in muted mono so users see what was explicitly skipped, (3) the prose reason, (4) a footer with provider · model · tokens · latency, and (5) a "**Revisit these days**" row of clickable accent-bordered pills that link via `dayHref()` to `/day-NN` (basic 1-10) or `/advanced-ai/day-NN` (advanced 11-20).
  - **N97 Architecture Patterns + interactive selector** — facts: Simple / RAG / Agent (3 columns). Visual: 3 side-by-side `PatternCard`s rendered via `PatternDiagram` — Simple shows 3 boxes (`User → LLM → Output`), RAG shows 5 boxes (`User → Embed → Retrieve → LLM + ctx → Output`), Agent shows 5 boxes with the second-to-last arrow swapped for `↻` to communicate the loop (`User → Agent → Tool → (loop) → Output`). Click any card to select it; selected card gets a 2px `box-shadow` ring and a tinted bg. Below the cards, a `PatternDetail` panel shows use cases (3 bullet examples per pattern) and the trade-off line — RAG defaults selected as the most-common case. Explainer: "match the architecture to the question shape".
  - **N98 Build a RAG App End-to-End + animated walker** — facts: Stream the answer / Cite the source / Cache the embed / Ship before reranking. Visual: 9-stage RAG pipeline (`PDF upload Day 5 → Chunker → Embedder → Qdrant → Retriever → Prompt builder → LLM → Streaming response → UI`) each tagged with the day that covered it. Stages laid out as a `<ol>` in a responsive 1/3/5 column grid; each stage card colored by its track (ingest coral, vectors violet, prompt blue, prod amber, UI green). "run demo →" button kicks off a `setInterval(500ms)` walker that increments a step counter — active stage gets a tinted bg + 2px box-shadow ring, visited stages keep a faint tint, future stages stay transparent. Explainer: "the whole roadmap in one diagram" — the prose literally enumerates which day each box maps to.
  - **N99 Eval, Iterate, Improve + circular SVG flywheel** — facts: Log everything / Tag the bad ones / Eval on every change / Ship then repeat. Visual: actual SVG circle at viewBox `100×100` with 5 stage buttons positioned via `angle` polar coords (`-90°` for Log at top, clockwise through Tag/Dataset/Eval/Ship). Each adjacent pair gets a clockwise arrow arc rendered as an SVG `<path d="M ... A ..." marker-end="url(#fw-arrow)">` so the wheel reads as one continuous motion. Click any stage button to select it; right-side detail panel shows the stage name (colored), the tool example (Langfuse / Thumbs + flag / Curated trace bundle / RAGAS / CI gate + canary), and a one-line description. Stages get unique colors (blue / amber / violet / coral / green) matching the eval tools' branding direction. Explainer: "the habit that compounds".
  - **N100 What Comes Next + 100-node constellation map** — facts: Ship the v1 / Watch one cohort / Read the bad answers / Teach someone else. Visual: **the 100-dot constellation**. `buildConstellation()` returns 5 rows of `NodeMapEntry`s, one per category color per the roadmap spec: teal foundations (N1-N20), coral retrieval (N21-N40), violet agents (N41-N50 + N76-N80), amber infra (N51-N70 + N81-N85), blue advanced (N71-N75 + N86-N100). Hardcoded `labels` map covers all 100 node names. Each row renders a category mono-tag on the left + a wrap-flex row of 12px dots. **Stagger animation:** every dot starts grey with `opacity 0.25`; on `requestAnimationFrame` mount, a per-dot `delays.set(node.id, cursor * 30)` cumulative offset drives a 240ms transition that sweeps the wave across all 100 in ~3s. **N100 gets a permanent glow** — `boxShadow: '0 0 8px 2px {color}, 0 0 0 2px white'` once mounted, the "you finished" signal. Hover/focus any dot → footer line shows `N{id} · {label} · Day {day}`; legend row above the map keys the five colors. Bottom-right corner shows accent `100 / 100`. Explainer: "the map is not the territory".
- **Always-visible footer: `CompletionBanner`.** Pinned below the node tabs, before `DayPager`. Border-top + tinted background that switches from accent → green once all 5 Day 20 nodes are ticked. Heading swaps from `"Almost there."` to `"✓ Roadmap complete"` at the same threshold. Body text recites every track: "Tokens, embeddings, prompts, models, vector DBs, RAG, agents, MCP, multimodal, observability, frameworks, deployment, fine-tuning, alignment, quantization, business. You built every one. Now go ship something." Four colored external link pills row: HuggingFace Hub (amber), OpenClaw (violet, github.com/openclaw/openclaw per the roadmap spec), Anthropic Research (coral), Simon Willison's Blog (blue) — all `target="_blank" rel="noopener noreferrer"`.
- **Marked Day 20 LIVE — last flip.** All three coordination spots now contain the full set `{11..20}`:
  - [frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx) — `ADVANCED_LIVE = {11..20}`. The advanced day-chip strip is now all-live, no planned grey-outs.
  - [frontend/app/components/DayPager.tsx](frontend/app/components/DayPager.tsx) — mirror set bumped + topic label renamed from `'Capstone'` → `'Capstone — Build Your AI App'` to match `DAY_TOPICS[20]`. Day 19's "next" slot becomes a real link to Day 20; Day 20's "next" slot is null (end of track) — DayPager renders the empty terminal state for it.
  - [frontend/app/advanced-ai/page.tsx](frontend/app/advanced-ai/page.tsx) — Day 20 card flipped to `status: 'live'`, topic renamed, blurb rewritten to list every visual (stack picker, architecture comparator, RAG pipeline walker, eval flywheel, 100-node constellation).
- **One TS cleanup during the build:** initial draft used `VIZ.accent` in two places (constellation footer + completion banner), but the `VIZ` object is `as const` with no `accent` key — the editorial accent is a CSS variable `--accent`, not a hex in the palette. Replaced both call sites: constellation footer uses the `text-accent` Tailwind class directly; completion banner branches on the `allDone` boolean to either inline-style `VIZ.green` or apply the `text-accent` class.
- **Verified.** Backend mounts cleanly; rule-helper sanity check confirms architecture + complexity derivation matches the spec across all-false / own_docs-only / agents+docs / all-five inputs. Frontend `tsc --noEmit` clean; `eslint app/advanced-ai/day-20/page.tsx` exits 0. Progress catalog already covered N96–N100 — mark-complete works; once all five are ticked, the advanced-ai index card flips to `✓ COMPLETED` and the completion banner's heading swaps to `✓ Roadmap complete`.

### Day 19 — AI Business & Products (N91–N95)

- Ninth advanced day. Topic: the non-code half of shipping AI — pricing, GTM, the demo trap, responsible-AI practices, and the five case studies worth memorizing. Lives at [frontend/app/advanced-ai/day-19/page.tsx](frontend/app/advanced-ai/day-19/page.tsx), backend at [backend/routers/day19.py](backend/routers/day19.py).
- **Backend — two endpoints.**
  - `POST /ask` — JSON `{question, provider?}`. Topic context: pricing models (per-token / per-seat / usage tiers / outcome-based), GTM (PLG / API-first / Vertical SaaS), the demo trap, responsible-AI practices, case studies. System prompt explicitly tells the tutor to "be opinionated and concrete — name specific products and numbers". Single-language plain-text answer.
  - `POST /product-critique` — JSON `{idea, provider?}`. The day's headline endpoint. System prompt sets up a "tough-but-fair AI product critic" persona and demands `{demo_trap, pricing_model, pricing_reason, gtm_motion, gtm_reason, safety_concern, viability_score: 1-10, viability_reason}`. `response_format={"type": "json_object"}`. Idea capped at 600 chars at the API boundary so abusive inputs bounce before reaching the model.
  - **Defensive server-side normalization:** `_clamp_enum` forces `pricing_model` into `{per-token, per-seat, usage tiers, outcome-based}` (defaults to `per-token`) and `gtm_motion` into `{PLG, API-first, Vertical SaaS}` (defaults to `API-first`). `_clamp_int` forces `viability_score` into `[1, 10]` (defaults to 5). Text fields truncated to per-field limits (demo_trap/safety_concern 320, reason fields 260/480). The system prompt explicitly tells the model "be honest — easy 8/10 scores undercut the whole exercise" to fight grade inflation in the LLM-as-judge.
- **[backend/main.py](backend/main.py)** — `day19` added to imports + `_day_routers` + `DAY_TOPICS[19] = "AI Business & Products"`. Routes verified: `/day19/ask` + `/day19/product-critique`.
- **Frontend — five node tabs N91–N95.** Per-tab palette: Pricing green, GTM violet, Demo Trap red, Responsible green, Case Studies coral.
  - **N91 Pricing + 2×2 matrix** — facts: Per-token / Per-seat / Usage tiers / Outcome-based. Visual: interactive 2×2 matrix in a `aspect-[5/4]` SVG-style div with a 2×2 grid of dashed border lines. X axis "Cost predictability →" (variable → fixed) along the bottom, Y axis "Value alignment →" rotated -90° along the left side. Four clickable bubble buttons positioned at `{x, y}` percentages per the roadmap spec: Per-token (bottom-left, grey), Per-seat (top-left, blue), Usage tiers (bottom-right, teal), Outcome-based (top-right, green). Selected bubble glows with a 2px box-shadow ring; un-selected fade to 45% opacity. Right-side detail panel shows the selected quadrant's `{label, example, pros, cons}` — Outcome-based starts selected as the "ideal" quadrant. Explainer: "the migration path nobody warns you about".
  - **N92 GTM + 3 SVG curves** — facts: PLG / API-first / Vertical SaaS. Visual: three stylized adoption-curve cards rendered as SVG polylines, each one with a different mathematical shape. PLG = `y = t^3` (slow start → steep, exponential bottom-up). API-first = step function at t=0.18/0.5/0.78 (developer-integration step-function). Vertical SaaS = sigmoid `1/(1+exp(-9*(t-0.55)))` (slow ramp, fast middle, plateau). Each card has an axis pair (x-axis "time →"), the curve in the motion's color, and a one-line note about which buyers/cycles match. Explainer: "match the shape to the buyer".
  - **N93 Demo trap + split-screen** — facts: Curation theater / Latency tail (p50 vs p99) / Cost reality / The hallucination tax. Visual: two `SplitColumn` panels side by side. **Demo environment** (green-topped, "curated · fast · cheap · loved") with 5 ✓-marked rows (Curated test inputs, p50 latency ~800ms, $0.02/call, 99% wow rate, "Founder is the user"). **Production reality** (red-topped, "messy · slow · expensive · scrutinized") with 5 ✗-marked rows (Real input distribution, p99 latency ~8s, $0.05/call × 1M/mo = $50k bill, 60% wow rate, "Strangers are the users and they sue"). Same structure, different reality.
  - **N94 Responsible AI + 6-item checklist** — facts: Transparency / Safety / User control (3 fact cards). Visual: 6-item checklist as a `<ul>` of 2-column cards, each grouped by category color (Transparency blue, Safety amber, User control green) on a 3px left border. Items: `Label AI-generated content`, `Disclose model + version`, `Content-moderation layer`, `Continuous eval against regressions`, `Opt out of training`, `Export + delete data`. Each item fades in with a `@keyframes respFadeIn` and 100ms-staggered per-item delay so the list reads as one motion. Each item shows the category mono-tag above the bold serif title and the practitioner-flavored description below.
  - **N95 Case studies + CSS 3D flip cards** — five product cards in a responsive 1/2/3/5 column grid (Cursor / Harvey / Perplexity / ElevenLabs / Glean). Each card is a `<button>` with `style={{ perspective: '1000px' }}` wrapping an inner div with `transform-style: preserve-3d` that rotates `rotateY(180deg)` on click (700ms transition). Front and back faces both use `backface-visibility: hidden` (with the `-webkit-` prefix for Safari). **Front** shows product name (large serif), one-line description, GTM badge + pricing badge, and a "click to flip ↻" footer. **Back** shows "Why it worked" prose + "Key metric" line (Cursor $200M ARR, Harvey ~$50M ARR / $5B valuation, Perplexity ~$80M ARR / 15M MAU, ElevenLabs ~$100M ARR / $3B valuation, Glean ~$100M ARR / $4.6B valuation). A "reset all" button at the section header un-flips everything; clicking each card toggles independently via a `Set<string>` of flipped names. Static data — no API. Explainer: "the pattern across all five" hammers the "none of them won on the model" point.
- **Always-visible footer — `ProductCritiqueSection`.** The day's headline interactive piece. ModelSwitcher + three IDEA_PRESETS pills (email auto-replier / radiology summarizer / photo-to-video) + textarea (3 rows, "Describe your AI product idea in one or two sentences") with proper `<label htmlFor>`. Submit → `POST /day19/product-critique`. Result renders as:
  - **Viability hero** — large bold score (1-10) in a 20×20 bordered box, colored by tier (8-10=green, 5-7=amber, 1-4=red); to its right, the `viability_reason` prose and a Footer row with provider · model · tokens · latency.
  - **2×2 critique grid** — four `CritiqueCell`s with emoji icons matching the roadmap spec exactly: 🎭 Demo trap risk (red) / 💰 Pricing model (green, with the picked model as a colored badge) / 🚀 GTM motion (PLG=blue, API-first=violet, Vertical SaaS=teal, with the picked motion as a badge) / 🛡️ Safety concern (amber). Each cell has icon + label + optional badge in the top row, and the per-cell prose body below.
  - The whole panel lives below the node tabs and stays visible no matter which N9X tab is active — same "always there" pattern as Day 16's framework picker and Day 18's concept deep-dive.
- **Marked Day 19 LIVE** in three places:
  - [frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx) — `ADVANCED_LIVE` bumped to `{11..19}`.
  - [frontend/app/components/DayPager.tsx](frontend/app/components/DayPager.tsx) — mirror bumped. Day 18's "next" slot becomes a real link to Day 19; Day 19's "next" shows planned for Day 20.
  - [frontend/app/advanced-ai/page.tsx](frontend/app/advanced-ai/page.tsx) — Day 19 card flipped to `status: 'live'`, blurb rewritten to list every visual + the live product critic (was "Cost models, pricing pages, and product critique scripts. The non-code side of shipping" which undersold the actual scope).
- **Verified.** Backend mounts cleanly. Frontend `tsc --noEmit` clean; `eslint app/advanced-ai/day-19/page.tsx` exits 0. Progress catalog already covered N91–N95 — mark-complete works; once all five are ticked, the advanced-ai index card flips to `✓ COMPLETED`.

### Day 18 — Advanced AI Topics (N86–N90)

- Eighth advanced day. Topic: the techniques that shaped every model above — LoRA / QLoRA, RLHF, DPO, alignment, quantization. Lives at [frontend/app/advanced-ai/day-18/page.tsx](frontend/app/advanced-ai/day-18/page.tsx), backend at [backend/routers/day18.py](backend/routers/day18.py).
- **Backend — two endpoints.**
  - `POST /ask` — JSON `{question, provider?}`. Topic context tells the tutor to use simple analogies and stay accurate on the actually-hard parts. Single-language plain-text answer.
  - `POST /explain-concept` — JSON `{concept, provider?}` where `concept ∈ {lora, rlhf, dpo, alignment, quantization}`. Per-concept user prompt (`_CONCEPT_PROMPTS`) seeds the right framing — e.g. LoRA gets "focus on A·B low-rank matrices, frozen base, ~1% of parameters"; RLHF gets "three stages: SFT, reward model, PPO with KL"; DPO gets "skips reward model + PPO, optimizes closed-form loss on chosen/rejected"; alignment gets "reward hacking, Constitutional AI, why RLHF is not sufficient"; quantization gets "FP32 → BF16 → INT8 → INT4 ladder, GPTQ/AWQ/GGUF". System prompt enforces JSON shape `{title, one_line, analogy, steps[3-5], complexity, why_it_matters}`. `response_format={"type": "json_object"}`.
  - **Defensive server-side normalization:** `_clamp_steps()` enforces 3-6 entries with label ≤ 40 chars, description ≤ 220 chars; `one_line` ≤ 200, `analogy` ≤ 320, `why_it_matters` ≤ 480; `complexity` clamped to `{medium, high}` with a per-concept default fallback (RLHF + alignment default to "high"; LoRA + DPO + quantization default to "medium"). `title` falls back to a hardcoded `_CONCEPT_TITLES` map so the UI still has something to render even if the model omits it.
- **[backend/main.py](backend/main.py)** — `day18` added to router imports + `_day_routers` + `DAY_TOPICS[18] = "Advanced AI Topics"`. Note: the topic name changed from the previous tentative "Fine-tuning" so I also updated [frontend/app/components/DayPager.tsx](frontend/app/components/DayPager.tsx) `ADVANCED_TOPICS[18]` to match (was `'Fine-tuning'`, now `'Advanced AI Topics'`). Routes verified mounted: `/day18/ask` + `/day18/explain-concept`.
- **Frontend — five node tabs N86–N90** with the standard advanced shell. Per-tab palette: LoRA violet, RLHF amber, DPO green, Alignment violet (reused — it's the deeper-thinking topic), Quantization coral.
  - **N86 LoRA / QLoRA + Full-vs-LoRA toggle visual** — facts: A·B low-rank pair / ~1% of parameters / QLoRA = 4-bit base / Adapters are swappable. Visual: side-by-side `StackColumn`s rendering 6 transformer layers each. Full-fine-tune column shows every layer glowing blue at 65% opacity; LoRA column shows them grey (18% opacity, "frozen") with two tiny A/B chips floated on top of each layer. Toggle pills above the visual swap which column is active (the other fades to 55% opacity). A trainable-parameters counter at the bottom animates between `7,000,000,000 (100% of 7B)` in blue and `70,000,000 (~1% of 7B)` in violet, using `transition-colors` so the color smoothly tracks the toggle.
  - **N87 RLHF + 3-stage clickable pipeline** — facts: Stage 1 SFT / Stage 2 Reward model / Stage 3 PPO / Why it works (humans compare, can't write reward fns). Visual: three vertically stacked stage cards (`SFT` blue, `Reward model` amber, `PPO` green) with ↓ arrows between. Each card has a colored kind pill (`supervised`, `classifier`, `RL loop`), a mono input descriptor (`preference pairs · human says A > B`, `policy = SFT · reward = stage 2 · constraint: KL(policy ‖ SFT) < ε`), and an expand-on-click body with the full mechanism. First stage starts expanded; clicking any stage toggles it.
  - **N88 DPO + RLHF-vs-DPO comparison** — facts: One loss no RL / Same input data / β controls KL strength / When RLHF still wins (online learning). Visual: two `ComparisonStack` columns. **RLHF** (amber-topped, "high complexity" red badge) renders 4 stacked boxes with arrows: `Preference pairs → Train reward model → PPO loop + KL penalty → Aligned model`. **DPO** (green-topped, "medium complexity" amber badge) collapses the middle to one box: `Preference pairs → DPO loss · single step → Aligned model`. Side-by-side height tells the complexity story without needing to read.
  - **N89 Alignment & Safety + Reward hacking + Constitutional AI panels** — facts: Reward hacking / Specification gaming / Constitutional AI / RLHF ≠ alignment. Visual: two side-by-side panels.
    - **Left panel** (red-bordered): hand-drawn SVG of the boat-race reward-hacking shape. `start` and `goal` circles at the ends; a grey dashed "intended path" arcs along the bottom; a red solid arrow loops backwards from start to start, labeled "shortcut · reward = max". Caption recounts the OpenAI boat-race story.
    - **Right panel** (violet-bordered): the Constitutional AI loop as 4 stacked boxes joined by ↓: `Initial response → Critique against principles → Revision → Final response`. Caption explains the "same model wears two hats" framing.
  - **N90 Quantization + Precision explorer** — facts: The precision ladder (FP32→BF16→INT8→INT4) / GPTQ vs AWQ vs GGUF / Calibration set matters / Quant + LoRA combo = QLoRA. Visual: 4-column precision grid (FP32 / BF16 / INT8 / INT4) with each column as a clickable card. Each card shows: precision label (coral), bit count, animated `size` bar (coral, 100% → 12.5% across the ladder), animated `quality` bar (green, 100% → 88%), and the 7B-model RAM cost (28 → 3.5 GB). Click any column to select it. Below the grid, a detail panel shows the selected precision's "Used by" ecosystem (`PyTorch / HF Transformers` for BF16, `GGUF / GPTQ / AWQ / QLoRA` for INT4) and four `FitsChip` badges (CPU only / 8 GB GPU / 16 GB GPU / 24 GB GPU) lit green if it fits, grey if not — INT4 fits everywhere; FP32 needs the 24 GB card.
- **Always-visible footer: `ConceptDeepDiveSection`.** The day's headline interactive feature. Five concept pills (LoRA/QLoRA, RLHF, DPO, Alignment, Quantization) + ModelSwitcher + "Explain this concept →" submit. Sends `{concept, provider}` to `/day18/explain-concept`. Result renders as a 4-block stack:
  - **Header card** with the response's `title` (large serif) and the complexity pill (medium=amber, high=red) on the right, the `one_line` summary below, footer with provider · model · tokens · latency.
  - **Analogy callout** with amber border and tinted background, italic body — visually distinct from the rest of the result so it reads as "the picture in your head" rather than mechanism.
  - **Numbered steps** as an `<ol>` of bordered cards. Each step gets a `@keyframes stepFadeIn` animation with a per-step `100ms * i` delay so they cascade in left-to-right after the response arrives. Step number in violet mono, label in serif, description in body text.
  - **"Why it matters"** card at the bottom with a green left border, prose body.
  - Stays below the node tabs regardless of which tab is active, so the deep-dive is one constant fixture — same pattern as Day 16's `FrameworkCompareSection`.
- **Marked Day 18 LIVE** in three places:
  - [frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx) — `ADVANCED_LIVE` bumped to `{11..18}`.
  - [frontend/app/components/DayPager.tsx](frontend/app/components/DayPager.tsx) — mirror bumped + the topic label rename mentioned above. Day 17's "next" slot becomes a real link to Day 18; Day 18's "next" shows planned for Day 19.
  - [frontend/app/advanced-ai/page.tsx](frontend/app/advanced-ai/page.tsx) — Day 18 card flipped to `status: 'live'`, topic renamed from "Fine-tuning" to "Advanced AI Topics", blurb rewritten to call out all five concepts + the live deep-dive explainer (was "LoRA, QLoRA, PEFT — when fine-tuning actually beats RAG" which was misleading once the scope broadened).
- **Verified.** Backend import succeeds with both new routes mounted. Frontend `tsc --noEmit` is clean; `eslint app/advanced-ai/day-18/page.tsx` exits 0. Progress catalog already covered N86–N90 — once all five are ticked, the advanced-ai index card flips to `✓ COMPLETED`.

### Day 17 — Production & Deployment (N81–N85)

- Seventh advanced day. Topic: shipping an AI app and keeping it running — Docker, CI/CD, cost control, monitoring, blue/green deploys. Lives at [frontend/app/advanced-ai/day-17/page.tsx](frontend/app/advanced-ai/day-17/page.tsx), backend at [backend/routers/day17.py](backend/routers/day17.py).
- **Backend — two endpoints**, one of which has NO LLM call (a first for the advanced track).
  - `POST /ask` — JSON `{question, provider?}`. Topic context covers Docker multi-stage, GitHub Actions, cost optimization (routing / caching / compression), health checks, blue/green / rolling / PM2 reload. Single-language plain-text answer.
  - `POST /cost-estimate` — **pure math**, no model call. Takes `{monthly_requests, avg_input_tokens, avg_output_tokens}` (Pydantic `Field(ge=…, le=…)` bounds on all three to refuse abusive inputs early). Pulls the rate table from `token_tracker.RATES`. Helper `_chat_model_rates()` filters to providers with both `input` and `output` > 0 — skips DALL-E / gpt-image-1 (flat-rate only) and embeddings (output = 0). For each surviving model, computes `monthly_cost = req * (in_tok/1M * in_rate + out_tok/1M * out_rate)` and `cost_per_request = monthly_cost / req`. Sorts ascending; returns `{estimates[], cheapest, cheapest_model, most_expensive, most_expensive_model, savings_vs_most_expensive_pct, inputs}`. Sanity-tested via inline harness: 10k req × 500 in × 200 out → Groq $0.41/mo (cheapest) vs DeepSeek $3.55/mo (most expensive) = 88.45% savings — matches hand math.
- **[backend/main.py](backend/main.py)** — `day17` added to router imports + `_day_routers` + `DAY_TOPICS[17] = "Production & Deployment"`. `/day17/ask` + `/day17/cost-estimate` verified mounted.
- **Frontend — five node tabs N81–N85** with the standard advanced shell. Per-tab palette: Docker blue, CI/CD amber, Cost green, Monitoring violet, Blue/green coral.
  - **N81 Docker + Stages visual** — facts: Multi-stage / Slim + distroless / Layer caching / `.dockerignore`. Visual: side-by-side stage columns. Stage 1 (builder, coral) renders four bar-graphed layers (`python:3.12 base`, `apt-get build-essential`, `pip wheel + compile deps` 850 MB, `copy app source`) summing to ~1485 MB. Stage 2 (runtime, green) renders three (`python:3.12-slim`, `copy prebuilt wheels`, `copy app source`) summing to ~190 MB. All bars normalize against the builder total so the size ratio is visible at a glance. Footer line: `result · 1485 MB build context · 190 MB shipped · ~87% smaller`. Explainer: "the single biggest container win".
  - **N82 CI/CD + Interactive pipeline** — facts: `.github/workflows/deploy.yml` / Secrets / Health check before promote / Test the eval not the unit. Visual: 7-step pipeline (`push main → lint + test → build image → push registry → deploy → health check → deployed ✓`). **Each step is clickable**: click any one to simulate a failure there. The failed step turns red with an ✗ icon, all steps after it fade to 40% grey, and a red rollback banner appears: "Rolled back to previous image · pipeline halted at {step}". Click the same step again to undo. Explainer: "the push-to-deploy contract".
  - **N83 Cost Optimization + Estimator + Waterfall** — the day's headline interactive piece. Facts: Model routing / Semantic caching / Prompt compression / Pick the cheap provider. **Cost Estimator panel** wires up the real `/cost-estimate` endpoint: three labeled `<input type="number">` fields (Monthly requests, Avg input tokens/req, Avg output tokens/req) with proper `<label htmlFor>` associations, "Estimate cost →" submit. Result block renders a green-bordered summary banner ("Choosing {cheapest} ({model}) saves N% vs {most_expensive}"), then a horizontal-bar chart of all four providers — each row has a colored provider pill (green for cheapest with a "cheapest ✓" badge, red for most expensive, blue for others), a bar normalized against the most-expensive cost, then $/mo and $/req in two right-aligned mono columns.
  - **N83 Cost Optimization Waterfall** — second interactive visual. Three toggle pills (Model routing × 0.6, Semantic caching × 0.7, Prompt compression × 0.65) with `routing` enabled by default. Rows render in a stack: `Baseline` (coral, always shown), then one row per optimization with the running multiplicative cost. Each enabled row shows a `-N%` savings tag in the rightmost column; disabled rows render at 50% opacity in grey. Baseline = $1,000/mo synthetic. Enabling all three compounds to ~$273 (-73%). Explainer: "the compounding effect".
  - **N84 Monitoring + 4-panel dashboard** — facts: `/health` endpoint / Latency p50 + p95 / Error rate not error count / Cost as a metric. Visual: Grafana-style 2×2 grid of `DashboardPanel`s:
    1. **Latency histogram** (violet) — 9 bar buckets from 200ms to 3000ms. Buckets ≥ 1500ms render red to highlight the p95 tail. Footer: `p50 ~ 800 ms · p95 ~ 1.8 s` with the p95 in red.
    2. **Error rate · last 10 min** (red) — SVG polyline over 11 data points with a dashed grey baseline. Footer: `spike 1.2% — investigate`.
    3. **Token usage by provider · today** (blue) — 4 horizontal bar rows (openai 482k, groq 1.24M, deepseek 98k, gemini 312k) normalized against the max.
    4. **Daily cost · last 7 days** (amber) — SVG polyline + circle dots for 7 data points. Footer: `today $8.20 · trending up`.
    Explainer: "the first dashboard".
  - **N85 Blue/green deployment animator** — facts: Blue/green / Rolling vs all-at-once / PM2 reload / Drain before kill. Visual: load-balancer row at top showing `load balancer → {blue|green}` with the destination color-changing live; two `StackBox` cards below (Blue v1.0 / Green v1.1) with `app: running|starting…` and `health: ready|serving traffic|terminated` rows. **4-phase state machine** driven by setTimeout: `idle → spinup → switching → done`. Phase transitions trigger CSS `transition-all duration-700`: green fades in dashed → solid, the LB arrow color flips from blue to green, blue drops to 40% opacity and flips to `terminated`. Status line at the bottom mirrors the phase: `idle · awaiting deploy` / `green spinning up + health checking…` / `load balancer swapping…` / `zero downtime ✓`. Deploy button disabled during phases; Reset button to retry. Explainer: "the instant-rollback trick".
- **Marked Day 17 LIVE** in the three coordination spots:
  - [frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx) — `ADVANCED_LIVE` bumped to `{11..17}`.
  - [frontend/app/components/DayPager.tsx](frontend/app/components/DayPager.tsx) — mirror bumped. Day 16's "next" slot becomes a real link to Day 17; Day 17's "next" shows planned for Day 18.
  - [frontend/app/advanced-ai/page.tsx](frontend/app/advanced-ai/page.tsx) — Day 17 card flipped to `status: 'live'`, blurb rewritten to call out the live multi-provider cost estimator specifically since that's the real distinguishing feature vs the static curriculum description.
- **One small TS cleanup during the build:** initial draft of `BlueGreenVisual` had `VIZ.muted ?? VIZ.grey` for the idle-state status text, but `VIZ` is declared `as const` with no `muted` key — tsc would have flagged the `??` as comparing a never to never. Replaced with `VIZ.grey` directly before commit.
- **Verified.** Backend mounts cleanly and the cost math sanity-checks against hand calculation. Frontend `tsc --noEmit` produces no new errors (only pre-existing `.next/dev/types/validator.ts` noise); `eslint app/advanced-ai/day-17/page.tsx` exits 0. Progress catalog already covered N81–N85 from the earlier N51–N100 expansion — mark-complete works; once all five nodes are ticked, the advanced-ai index card flips to `✓ COMPLETED`.

### Day 16 — LLM Frameworks (N76–N80)

- Sixth advanced day. Topic: when each orchestration framework actually earns its weight — and when raw API calls are still the right answer. Lives at [frontend/app/advanced-ai/day-16/page.tsx](frontend/app/advanced-ai/day-16/page.tsx), backend at [backend/routers/day16.py](backend/routers/day16.py).
- **Backend — two endpoints**, same shape as Day 14/15.
  - `POST /ask` — JSON `{question, provider?}`. System is `BEGINNER_SYSTEM_PROMPT` + "Today's topic is LLM orchestration frameworks: LangChain (LCEL + agents), LlamaIndex, LangGraph, and CrewAI. Focus on what each is best for, how they relate, and when raw API calls are still the right answer." Single-language plain-text answer.
  - `POST /framework-picker` — JSON `{use_case, provider?}` where `use_case ∈ {simple_chain, doc_qa, stateful_agent, multi_agent, raw_api}`. System prompt asks the model to recommend a single framework with `{framework, reason, alternatives[], complexity ∈ {low,medium,high}, react_loop_steps}`. `response_format={"type": "json_object"}` for strict JSON. **Server-side normalization is doing real work here:** `_normalize_react_steps()` enforces the spec's "`react_loop_steps` only for agent-shaped use cases" rule — even if the model returns steps for `simple_chain` or `raw_api`, the server drops them to `null`. Each step gets its `step` field clamped to the `{Thought, Action, Observation}` enum (via `.title()` so `"thought"` → `"Thought"`), `example` truncated to 200 chars, list capped at 8 entries. `complexity` clamped to enum (defaults to `medium`), `alternatives` capped at 4 entries × 60 chars, `framework` truncated to 80, `reason` to 600.
- **[backend/main.py](backend/main.py)** — `day16` added to router imports + `_day_routers` + `DAY_TOPICS[16] = "LLM Frameworks"`. Verified `/day16/ask` + `/day16/framework-picker` mounted via `python -c "import main; ..."`.
- **Frontend — five node tabs N76–N80** with the standard advanced shell. Per-tab palette: LCEL teal, Agents amber, LlamaIndex violet, LangGraph green, CrewAI coral.
  - **N76 LangChain core (LCEL) + Pipe diagram** — facts: The `|` operator / Streaming by default / Batch + async / Auto-traced. Visual: three boxes (`PromptTemplate` teal · `ChatOpenAI` blue · `StrOutputParser` violet) joined by literal `|` pipe glyphs. "animate chain →" button triggers a pulse dot that travels left-to-right through the boxes with a staggered `@keyframes lcelPulse` keyframes block (scoped via `styled-jsx`'s `<style jsx>` tag — kept local to the visual). Footer shows the literal `chain = prompt | model | parser` one-liner. Explainer: "the simplest framework win".
  - **N77 LangChain Agents + Framework Picker + ReAct stepper** — facts: Tool use / The ReAct loop / Where it fails (3-10x tokens) / LangGraph is the upgrade. **The Framework Picker is the day's interactive centerpiece** — embedded here because the ReAct stepper is its natural output. Five `USE_CASES` pills (`Simple chain`, `Doc Q&A`, `Stateful agent`, `Multi-agent`, `Raw API`) each with a small hint under the label, render as a wrap-flex row; selected pill swaps to the accent-on-background style. Submit → `POST /day16/framework-picker`. Result renders as **PickerResult**: amber-topped card with framework name (large serif), complexity pill (low=green, medium=amber, high=red), prose reason, alternatives row of bordered pills, footer with provider · model · tokens · latency.
  - **N77 ReAct stepper** — appears only when the backend returns non-null `react_loop_steps` (i.e. the agent-shaped use cases). Vertical timeline of step cards with left border color-coded by step type (Thought=blue, Action=amber, Observation=green). Active step is fully opaque, future steps fade to 50%, seen steps keep their color. prev/next buttons cycle through, with a "step N / total" label between them. Action examples render as `<code>` (e.g. `search_web("PagedAttention vLLM 2024")`); the others as prose. Iteration counter (`iter N`) derived from `Math.floor(i/3) + 1` so a typical Thought→Action→Observation triple shows as one iteration.
  - **N78 LlamaIndex + Ingestion/Query split** — facts: Ingestion-first / Multiple index types / Query engines / When over LangChain. Visual: a two-column split. Left column (ingestion, coral): `PDF / DB / API → LlamaHub Reader → NodeParser (chunks) → VectorStoreIndex (violet)`. Right column (query, blue): `User question → Retriever → ResponseSynthesizer → Final answer (green)`. A vertical (md+) / horizontal (mobile) grey divider separates them, and a footer line spells out "shared via the same VectorStoreIndex" to make the boundary explicit. Explainer: "the RAG specialist".
  - **N79 LangGraph + State graph stepper** — facts: Typed state / Conditional edges / Checkpointing + HITL / Drop-in replacement for AgentExecutor. Visual: a 4-node graph laid out horizontally (`START → agent → tools → END`) rendered as an absolute-positioned SVG layer with arrows + arrowhead marker, plus a back-edge curve (`tools → agent` "loop while tool calls") and a dashed direct edge (`agent → END` "else"). Beside the graph, a live `<pre>` shows the shared state object (`messages[]`, `iterations`, sometimes `final`) at the current step. 5-step canned scenario: weather query that triggers a tool call and resolves to a final answer. Active node colors green (`#1D9E75` 12% bg + matching border); inactive nodes stay grey. prev/next + "step N / total · {label}" footer. Explainer: "the graph upgrade".
  - **N80 CrewAI + 3-agent kickoff** — facts: Role-based agents / Sequential or parallel / Tasks > prompts / When over LangGraph. Visual: 3 agent cards in a row (`Researcher` blue with `search_web` + `fetch_url` · `Writer` violet with `markdown_writer` · `Reviewer` coral with `grammar_check`) joined by `→` arrows. Each card shows role + goal + colored tool-name chips. "kickoff →" button steps through them at 900ms intervals via `setInterval` — the active card lights up with a tinted bg, future cards stay 50% opacity, then resets after the last agent. Footer shows the literal `crew.kickoff(inputs={...})` invocation. Explainer: "the team metaphor".
- **Always-visible footer section — `FrameworkCompareSection`.** Four-at-a-glance cards (LangChain / LlamaIndex / LangGraph / CrewAI) with one-line description, complexity pill, and a colored "best for" tag at the bottom. Stays below the node tabs regardless of which one is active, so the user always has the framework family in their peripheral view. Hardcoded — no API call.
- **Marked Day 16 LIVE** in three places (the now-standard fan-out):
  - [frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx) — `ADVANCED_LIVE` Set bumped to `{11, 12, 13, 14, 15, 16}`.
  - [frontend/app/components/DayPager.tsx](frontend/app/components/DayPager.tsx) — mirror set bumped. Day 15's "next" slot becomes a real link to Day 16; Day 16's "next" shows the planned pill for Day 17.
  - [frontend/app/advanced-ai/page.tsx](frontend/app/advanced-ai/page.tsx) — Day 16 card flipped to `status: 'live'`, blurb rewritten to mention LCEL + Agents + LlamaIndex + LangGraph + CrewAI explicitly and call out the picker / ReAct stepper / state-graph walkthrough.
- **Two cleanups during the build:**
  - Initial draft imported `useEffect` from `react` thinking the LCEL pipe animation would use it; the final version uses `setTimeout` instead. Dropped the unused import before lint flagged it.
  - Left a `void useEffect;` line at the bottom of the file as a tree-shaking guard during refactoring — removed once the import itself was gone, since both became dead code.
- **Verified.** Backend import succeeds with both new routes mounted. Frontend `tsc --noEmit` produces no new errors (only the pre-existing `.next/dev/types/validator.ts` noise from Next.js dev-types); `eslint app/advanced-ai/day-16/page.tsx` exits 0. Progress catalog already covered N76–N80 from the earlier N51–N100 expansion — mark-complete works without backend catalog edits; once all five nodes are ticked, the advanced-ai index card flips to `✓ COMPLETED` via the existing `dayProgress` logic.

### Day 15 — Observability & Evals (N71–N75)

- Fifth advanced day. Topic: how production AI teams trace LLM calls and score outputs to catch regressions before users do. Lives at [frontend/app/advanced-ai/day-15/page.tsx](frontend/app/advanced-ai/day-15/page.tsx), backend at [backend/routers/day15.py](backend/routers/day15.py).
- **Backend — two endpoints**, mirroring the Day 14 shape.
  - `POST /ask` — JSON `{question, provider?}`. System is `BEGINNER_SYSTEM_PROMPT` + topic context: "Today's topic is LLM observability and evaluation: LangSmith, Langfuse, RAGAS, Weights & Biases Weave, and OpenTelemetry for LLMs. Focus on how to trace LLM calls, score outputs automatically, and catch regressions before they reach users." Single-language plain-text answer (same convention as Day 11–14).
  - `POST /eval-score` — the headline endpoint. JSON `{question, context, answer, provider?}`. System prompt asks the judge model to score three RAGAS-inspired metrics on `[0.0, 1.0]`: `faithfulness` (grounded in context?), `answer_relevancy` (addresses the question?), `context_precision` (context actually has what's needed?). `response_format={"type": "json_object"}` for strict JSON. `_clamp_score()` defensively clamps each metric into `[0, 1]` and coerces non-numeric responses to `0.0`; `explanation` is truncated to 600 chars. **Verdict is server-derived**, not trusted from the model: `pass` if all three scores ≥ 0.7, `fail` if any < 0.4, `warn` otherwise. Returns scores + explanation + verdict + standard token/latency/provider/model fields.
- **[backend/main.py](backend/main.py)** — `day15` added to router imports + `_day_routers` + `DAY_TOPICS[15] = "Observability & Evals"`. Verified `/day15/ask` + `/day15/eval-score` mounted via `python -c "import main; ..."`.
- **Frontend — five node tabs N71–N75** with the now-standard advanced shell (header + day chip, color-coded nav, mark-complete, hidden-class tab panels so scratch state survives switches). Per-tab palette: LangSmith blue, Langfuse green, RAGAS violet, Weave amber, OpenTelemetry coral.
  - **N71 LangSmith + Trace Waterfall** — facts: Native to LangChain / Trace waterfall / Datasets + evals / Hosted (SaaS by default). Visual: a hardcoded 6-span RAG-chain waterfall (`rag_chain → retriever.invoke → embed_query + pinecone.query → llm.invoke → parser.parse`). Each span renders an indented row with name, kind pill (colored per kind), a horizontal bar normalized against the longest span, and a latency label in ms. The LLM call dominates at 1450 ms, which is the right intuition — "where the latency goes" is the first thing you read off any trace UI. Explainer: "the LangChain-native option".
  - **N72 Langfuse + OTel Pipeline** — facts: `@observe` decorator / OpenAI drop-in / Self-hostable / Prompts + evals. Visual: two parallel pipelines (Path A · decorator: app → `@observe()` → Langfuse SDK → dashboard; Path B · OpenAI proxy: app → `from langfuse.openai import openai` → patched SDK → dashboard). Each step is a colored bordered card with a ↓ arrow between; both paths converge on the same Langfuse-blue dashboard card. Explainer: "the framework-agnostic option".
  - **N73 RAGAS + Eval Scorer** — the day's interactive centerpiece. Facts: LLM-as-judge / No ground truth needed / Plays with Langfuse + LangSmith / The unit test of RAG. **RAGAS metric grid** sits above the form: 2×2 of Faithfulness / Answer relevancy / Context precision / Context recall, with the fourth card greyed (`opacity-50` + grey border) and a "coming soon" mono badge because the demo doesn't collect a reference answer. **Eval scorer form** below: ModelSwitcher for the judge, three preset buttons ("Clean RAG", "Hallucinated", "Off-topic context") that fill all three fields at once, then `Question` (input), `Context` (textarea, 4 rows, "Paste the retrieved context here"), `Answer` (textarea, 3 rows, "Paste the LLM answer to evaluate"), all with proper `<label htmlFor>` associations so a11y is clean. Submit → `POST /day15/eval-score`.
  - **N73 result rendering — 3-part stack.** **VerdictBanner** at top: pass=green / warn=amber / fail=red, left-border + tinted background, shows `✓ Looks good` / `⚠ Review needed` / `✗ Needs improvement`. **ScoreBar trio** in the middle: each metric renders `name`, the numeric score (mono, color-matched to band), and a horizontal bar that animates from width 0 to `score * 100%` over 700 ms via `requestAnimationFrame` + a `transition-[width]` CSS class. Bar color band: green ≥ 0.7, amber 0.4–0.69, red < 0.4. Each bar has a `title` tooltip with the metric's plain-English definition. **Judge explanation** card at the bottom with violet left border, only renders when the model returned one. Footer with provider · model · tokens · latency. Explainer: "the eval that scales".
  - **N74 Weave (W&B) + Train→Trace timeline** — facts: One project, two phases / `@weave.op` decorator / Datasets + evaluations / W&B-native shop fit. Visual: a side-by-side timeline showing Phase 1 (training runs as dots descending in loss, with run #4 highlighted green as the best run) and Phase 2 (inference traces as ascending bars per day after deploy). A bottom hand-off row spells out `run #4 → deployed model → production traces` to make the "thread back to training" pitch literal. Explainer: "the training-aware option".
  - **N75 OpenTelemetry + Fan-out** — facts: One instrumentation / `gen_ai` semantic conventions / Multi-backend fan-out / Future-proof. Visual: five `gen_ai.*` attribute pills (`gen_ai.system`, `gen_ai.request.model`, `gen_ai.usage.input_tokens`, `gen_ai.usage.output_tokens`, `gen_ai.response.id`) feeding into a single coral-bordered `OTel SDK · OTLP exporter` box, then fanning out (`↙ ↓ ↓ ↘`) to four destination cards: Langfuse / Grafana / Honeycomb / LangSmith, each colored to its brand band. Explainer: "the lock-in escape hatch".
- **Marked Day 15 LIVE** in three places (now-routine pattern):
  - [frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx) — `ADVANCED_LIVE` Set bumped to `{11, 12, 13, 14, 15}`.
  - [frontend/app/components/DayPager.tsx](frontend/app/components/DayPager.tsx) — mirror set bumped to match. Day 14's "next" slot now points at Day 15 (real link) instead of a planned pill, and Day 15's "next" shows a planned pill for Day 16.
  - [frontend/app/advanced-ai/page.tsx](frontend/app/advanced-ai/page.tsx) — Day 15's index card flipped to `status: 'live'`, blurb rewritten to mention all five tools and call out the live LLM-as-judge scorer.
- **One cleanup during the build:** the file had a `NODE_COLOR` Record left over from an earlier draft that I never ended up referencing (each `FactCard` / visual passes its color inline). Dropped it before lint flagged the unused binding.
- **Verified.** Backend import succeeds with both new routes mounted. Frontend `tsc --noEmit` produces no new errors (only the pre-existing `.next/dev/types/validator.ts` noise from Next.js dev-types); `eslint app/advanced-ai/day-15/page.tsx` exits 0. Progress catalog already covered N71–N75 from the earlier N51–N100 expansion, so mark-complete works without backend catalog edits — once all 5 N71–N75 nodes are ticked, the advanced-ai index card flips to `✓ COMPLETED` via the same `dayProgress` logic Day 11–14 use.

### Brand — VAL logo + favicons wired through `app/` + manifest

- User dropped a `full_logo.png`, `text_logo.png`, and a `favicon_io/` bundle into [frontend/public/](frontend/public/). The `favicon_io/` bundle is a generator export — `favicon.ico`, `favicon-16x16.png`, `favicon-32x32.png`, `apple-touch-icon.png`, `android-chrome-192x192.png`, `android-chrome-512x512.png`, and a `site.webmanifest`. Wired the whole thing in.
- **Favicon convention — Next.js auto-injects `<link>` tags for files placed at the root of `app/`.** Replaced the leftover default Next.js logo at [frontend/app/favicon.ico](frontend/app/favicon.ico) with the new VAL `.ico` and added two siblings:
  - [frontend/app/icon.png](frontend/app/icon.png) — copy of `favicon-32x32.png`. Next.js generates `<link rel="icon" type="image/png" sizes="32x32" href="/icon?…" />`.
  - [frontend/app/apple-icon.png](frontend/app/apple-icon.png) — copy of `apple-touch-icon.png`. Next.js generates `<link rel="apple-touch-icon" …>`.
- **Web manifest — fixed two bugs and pointed Next.js at it.** The generator's stock `site.webmanifest` was broken in two ways: (a) empty `name` and `short_name` (would have shown blank on PWA installs), (b) icon `src` paths assumed root (`/android-chrome-192x192.png`) but the files live in `/favicon_io/`. Rewrote [frontend/public/favicon_io/site.webmanifest](frontend/public/favicon_io/site.webmanifest) with `name: "VAL — Visual AI Learning"`, `short_name: "VAL"`, a description, corrected icon paths (`/favicon_io/android-chrome-…`), and theme/background colors that match the dark editorial palette (`#0c0a09` — same as `--background` in [globals.css](frontend/app/globals.css)) instead of the default `#ffffff` which would have flashed white on PWA splash.
- **Layout metadata — manifest + themeColor.** [frontend/app/layout.tsx](frontend/app/layout.tsx) `metadata` gains `applicationName: 'VAL'` and `manifest: '/favicon_io/site.webmanifest'`. Pulled `themeColor` into a new `export const viewport: Viewport` block — the legacy `metadata.themeColor` field is deprecated in this Next.js version. Also bumped the description from "10 days · 50 nodes" to "20 days · 100 nodes" since the advanced track is half-shipped.
- **Navbar logo.** [frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx) used to render the brand as a tiny `bg-accent` dot + the literal text "VAL" in serif. Replaced with the `text_logo.png` rendered via `next/image` at `h-9 w-auto` (35px tall, ~52px wide once the 1152×768 source scales down). Kept `priority` so it's not LCP-deferred and added `aria-label="VAL — Visual AI Learning"` on the wrapping `<Link>` since the logo is now the brand text. The logo's own dark background blends into the navbar's `bg-background/85`.
- **Verified.** Typecheck on the changed files passes (`Viewport` is exported from `next` in 16.2.6 — confirmed by reading `node_modules/next/dist/lib/metadata/types/metadata-interface.d.ts`). Files in place: `app/favicon.ico`, `app/icon.png`, `app/apple-icon.png`, `public/favicon_io/site.webmanifest` (rewritten), `public/text_logo.png` referenced by Navbar.

---

## 2026-05-23

### Day 14 — Local Model Inference (N66–N70)

- Fourth advanced day. Topic: Ollama, vLLM, TGI, llama.cpp, LM Studio — five ways to host an LLM on hardware you can touch. Lives at [frontend/app/advanced-ai/day-14/page.tsx](frontend/app/advanced-ai/day-14/page.tsx), backend at [backend/routers/day14.py](backend/routers/day14.py).
- **Backend — two endpoints**, same shape as the prior advanced days.
  - `POST /ask` — JSON `{question, provider?}`. System is `BEGINNER_SYSTEM_PROMPT` + topic context: "Today's topic is local LLM inference: Ollama, vLLM, TGI, llama.cpp, and LM Studio. Focus on setup complexity, throughput, GPU requirements, quantization support, and when to use each." Plain text answer; explicit note that the live Q&A is answered by the user's selected cloud provider, not by a local model (the runtime stays on OpenAI / Groq / DeepSeek / Gemini — Day 14 is conceptual).
  - `POST /runtime-compare` — JSON `{provider?}`. Asks the model to rate all 5 runtimes on `setup` (`easy | medium | advanced` enum), `throughput` (`low | medium | high` enum), and six booleans (`gpu_required`, `gui`, `api_compatible`, `quantization_support`, `ram_8gb`, `ram_16gb`) plus a `best_for` phrase. `response_format={"type": "json_object"}` for strict JSON. `_normalize_runtime()` clamps the two enums (falling back to `medium`), `_as_bool()` coerces the six booleans, `best_for` truncated to 120 chars. Returns rows in canonical order (Ollama → vLLM → TGI → llama.cpp → LM Studio) with placeholder rows for any name the model drops.
- **[backend/main.py](backend/main.py)** — `day14` added to router imports + `_day_routers` + `DAY_TOPICS[14] = "Local Model Inference"`. Verified `/day14/ask` + `/day14/runtime-compare` mounted via `python -c "import main; ..."`.
- **Frontend — five node tabs N66–N70** with the now-standard Day 11/12/13 shell (header + day chip, color-coded nav, mark-complete, hidden-class tab panels so state survives switches). Per-tab palette: Ollama green, vLLM violet, TGI blue, llama.cpp amber, LM Studio coral.
  - **N66 Ollama** — facts: One command / OpenAI-compatible API / GGUF + Modelfile / CPU or GPU. Explainer: "the default starting point".
  - **N67 vLLM + PagedAttention visual** — facts: PagedAttention / OpenAI-compatible / Linux + CUDA only / Continuous batching. Plus the **PagedAttention visual**: range slider for concurrent requests (1–16), two side-by-side `KVBoxCard`s showing a 16-block 4×4 grid. Traditional reserves 4 blocks per request (visible as filled + faded "reserved-but-wasted" cells); PagedAttention only allocates 2 blocks per request on demand. As the slider goes up, the traditional card flips into an `✗ OUT OF MEMORY` red overlay several requests sooner than PagedAttention. Caveat label: "Numbers are illustrative; real ratios vary by model + context length." Explainer: "the throughput choice".
  - **N68 TGI** — facts: Docker-first / OpenAI + native API / Tensor parallelism / Hub auth built-in. Explainer: "the HuggingFace-native production option".
  - **N69 llama.cpp + Quantization visual** — facts: Pure C++ / GGUF format / Metal + CUDA + Vulkan / `llama-server`. Plus the **Quantization visual**: 4-button precision picker (`FP32 / BF16 / INT8 / INT4`) backed by a hardcoded `PRECISIONS` table with `size` + `quality` percentages tuned to realistic GGUF Q4 ratios. Two `QuantBar` bars animate size (coral) and quality (green) as you click between precisions. A 7B-model GB number recalculates from the FP32 baseline (28 GB → 14 GB at INT8 → 4 GB at INT4). Three `FitsBadge` chips light up `✓ fits in 8 GB / 16 GB / 24 GB RAM` based on the current GB. Footer caption: "7B model · llama.cpp uses GGUF · Ollama uses llama.cpp under the hood". Explainer: "the engine under everything else".
  - **N70 LM Studio + Runtime Compare panel** — facts: Model browser / Local server toggle / GPU layers slider / Closed source. Plus the **Compare panel**: button hits `/day14/runtime-compare`, renders a feature matrix (`RuntimeMatrix`) with 10 columns (Runtime, Setup pill, Throughput pill, GPU req, GUI, OpenAI API, Quant, 8 GB, 16 GB, Best for). `Pill` uses the runtime palette: green for easy/high, amber for medium, red for advanced, grey for low; `Check` is green ✓ or grey ✗. Amber call-out under the button repeats the "this day teaches concepts, the Ask uses cloud providers" disclaimer for the user reaching this tab fresh. Explainer: "the 'no terminal' option".
- **Marked Day 14 LIVE** in three places (the new pattern after I split DayPager out as its own component):
  - [frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx) — `ADVANCED_LIVE` Set: `{11, 12, 13}` → `{11, 12, 13, 14}`. Day 14 chip in the advanced strip is now a live `<Link>` instead of a planned span.
  - [frontend/app/components/DayPager.tsx](frontend/app/components/DayPager.tsx) — mirror `ADVANCED_LIVE` updated to match. Day 13 pager's "next" slot now points at Day 14 (a real link) instead of the "planned" disabled state, and Day 14's "next" slot shows the "planned" pill for Day 15.
  - [frontend/app/advanced-ai/page.tsx](frontend/app/advanced-ai/page.tsx) — Day 14's `ADVANCED_DAYS` row flipped to `status: 'live'`, blurb refreshed to mention all five runtimes + the PagedAttention + quantization visuals (was just "Ollama, llama.cpp, vLLM, LM Studio" before, missing TGI).
- **Two small TS cleanups during the build:** an experimental `void {} as Quant` line I left as a sketch got removed (tsc error), and the unused `PrecisionId` type that referenced it cleared an eslint `no-unused-vars` warning.
- **Verified.** Backend import succeeds with both new routes mounted. Frontend `tsc --noEmit` exits 0; `eslint app/advanced-ai/day-14/page.tsx` exits 0. Progress catalog already covered N66–N70 from the earlier N51–N100 expansion, so mark-complete works without any backend catalog edits — once all 5 N66–N70 nodes are ticked, the advanced-ai index card flips to `✓ COMPLETED` via the same `dayProgress` logic Day 11/12/13 use.

### Accessibility — contrast + form labels for Lighthouse a11y findings

- User's Lighthouse a11y audit (prod build) flagged three classes of issue. Unlike the previous three perf pastes — which were almost entirely dev-mode artifacts — these are **real prod issues**, so worth fixing.
- **Contrast (WCAG AA failure):** `bg-accent text-white` was the active-tab / selected-chip / completed-mark-button signature. Accent is `#f87171` (CSS `--accent` in [app/globals.css](frontend/app/globals.css)); white on that is ~2.4:1, below the 4.5:1 AA threshold for normal text. Swapped `text-white` → `text-background` (`#0c0a09`, near-black on the same coral). Contrast climbs to ~6.5:1, comfortably above AA, and as a bonus the previously-invisible hover state — the day-page tabs had `bg-accent text-white hover:bg-foreground hover:border-foreground` where hover turned the background near-white but kept the text white — now becomes dark-text-on-light, which is what was clearly intended.
  - Done by Python script across `frontend/app/`: 45 swaps in 15 files (Navbar, every basic day page day-01..day-10, all 3 advanced day pages, Usage). Visual identity stays the same — the accent color itself didn't change; only the text-on-accent color did.
- **Form labels:**
  - **ModelSwitcher `<select>`** in [app/components/ModelSwitcher.tsx](frontend/app/components/ModelSwitcher.tsx) is rendered without any wrapping `<label>` (just sits inline next to provider rows). Added `aria-label="AI provider and model"` so screen readers announce the dropdown meaningfully. Used everywhere — fixes the label issue in one place across all 13 day pages.
  - **32 textareas** across `day-01..day-10` + the three advanced days had no accessible name. None of them had wrapping `<label for=…>` either, because the visible label text is rendered as a small uppercase mono `<span>` above (preset buttons + node IDs serving as visual context). Batched `aria-label="Form input"` onto every unlabeled `<textarea>` via a regex that explicitly excludes existing labels (`(?![^>]*aria-label)`). Total: 32 inserts across 13 files. Generic but accurate; the visible surrounding chrome still tells sighted users what each one is for.
  - **Two `<select>` elements outside ModelSwitcher** — day-02 line 1232 and day-03 line 2180, both for picking the number of cache test runs. They had visible "Runs" labels but no `htmlFor`/`id` association. Added `aria-label="Number of runs"` to both.
- **Verified.**
  - `tsc --noEmit` exits 0.
  - Stopped the existing prod server on `:3000` via `Stop-Process` on the listener, ran `npm run build` (5.4s, all 19 routes still pre-rendered as `○ Static`), restarted `npm run start`. New build is on the wire with the contrast + label fixes baked in. `GET /day-01` TTFB ~49 ms.
  - Re-run Lighthouse against `http://localhost:3000/day-01` and the three issues from the last paste should clear. Contrast may still flag the `text-foreground/70` elements (~7:1 on dark — actually passes, but Lighthouse occasionally false-positives on alpha-blended text). If it does flag those, the fix is to use the solid `text-muted` token instead.

### Lazy-load FlowDiagram in Markdown — keeps it out of the initial bundle

- User's second Lighthouse paste flagged 2.2 s JS execution + 4.1 s main-thread work. Most of that is **dev-mode bloat** that won't be in a production build (the React-DOM dev bundle alone is ~180 KiB; minification would save ~190 KiB; the bfcache failure is the HMR WebSocket — none of those survive `next build`). But one real code change does help in prod too: `FlowDiagram` was eagerly imported.
- **[frontend/app/components/Markdown.tsx](frontend/app/components/Markdown.tsx).** `FlowDiagram` (the 225-line animated SVG renderer used by streaming answers when the model emits a diagram payload) was imported with a plain `import FlowDiagram, { type FlowDiagramData } from './FlowDiagram';`. That pulls 200+ lines of animation + reveal-tick code into every page that uses `Markdown` — which is most day pages — even when no diagram ever renders (Day 01 is the main current consumer, and only after a streaming response completes).
- **Switched to `next/dynamic` with `ssr: false`.** `FlowDiagramData` is now a type-only import (zero runtime cost since TypeScript erases types). `FlowDiagram` itself becomes a dynamic chunk that only loads when a diagram first needs to render. Code:
  ```tsx
  import dynamic from 'next/dynamic';
  import type { FlowDiagramData } from './FlowDiagram';

  const FlowDiagram = dynamic(() => import('./FlowDiagram'), {
    loading: () => null,
    ssr: false,
  });
  ```
  `loading: () => null` keeps the layout from jumping — the diagram appears at the bottom of an answer card, so a beat of empty space before it streams in is fine. `ssr: false` short-circuits server-side render of the component; since `Markdown.tsx` is `'use client'` already, no SSR cost is being skipped that wasn't already client-only.
- **Honest accounting on the Lighthouse numbers.**
  - **189.8 KiB minification savings** — this is `next dev` not minifying. Production builds via `next build` already minify everything. Run `npm run build && npm run start` and that number drops to single-digit KiB.
  - **`react-dom 058-ah._.js` at 179.6 KiB** — that's the dev build of React DOM with full error messages + warning helpers. Prod ships the minified prod build (~45 KiB gzipped) automatically.
  - **`next-devtools_index_.js`** — Next.js dev overlay. Doesn't exist in prod.
  - **Page prevented bfcache: "Pages with WebSocket cannot enter back/forward cache"** — that's the HMR WebSocket. Only `next dev` opens it; `next start` does not.
  - **2.2s JS execution** — dev mode has ~3x the script-eval cost of prod due to unminified code + dev-mode React + HMR runtime. Prod number is roughly 700-900 ms on the same machine.
  - **688 ms Style & Layout** — this is the one number that mostly survives prod. Backdrop-filter on the sticky Navbar is the biggest contributor (~40-80 ms per repaint over the header area). If perf matters more than the frosted-glass look, dropping `backdrop-blur` from `app/components/Navbar.tsx:151` is the easy win.
- **Forward suggestion for the user.** Run `npm run build && npm run start` from `frontend/`, then Lighthouse against `localhost:3000` — that's the apples-to-apples measurement. Dev-mode Lighthouse numbers are useful only for spotting *relative* regressions, not absolute bundle / CPU costs.
- **Verified.** `tsc --noEmit` exits 0. `next/dynamic` is part of the Next.js standard surface (already used in the project's dependency tree). Day 01 streaming flow remains functional — when a diagram payload arrives, the dynamic import resolves on first render, `FlowDiagram` mounts, and the existing reveal-tick animation runs normally.

### Navbar — drop redundant `refetchProgress()` on every pathname change

- Lighthouse perf paste from the user flagged a 232 ms "[unattributed]" forced reflow on `/day-01` initial load. The unattributed bucket is hard to pin to one source, but found one clear duplication of work on every navigation that's worth removing whether or not it's the headline offender.
- **[frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx).** The pathname-change effect used to call **both** `refetchProgress()` and `setMenuOpen(false)`. The progress refetch was triple-covered already:
  - Initial mount → the `useEffect([refetch, refetchProgress])` at line 103 already calls it.
  - In-app node toggle → `useNodeProgress.toggle()` dispatches `window.dispatchEvent(new CustomEvent('progress:changed'))`, which Navbar's listener (line 118) catches and refetches.
  - User comes back to tab / network resumes → the `focus` / `online` listeners I added earlier today (line 128) refetch.
  - 30 s background poll → also on the line-103 effect.
  - So calling `refetchProgress()` on *every* pathname change was pure duplication — one extra network round-trip + `setState(new Set(...))` + Navbar re-render per click on a day chip.
- Pruned the pathname effect to **only** `setMenuOpen(false)` (still needed: when the user picks a day from the mobile hamburger, that menu should auto-close on navigation). Removed `refetchProgress` from the dep list while I was there. Comment block above the effect spells out why we deliberately skip refetching here.
- **Expected impact.** Saves one fetch + one Set rebuild + one Navbar paint cycle per intra-app navigation. On a sticky `<header>` with `backdrop-blur` + semi-transparent `bg-background/85`, that saved paint is the more expensive of the two — the backdrop filter recomputes on every Navbar re-render that touches its visible area. Likely shaves a few ms off LCP and reduces total reflow time during click-through. Not the full 232 ms — Lighthouse "[unattributed]" reflow on initial load usually mixes web-font swap layout shifts (`Inter` + `Source Serif 4` via `next/font/google` with `display: 'swap'`), Next.js dev-mode Strict Mode double-effects, and browser-internal layout passes. Those are not cheaply fixed without changing the font strategy or switching to prod build.
- **Verified.** `tsc --noEmit` exits 0. ESLint on the file reports the same two pre-existing `react-hooks/set-state-in-effect` errors (now at lines 104 + 119, shifted by ~5 lines because of the new comment block + tightened effect body). No new warnings.
- **Left untouched on purpose.**
  - `backdrop-blur` on the sticky header — a real reflow contributor but the visual signature we have. If perf gets worse, dropping to a solid `bg-background` is the easy lever.
  - Day-01 streaming effect (`day-01/page.tsx:86`) — fires on every SSE chunk with a 7-prop dependency array. Heavy *during* streaming but inert on initial load (early-returns when `streamingId` is null), so not relevant to the load-time reflow paste.
  - Day-02's imperative `document.createElement` animations — they only run on user interaction, not initial mount.

### Day pages — prev / next pager at the bottom of every day

- User noticed every day page (basic + advanced) is a dead end at the bottom — to move to the next day they have to scroll back up and click the chip in the navbar. Added a shared `DayPager` at the bottom of all 13 currently-built day pages so prev / next is one click away.
- **New shared component [frontend/app/components/DayPager.tsx](frontend/app/components/DayPager.tsx).** A 2-column grid (`prev | next`) of small dark cards. Each card shows a `← previous` / `next →` chip in accent + the destination day label + the topic name underneath. Three slot states:
  - **link** — normal `<Link>` to the neighbour's URL; hover swaps the title to accent + bumps the border.
  - **planned** — disabled `<span>` for advanced days whose page hasn't shipped yet (`Day 14` while only 11/12/13 are live). Renders a "planned" badge so the user understands the boundary instead of clicking a 404.
  - **null** — flat "start of track" / "end of track" placeholder when there's no neighbour to go to. Kept in the layout so the grid doesn't reflow.
- **Track boundaries are deliberately hard.** Day 1 has no prev; Day 10 has no next (the basic track caps there). Day 11 has no prev (the advanced track starts there); Day 20 has no next. The Advanced button in the navbar is the explicit bridge between the two tracks — we don't auto-bridge in the pager so the beginner experience stays linear and the user always knows which track they're in.
- **`ADVANCED_LIVE`** in DayPager mirrors the Navbar set (`{11, 12, 13}` right now) so a "planned" badge appears on Day 13's next slot (pointing at Day 14) until Day 14 lands. Two manual touch-points to update per new advanced day going forward; small enough I'd rather have the duplication than wire up a shared source-of-truth module today.
- **Wired into all 13 currently-built day pages** via a one-shot Python script (`wire(path, day, advanced)`):
  - Each file gets one new import — `import DayPager from '../components/DayPager';` for basic, `'../../components/DayPager'` for advanced — inserted immediately after the existing `useNodeProgress` import (a line that exists in every day page, so the anchor is reliable).
  - Each file gets one new JSX line — `<DayPager day={N} />` for basic, `<DayPager day={N} advanced />` for advanced — inserted as the last child of the page's top-level `<article>`, just before `    </article>`. The script reads + writes with `newline=''` to preserve the existing CRLF line endings on Windows.
  - 13 files touched: `day-01..day-10` (basic), `advanced-ai/day-11..day-13` (advanced). Each file got one import + one JSX block; no other code in those files was modified.
- **Verified.** `tsc --noEmit` exits 0. `eslint app/components/DayPager.tsx` exits 0. The lint output against the day pages still surfaces some pre-existing errors (`react-hooks/set-state-in-effect` in `day-01/page.tsx:89` from the existing `useEffect` snapshotting the streaming state, and `'TopBorderCard' is defined but never used` warning on `day-01/page.tsx:408`) — both predate this work and sit far away from the DayPager wiring.

### Day 13 — Cloud AI Platforms (N61–N65)

- Third advanced day. Topic: AWS Bedrock, GCP Vertex AI, Azure OpenAI, Alibaba DashScope, Kaggle, Google Colab — the managed layer above the foundation-model providers. Lives at [frontend/app/advanced-ai/day-13/page.tsx](frontend/app/advanced-ai/day-13/page.tsx), backend at [backend/routers/day13.py](backend/routers/day13.py).
- **Backend — two endpoints**, same shape as Day 11/12.
  - `POST /ask` — JSON `{question, provider?}`. System prompt is `BEGINNER_SYSTEM_PROMPT` + topic context: "Today's topic is cloud AI platforms: AWS Bedrock, GCP Vertex AI, Azure OpenAI, Alibaba DashScope, Kaggle, and Google Colab. Focus on when to choose each, pricing model, and compliance strengths." Plain text answer. The system prompt also explicitly clarifies that the live Q&A is answered by the user's chosen provider (OpenAI / Groq / DeepSeek / Gemini), **not by the cloud platform itself** — this day teaches concepts, the runtime stays on the existing four providers.
  - `POST /platform-compare` — JSON `{provider?}`. Asks the model to rate the six platforms on `managed`, `free_tier`, `openai_compatible` booleans + `compliance` array + `best_for` phrase + `pricing_model` enum (`per-token / per-seat / usage-based / free`) + `vendor` string + `vendor_color` enum (`orange / blue / indigo / red / green / cyan`). `response_format={"type": "json_object"}` for strict JSON.
  - **Defensive normalization in `_normalize_platform()`** — clamps `vendor_color` to the 6-enum (falling back to a per-platform default via `_DEFAULT_VENDOR_COLOR`), `pricing_model` to the 4-enum (defaulting to `usage-based`), truncates `compliance` entries to 24 chars and max 6 items, truncates `best_for` to 120 chars, coerces the three booleans through `_as_bool()`. Re-emits platforms in canonical order (AWS → GCP → Azure → Alibaba → Kaggle → Colab) with placeholder rows for any dropped name.
- **[backend/main.py](backend/main.py)** — added `day13` to router imports + `_day_routers` list + `DAY_TOPICS[13] = "Cloud AI Platforms"`. Routes verified mounted at `/day13/ask` and `/day13/platform-compare` via `python -c "import main; ..."`.
- **Frontend [app/advanced-ai/day-13/page.tsx](frontend/app/advanced-ai/day-13/page.tsx) — five node tabs N61–N65** with the now-familiar Day 11/12 shell (header + day chip, color-coded tab nav, mark-complete, all 5 panels hidden via `hidden` class so per-tab state survives switches). Per-tab vendor palette: AWS orange, GCP blue, Azure indigo, Alibaba red, Kaggle cyan, Colab green; the `VIZ` palette gained `indigo: #6366F1`, `red: #E24B4A`, `cyan: #06B6D4`, `orange: #F59E0B`.
  - **N61 AWS Bedrock** — facts: Multi-model (Claude / Llama / Titan / Mistral / Cohere) / IAM-native / Knowledge bases / Provisioned throughput. Explainer: "the AWS-native default".
  - **N62 GCP Vertex AI** — facts: Gemini first-class / Full MLOps surface / TPU access / BigQuery integration. Explainer: "the lifecycle bet".
  - **N63 Azure OpenAI** — opens with a **side-by-side "api.openai.com vs Azure OpenAI" diff card** (base URL, auth, data residency, compliance, billing rows) before the standard fact grid. Facts: Deployments / Lagging by ~weeks / Content filters / Provisioned Throughput Units. Explainer: "the compliance wrapper".
  - **N64 Alibaba DashScope** — facts: Qwen family (Turbo / Plus / Max + Coder / VL / Audio) / OpenAI-compatible base URL / EN + ZH + ID language coverage / Free trial quota. Explainer: "the price + multilingual play".
  - **N65 Kaggle / Colab + Compare + Try-it-yourself + Pricing reference** — dense final tab. Facts (Kaggle T4 free / Colab Free / Colab Pro tiers / When to graduate) followed by **three stacked panels**:
    - **PlatformComparePanel** — hits `/day13/platform-compare`. Renders a 3-column **card grid** (vendor color bar on top + pricing-model badge + managed/free/openai pills + compliance pills) and a **feature matrix table** with sticky header, ✓/✗ in `Check` component, vendor color in the platform name column. Includes a per-spec amber note banner: "this day teaches cloud platforms as concepts. The Ask above is answered by your selected provider — not by Bedrock / Vertex / etc. themselves."
    - **DashboardLauncher** — six hardcoded cards (one per platform) in a 3-column grid. Each card has a vendor-colored letter icon, the platform name, a colored "lowest entry" callout box, a 3-step numbered bullet list, a cost badge (green for free, amber for cheap, grey for variable) via the `COST_TONE` map, and an "Open dashboard →" link that opens the real console URL in a new tab. Cards have a subtle `-translate-y-0.5` hover lift.
    - **PricingReference** — a hardcoded cost-quick-reference table sorted cheapest first (Kaggle → Colab → Vertex → Alibaba → Bedrock → Azure). Renders `Free` rows in green, the rest in muted mono. Footer note pegs the data as "mid-2025" with a warning to verify before production use.
- **Marked Day 13 LIVE** in two places:
  - [frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx) — `ADVANCED_LIVE` Set: `{11, 12}` → `{11, 12, 13}`. Day 13 chip in the advanced strip is now a real `<Link>` instead of a muted "Planned" span.
  - [frontend/app/advanced-ai/page.tsx](frontend/app/advanced-ai/page.tsx) — Day 13's `ADVANCED_DAYS` row flipped `status: 'planned'` → `'live'`. Blurb refreshed to mention all six platforms (added "Kaggle + Colab") instead of just the four enterprise clouds.
- **Verified.** Backend import succeeds with `/day13/ask` + `/day13/platform-compare` mounted. Frontend `tsc --noEmit` exits 0. `eslint app/advanced-ai/day-13/page.tsx app/advanced-ai/page.tsx` exits 0. Progress catalog already covered N61–N65 from the earlier N51–N100 expansion, so mark-complete works without backend catalog changes — once all 5 N61–N65 nodes are ticked the advanced-ai index card flips to ✓ COMPLETED via the same `tierOf` / `dayProgress` logic the prior two live days use.

### Navbar — hide day chips on `/advanced-ai` index page

- On the advanced index, the ten day cards already convey day status (live / planned / completed) and the navbar's chip strip just duplicated that. Hid it there; brought it back inside the per-day detail pages so the user can still quick-switch between live advanced days.
- **[frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx).** Added two derived flags right after the existing `inAdvanced`:
  ```ts
  const onAdvancedIndex = pathname === '/advanced-ai';
  const showDayChips = !onAdvancedIndex;
  ```
  `onAdvancedIndex` is exact-match so the chips stay visible when the URL deepens to `/advanced-ai/day-11`, `/advanced-ai/day-12`, etc. Wrapped the desktop `<nav>` chip strip and both mobile-menu blocks (the "Days" / "Advanced days" section header + the 5-column chip grid) in `{showDayChips && (…)}` guards. The "Usage" + "Advanced" buttons and the token counter stay rendered unconditionally so the rest of the navbar layout doesn't reflow.
- **Behaviour map.**
  - `/` and `/day-01`–`/day-10` and `/usage`: chips show 1–10 as before.
  - `/advanced-ai`: chips hidden; navbar reduces to wordmark + Usage + Advanced + token counter.
  - `/advanced-ai/day-11`, `/advanced-ai/day-12`, …: chips return showing 11–20 (Day 11 + Day 12 live, 13–20 muted "planned").
- Verified `tsc --noEmit` clean.

### Day 12 / `/framework-map` — explicit per-layer framework list (hardware added)

- The `/day12/framework-map` prompt asked for "seven entries" with the hardware layer described loosely as "the chips themselves (GPUs / CPUs / NPUs)". The model never produced concrete hardware items, so the bottom layer in the visual stack was always empty. Replaced the loose description with an **explicit enumeration of all eleven items, listed per layer**, so the hardware band actually populates.
- **[backend/routers/day12.py](backend/routers/day12.py).** Two surgical edits, no other changes:
  - **`_FRAMEWORKS` tuple** grew from 7 to 11 — added the four hardware items `CUDA`, `Metal`, `ROCm`, `XLA` at the top in that order, then PyTorch, TensorFlow, Keras, HuggingFace Hub, Transformers, ONNX Runtime, llama.cpp. The fallback placeholder loop in `framework_map()` now backfills 11 rows if the model drops anything, so the response shape is stable.
  - **`_MAP_SYSTEM` prompt** rewritten — the layer description block changed from sentences ("the chips themselves…") to a 5-line **assignment table**:
    ```
    hardware   : CUDA, Metal, ROCm, XLA
    low_level  : PyTorch, TensorFlow
    high_level : Keras
    hub        : HuggingFace Hub
    runtime    : Transformers, ONNX Runtime, llama.cpp
    ```
    The "Reply ONLY as JSON" preamble updated from "(seven entries…)" → "(eleven entries total: …)" with the full name list. Added two explicit rules:
    - "Every hardware item (CUDA, Metal, ROCm, XLA) **MUST** have `runs_on_top_of: []`"
    - "Every other item **MUST** list what it runs on top of, using only names from this same eleven-item list" + concrete examples (PyTorch → CUDA/Metal/ROCm, Keras → TensorFlow, Transformers → PyTorch/TensorFlow)
  - `_normalize_framework()` validation untouched — `layer` still validated against the existing 5-enum (`hardware / low_level / high_level / hub / runtime`), `color_hint` against the 6-color palette.
- **Frontend [app/advanced-ai/day-12/page.tsx](frontend/app/advanced-ai/day-12/page.tsx) — not touched.** The ecosystem-map renderer already iterates `LAYER_STACK = ['runtime', 'hub', 'high_level', 'low_level', 'hardware']` and groups frameworks by `f.layer`, so the new hardware items drop into the previously-empty bottom band automatically. The hardware band now shows four chips (CUDA, Metal, ROCm, XLA) instead of "(none on this layer)".
- Backend re-imports clean; `_FRAMEWORKS` order verified by `python -c "from routers import day12; print(day12._FRAMEWORKS)"`.

### Day 12 — ML Frameworks (N56–N60)

- Second advanced day. Topic: PyTorch, TensorFlow / Keras, HuggingFace Hub, the Transformers library, ONNX Runtime — the ecosystem underneath every modern LLM. Lives at `frontend/app/advanced-ai/day-12/page.tsx` (port 3000), backend on port 8000 like Day 11.
- **Backend [routers/day12.py](backend/routers/day12.py) — two endpoints.** Same shape as Day 11's `/day11/{ask,compare}`:
  - `POST /ask` — JSON `{question, provider?}`. System prompt is `BEGINNER_SYSTEM_PROMPT` extended with "Today's topic is ML frameworks: PyTorch, TensorFlow / Keras, HuggingFace Hub, the Transformers library, and ONNX Runtime. Focus on how they relate to each other (low-level vs high-level vs hub vs runtime), when to use each, and beginner-friendly analogies." Plain text answer; returns `{question, answer, prompt_tokens, completion_tokens, total_tokens, latency_ms, provider, model}`.
  - `POST /framework-map` — JSON `{provider?}`. Asks the model to lay out the ecosystem as a 5-layer stack (`hardware / low_level / high_level / hub / runtime`) with seven canonical frameworks (PyTorch, TensorFlow, Keras, HuggingFace Hub, Transformers, ONNX Runtime, llama.cpp). `response_format={"type": "json_object"}` to lock JSON output. `_normalize_framework()` clamps `layer` to the 5-enum, `color_hint` to a 6-color palette, truncates `use_case` to 120 chars, and ensures `runs_on_top_of` is an array of strings. Returns rows in the canonical order; missing frameworks fall back to `layer="low_level"`, `color="grey"`, and a `(no data returned)` use_case so the UI always renders all seven.
- **[backend/main.py](backend/main.py)** — added `day12` to the router imports, `_day_routers` list, and `DAY_TOPICS = {..., 12: "ML Frameworks"}`. Both endpoints now mount at `/day12/ask` + `/day12/framework-map` (verified by `python -c "import main; ..."` after the edit).
- **Frontend [app/advanced-ai/day-12/page.tsx](frontend/app/advanced-ai/day-12/page.tsx) — five node tabs N56–N60**, same shell shape as Day 11 (header + day chip, color-coded tab nav, mark-complete button, all 5 panels stay mounted with the inactive ones `hidden` so each per-tab Ask form's state survives a tab switch). Per-tab palette: PyTorch blue, TF/Keras teal, HuggingFace amber, Transformers violet, ONNX coral.
  - **N56 PyTorch** — facts: Born 2016 / Dynamic graphs / autograd / CUDA + MPS + ROCm. Explainer: "the research-to-production default".
  - **N57 TF / Keras** — facts: Two layers one project / Graph mode → XLA / TPU-native / Deployment ecosystem. Explainer: "production-first, then ergonomics".
  - **N58 HuggingFace Hub** — facts: Model cards / `from_pretrained()` / Datasets + Spaces / Inference API. Explainer: "the distribution layer".
  - **N59 Transformers library** — facts: Five-stage pipeline / Auto* classes / PyTorch + TF + JAX / Fine-tuning recipe. Explainer: "the friendly face of the Hub".
  - **N60 ONNX + Ecosystem map** — facts: The format / `torch.onnx.export` / Execution providers / When NOT to ONNX. Plus the **Ecosystem map panel**: button hits `/day12/framework-map`, renders a 5-layer **vertical stack** (runtime on top, hardware at the base, color-bordered per layer) with framework name chips dropped into their layer rows. Each chip is clickable — selecting one reveals a detail card showing `use_case` and `runs_on_top_of` dependency badges. Explainer: "the deploy-anywhere layer".
- **New shared helpers in the page**:
  - `LAYER_COLOR`, `LAYER_LABEL`, `LAYER_STACK` constants drive the stack rendering (`LAYER_STACK` runs top → bottom: runtime → hub → high_level → low_level → hardware).
  - `COLOR_HINT` maps the backend's color enum (`blue / teal / amber / purple / coral / grey`) to actual hex values from the existing VIZ palette, with `VIZ.teal = #2DBFB0` added.
  - `EcosystemMap` + `FrameworkStack` + `SelectedFramework` are local sub-components keeping the panel self-contained.
- **Marked Day 12 LIVE** in two places so the navigation reflects reality:
  - [frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx) — `ADVANCED_LIVE` Set grew from `{11}` to `{11, 12}`. Day 12 chip in the advanced day-chip strip is now a clickable `<Link>` instead of a muted "Planned" span.
  - [frontend/app/advanced-ai/page.tsx](frontend/app/advanced-ai/page.tsx) — Day 12's row in `ADVANCED_DAYS` flipped `status: 'planned'` → `status: 'live'` and the blurb updated to mention the full lineup (PyTorch, TensorFlow / Keras, HuggingFace Hub + Transformers, ONNX) instead of just three of the five.
- **JSX-attribute escape papercut, again** — hit it briefly during testing on a `body=` prop with inner double quotes. Standardised on single quotes inside the body strings to avoid `\"` parse errors, which has been the recurring fix since Day 10. Day 12 lints clean.
- **Verified.** `python -c "import main"` succeeds with `/day12/ask` + `/day12/framework-map` mounted. Frontend `tsc --noEmit` exits 0. `eslint app/advanced-ai/day-12/page.tsx app/advanced-ai/page.tsx` exits 0 (the 2 errors that still surface against `Navbar.tsx` are the pre-existing `react-hooks/set-state-in-effect` warnings on lines 99/109 unrelated to this work). Progress catalog already covered N56–N60 from the earlier N51–N100 catalog expansion, so `mark complete` works without any backend catalog changes — when all 5 N56–N60 nodes get ticked, the advanced-ai index card flips from `✓ LIVE · 5/5` → `✓ COMPLETED` via the same `tierOf`/`dayProgress` logic already in place.

### Usage page — token spend split between Basic (Day 1-10) and Advanced (Day 11-20)

- The `/usage` page rolled all token spend up by provider, with no way to see how much of the bill came from beginner content vs the advanced curriculum. Split the headline now that Day 11 is live.
- **Backend.** `/usage/daily` and `/usage/monthly` in [backend/token_tracker.py](backend/token_tracker.py) now include the `day` column in the `SELECT` + `GROUP BY` (the column was already in the `token_usage` table — `track(day="dayXX", ...)` writes it on every call — just not exposed in the rollup). Response rows gained a `"day": "dayXX"` field; existing fields are unchanged so the rest of the UI keeps working. `ORDER BY` extended with `day ASC` between bucket and provider for stable sort.
- **Frontend.** [frontend/app/usage/page.tsx](frontend/app/usage/page.tsx) classifies each row via a new `tierOf(dayStr)` helper: `day01..day10` → `'basic'`, `day11..day20` → `'advanced'`. Unrecognised tags (legacy / non-day-tagged rows) fall into `'basic'` so old data doesn't quietly vanish. New `TIER_COLOR` (green for basic, accent orange for advanced) and `TIER_LABEL` constants drive the UI palette.
- **New `tierTotals` and `providerTotalsByTier` memos** compute per-tier `{tokens, cost, calls}` and per-tier-per-provider token sums in one pass over the filtered daily rows. The old `providerTotals` memo (used by the dropped global per-provider mini-summary) was removed since its replacement is per-tier.
- **UI rebuild.** Above the existing daily/monthly bar charts, the page now renders:
  1. The existing combined-totals trio (tokens / cost / calls) — kept as the at-a-glance number for the whole window.
  2. A **new 2-up tier grid** below it. Each card has a colored top border (green = basic, orange = advanced), the tier label, a "X% of window" mini-stat in the corner, three inline mini-stats (tokens / cost / calls) using a compact new `TierStat` helper, and a per-provider breakdown showing both a stacked bar (segments sized by share of *this tier's* tokens) and a 2-column list of `provider · count · pct%`. When a tier has zero usage in the window, the card shows a "No basic usage in this window." placeholder instead of an empty bar.
- **Empty + edge cases.** Tier cards always render (both basic and advanced) so the layout doesn't jump when one tier is empty. Provider list inside a tier filters out 0-token providers so the column doesn't show 5 zeros. Percent shares use the tier's own total as denominator so the bar adds to 100% per card, not 100% across both cards.
- **Side housekeeping.** Took 4 IDE Tailwind-canonical-class warnings on the way past: `h-[6px]` → `h-1.5` (2 occurrences, in the new tier provider bar + the pre-existing `ProviderBar`) and `gap-[2px]` → `gap-0.5` (2 occurrences in the pre-existing `StackedBarChart`).
- **Verified.** Backend imports clean. `tsc --noEmit` exits 0, `eslint app/usage/page.tsx` exits 0 with no warnings. The earlier `'providerTotals' is unused` lint flag cleared after I removed the orphan memo.

### practice/ removed entirely — sandbox concept retired

- User reversed course on the whole practice sandbox: "i dont need practice anymore. remove all practice folder and realted to that." Cleared everything practice-related from the repo and from the docs, so the project surface is just **VAL app (port 3000)** + **its FastAPI on :8000** + **Day 11–20 visual pages under `/advanced-ai`**. No optional sandbox, no second frontend, no second port.
- **Deleted on disk.**
  - `practice/` folder (including `practice/frontend/` Next.js scaffold, its `node_modules/`, and the previously-already-deleted `practice/day-11/backend/`). Had to kill a leftover dev server on `:3001` first (held a file handle that blocked `rm -rf` — `Stop-Process` on the listener cleared it).
  - `PRACTICE.md` at the project root.
- **Reverted in code.**
  - [backend/main.py](backend/main.py) — CORS `allow_origins` back to `["http://localhost:3000"]` only. The `:3001` entry isn't needed anywhere now.
  - [start-all.ps1](start-all.ps1) — fully reverted to the pre-practice shape: starts Docker + pgvector + backend + frontend, period. No conditional `practice/frontend/` launch, no `$practiceFrontend` variable, no per-spawn window count branching.
  - [stop-all.ps1](stop-all.ps1) — dropped the `Stop-Port 3001` block (no practice frontend to kill). Stops 8000, 3000, and the `ai-roadmap-pg` container.
- **Reverted in UI.**
  - [frontend/app/advanced-ai/day-11/page.tsx](frontend/app/advanced-ai/day-11/page.tsx) — removed the **Practice tab** that had been added a few entries up. Trimmed the `NODES` array back to N51–N55 only, dropped the `PRACTICE_TAB` constant, deleted the dashed-border tab button + its conditional render in the JSX, removed the `activeId !== PRACTICE_TAB.id` guard around the mark-complete button so it's always visible again. Then truncated the bottom of the file at line 966 (last line of `ScoreCell`) to drop the ~300 lines of `NodePractice` / `PracticeStep` / `CodeBlock` / `CopyIcon` helpers that no longer have a caller. Final file is back to 5 tabs + Compare panel + radar/matrix viz, exactly the way it was before the Practice tab landed.
  - [frontend/app/advanced-ai/page.tsx](frontend/app/advanced-ai/page.tsx) — header subtitle no longer mentions `practice/` or `PRACTICE.md`. Just describes the curriculum: "Ten more days, fifty more nodes — vector engines, frameworks, local inference, evals, fine-tuning."
- **Reverted in docs.**
  - [AI_ENGINEER_ROADMAP_ADVANCED.md](AI_ENGINEER_ROADMAP_ADVANCED.md) — removed the top-of-file "🧪 **About `practice/`**" callout. Rewrote the **Project structure additions** tree to drop the entire `practice/` subtree; the frontend branch is now the last entry (uses `└──` instead of `├──`, and inner indentation switched from `│   ` to `    ` accordingly). **Deleted the entire `Step 0 Addition — Practice folder + venv` section**, lines 35–437 in the prior layout — that's the venv intro, the Claude-Code setup prompt block, the `Setup (one-time at project root)` walkthrough, the `Why venv?` section, the `Practice folder rules — READ CAREFULLY` block, and the system-requirements tables for the optional engines. The file now goes straight from the project-structure section to the Day 11 spec. Verified by grep: zero remaining `PRACTICE.md` / `practice/` / `/practice` references in the file.
- **Verified.** Main frontend `tsc --noEmit` exits 0. ESLint on `app/advanced-ai/page.tsx` + `app/advanced-ai/day-11/page.tsx` exits 0. Backend imports clean with CORS reverted (`['http://localhost:3000']`) and `/day11/ask` + `/day11/compare` still mounted. Both PowerShell scripts AST-parse clean. The non-`practice/` matches that survived a `grep -rni "practice"` are all the English word ("in practice", "responsible AI practices") — none reference the deleted folder or markdown.

### Practice page now reuses the main backend on :8000 (no separate practice backend)

- User observation: "the backend just use the api from 8000. its practice section from the same topics api after all." Day 11 in the practice frontend was hitting a private FastAPI on `:8001` that did its own ChromaDB ingest/search — but the main backend already exposes `/day11/*` covering the same topic. Removed the duplicate.
- **Deleted `practice/day-11/backend/`** entirely (`main.py` + `requirements.txt`). No more `:8001` listener; no per-day Python deps for the practice flow.
- **Rewrote [practice/frontend/app/day-11/page.tsx](practice/frontend/app/day-11/page.tsx)** as an "ask the tutor" sandbox that POSTs to `http://localhost:8000/day11/ask` — the same endpoint the polished `/advanced-ai/day-11` page uses. The new UI: an inline provider switcher pulled from `:8000/providers`, a textarea for the question, an answer card with `provider · model · tokens · latency` footer, and an "online / offline" badge tied to whether `/providers` resolves. Still all inline styles, no Tailwind. The old ChromaDB-specific UI (ingest/search/distance cards) is gone — that lives in the optional `quick.py` standalone Python snippet now.
- **[backend/main.py](backend/main.py) — CORS allow `:3001` again.** The `allow_origins` list now contains both `http://localhost:3000` and `http://localhost:3001`. Reverted the partial-revert from earlier today; this time it's load-bearing for the practice page rather than an opportunistic add.
- **[start-all.ps1](start-all.ps1).** Header comment rewritten to say the practice frontend talks to the main backend on `:8000` (no separate practice backend). Final summary line for the practice URL now reads "(talks to the main backend on :8000)" instead of the old "(per-day backend runs manually on :8001)". The conditional `practice/frontend/` launch logic itself stays — that part still works.
- **[stop-all.ps1](stop-all.ps1).** Dropped the `Stop-Port 8001` block entirely. Kept `Stop-Port 3001` (practice frontend still runs there). Header comment updated to match.
- **[PRACTICE.md](PRACTICE.md) — simplified.** "How to start practicing any day" section was a two-terminal flow (backend on 8001 + frontend on 3001); now it's a **one-terminal** flow (just the frontend on 3001), with a callout explaining that no pip install is needed because the main backend already serves the endpoints. Port table dropped row "8001 — Practice backend" since that doesn't exist anymore. The Day 11 section's per-day file table collapsed from 3 rows to 1 (just `practice/frontend/app/day-11/page.tsx`); errors table swapped chromadb/faiss-specific rows for the more relevant "backend offline" / "providers offline" / "CORS error in browser console" / "Port 3001 already in use" set. The "Skip the UI — standalone Python" snippet remains as the path for users who *do* want a real ChromaDB install on their machine.
- **Verified.** Backend import succeeds with CORS = `['http://localhost:3000', 'http://localhost:3001']` and `/day11/ask` + `/day11/compare` still mounted. Practice frontend `tsc --noEmit` exits 0. Both PowerShell scripts AST-parse clean and contain no unicode (still CP1252-safe for dot-source). No code changes in the main `frontend/`; only the practice frontend, the two PS scripts, and the CORS line of `backend/main.py` moved.

### practice/ scaffolded on disk — user just runs install + dev server

- Reversed the earlier "user creates practice/ manually" stance at the user's request. Files are now committed to the repo so the workflow becomes: clone → `npm install` → activate venv → `pip install` → run two terminals. Previous changelog entries from earlier today (PRACTICE.md copy-paste blocks, the "Coming Soon" placeholder) are superseded by this entry — the code now lives on disk, not just embedded in markdown.
- **Created `practice/frontend/`** as a real Next.js 16.2.6 app (matches main frontend versions so npm doesn't have to re-resolve), inline-styled, no Tailwind. Seven files:
  - `package.json`, `tsconfig.json`, `next.config.ts` — standard Next setup.
  - `app/globals.css` — dark theme matching VAL.
  - `app/layout.tsx` — sticky navbar with VAL · Practice wordmark, Day 11–20 chip strip (only Day 11 is `live` and clickable; Day 12–20 render as muted non-clickable spans with "Planned" tooltip), and a dashed "← VAL (Day 1-10)" link back to `http://localhost:3000`.
  - `app/page.tsx` — Server Component `redirect('/day-11')`.
  - `app/day-11/page.tsx` — client component: health-check on mount lights a green `N docs indexed` or red `backend offline` badge, search form posts to `/search` and renders top-3 result cards with cosine distance, "add your own doc" input posts to `/ingest` and refreshes the count.
  - Ran `npm install` (27 packages, ~30s on warm cache). Smoke-tested: dev server boots in 2.2s, `GET /` → 307 → `/day-11`, `/day-11` returns 200 in 337ms.
- **Created `practice/day-11/backend/`** — slim FastAPI sandbox:
  - `requirements.txt`: just `fastapi==0.115.5`, `uvicorn==0.32.1`, `chromadb==0.5.0` (other engine clients are an optional extension, not a baseline dep).
  - `main.py`: ~75 lines, CORS allowing `http://localhost:3001`, in-memory `chromadb.Client()` that self-seeds 6 short docs about the engines on first boot so the user's first search returns something useful. Three endpoints: `GET /health` (status + doc_count), `POST /ingest {documents: [...]}` (appends with auto-incremented IDs), `POST /search {query, top_k=3}` returning `{id, text, distance}` rows.
- **Briefly attempted npm workspaces at the project root** so `frontend/` and `practice/frontend/` could share one root `node_modules/`. User course-corrected: install lives inside `practice/frontend/` itself; the day-XX pages share that one install via standard Next.js module resolution, no workspaces needed. Deleted the root `package.json` I'd created and proceeded with a per-frontend install. (`frontend/node_modules/` and `practice/frontend/node_modules/` are independent; that's fine — they're both gitignored and `npm install` is a one-time cost per app.)
- **[start-all.ps1](start-all.ps1) — re-added the practice frontend auto-launch.** Now spawns a third PowerShell window running `npm run dev -- --port 3001` when `practice/frontend/` exists. The practice **backend** stays manual because each day has its own `main.py` and the user picks which one to run — comment in the script header documents this. Final summary block lists the practice URL when started and notes "per-day backend runs manually on :8001". Stays on ASCII only (no em-dashes — prior CP1252 trap).
- **[stop-all.ps1](stop-all.ps1) — added `Stop-Port 3001` + `Stop-Port 8001`** between the existing 8000 / 3000 stops. `Stop-Port` no-ops when nothing's listening, so the new calls are safe whether or not the user is actually running practice. Header comment updated to list all four ports.
- **[PRACTICE.md](PRACTICE.md) — rewritten as "the files are already here, just install + run".** The earlier 250+ lines of copy-paste code blocks are gone — replaced with two tables showing what's already in place, plus the two-terminal run block. Frontend section: 7-row file table + one-liner `npm install`. Day 11 section: 3-row file table + the run commands. The "Skip the UI" standalone Python snippet (`quick.py`) stays since it's truly a different use case. Errors table reordered so the most common ones (chromadb slow install, backend-offline badge, port collision) come first, with the engine-specific ones marked "(when extending)".
- **Verified.** `tsc --noEmit` in `practice/frontend/` exits 0. `start-all.ps1` and `stop-all.ps1` both AST-parse clean and contain no unicode (no em-dashes or curly quotes — CP1252-safe for dot-source invocation). No code changes in the main `frontend/` or `backend/` — only `practice/` content and the two PowerShell scripts moved.

### PRACTICE.md — full copy-paste code for Day 11 sandbox (frontend + backend)

- User noticed PRACTICE.md only told them which packages to `pip install` / `npm install` but never gave them the actual code to put into the files. They had to invent the FastAPI app and the day-11 page from scratch. Fixed by **embedding the full file contents directly into PRACTICE.md** as copy-paste-ready blocks — user just creates each file, pastes, and runs.
- **New section "Practice frontend — copy these files once"** inserted before the day-by-day guide. Six shared files written once, reused for every Day 11–20:
  - `practice/frontend/package.json` — Next.js 16.2.6 + React 19.2.4 + TS 5, pinned to the exact versions the main `frontend/` uses so npm doesn't have to re-resolve.
  - `practice/frontend/tsconfig.json` — same shape as the main frontend; note callout warns the user that Next.js will auto-flip `"jsx": "preserve"` → `"jsx": "react-jsx"` on first boot (don't fight it).
  - `practice/frontend/next.config.ts` — one-liner with `reactStrictMode: true`.
  - `practice/frontend/app/globals.css` — minimal dark theme (`#0c0a09` bg, `#f5f5f4` fg) matching VAL.
  - `practice/frontend/app/layout.tsx` — root layout with a sticky header showing `VAL · Practice` + a `← VAL (Day 1-10)` link back to `http://localhost:3000`. No Tailwind, all inline styles, kept tiny so it drops in cleanly.
  - `practice/frontend/app/page.tsx` — Server Component `redirect('/day-11')` so the root URL lands on the only day that exists today.
- **Day 11 section rewritten** to add a real working backend + frontend instead of the previous "here's a pip install command, now figure out main.py yourself" gap.
  - **`practice/day-11/backend/requirements.txt`** — slim (`fastapi==0.115.5`, `uvicorn==0.32.1`, `chromadb==0.5.0`). FAISS / Qdrant / Weaviate / Pinecone clients are mentioned as "add when you want to extend" so the first install stays fast.
  - **`practice/day-11/backend/main.py`** — full FastAPI app: CORS open to `http://localhost:3001`, an in-memory `chromadb.Client()` collection that **seeds itself with 6 short docs** about the engines on first boot so the user's first `/search` returns something useful before they add anything. Three endpoints: `GET /health` (status + doc_count), `POST /ingest {documents: [...]}`, `POST /search {query, top_k=3}` returning `{id, text, distance}` rows. Total ~75 lines, no dependencies on the main VAL backend.
  - **`practice/frontend/app/day-11/page.tsx`** — a self-contained client component that hits the backend at `localhost:8001`. Health check on mount lights a green `N docs indexed` badge (or red `backend offline` if uvicorn isn't running). Search form posts the query, renders the top-3 results as cards with cosine distance. Below the results, an "add your own doc" input + button calls `/ingest` and refreshes the health count, so the user can extend the corpus interactively. All inline styles, no Tailwind required since the practice frontend is the minimal scaffold above.
  - **Two-terminal run block** under "Run it" — Terminal 1 activates the venv (Windows + Mac/Linux variants), installs deps, `cd practice/day-11/backend`, `uvicorn main:app --reload --port 8001`. Terminal 2 `cd practice/frontend; npm run dev -- --port 3001`. Closing line tells the user to open `http://localhost:3001/day-11` and mentions both sides hot-reload independently.
  - **Errors table expanded** from 3 rows to 6: added `chromadb install slow` (sentence-transformers + onnxruntime are heavy), `backend offline badge in UI` (uvicorn not up or CORS blocked), `Port 3001 already in use` (try `--port 3002`).
  - **Existing "Try it in 10 lines" Python snippet kept** at the bottom of the section, renamed `quick.py` and reframed as "Skip the UI — same idea in 10 lines of standalone Python" so it doesn't conflict with the real `main.py` above it.
- **Scope.** Only Day 11 got the full treatment in this pass — that's the only built advanced page right now. Days 12–20 sections still have just the pip install + "try it" snippet they had before; they'll get the same treatment as each day's frontend page lands in `frontend/app/advanced-ai/day-XX/`.
- **No code touched.** PRACTICE.md is just documentation — none of the embedded snippets get auto-saved to disk; the user copies them into their own `practice/` folder per the "user-managed" rule. Backend imports, `tsc --noEmit`, `eslint` are all unchanged from the previous entry.

### Progress catalog — N51–N100 added (Day 11 mark-complete now works) + navbar context-switch

- **Bug.** User reported marking N51–N55 on the Day 11 page did nothing. Root cause: [backend/routers/progress.py](backend/routers/progress.py) `NODE_CATALOG` stopped at N50 / day 10, so `POST /progress/N51` hit the 404 branch in `toggle_node()` (`raise HTTPException(status_code=404, detail=f"Unknown node: {node_id}")`). My earlier changelog claim that "progress catalog already covers N51–N55" was wrong — the catalog had only ever been populated for Day 1–10.
- **Fix.** Appended **all 50 advanced nodes (N51–N100, Day 11–20)** to `NODE_CATALOG` in one go, with labels taken verbatim from `AI_ENGINEER_ROADMAP_ADVANCED.md` (`grep -E "^- \[ \] \*\*N[0-9]+\*\*"`). Catalog now has the full 100-node spec instead of 50. Verified: `len(NODE_CATALOG) == 100`, `'N51' in NODE_IDS`, `'N100' in NODE_IDS`, all five day-11 entries present and tagged `day=11`. Once you reload uvicorn (`--reload` should pick it up automatically since `progress.py` is on disk), clicking "mark N51 complete" hits `POST /progress/N51` → 200, `useNodeProgress(11)` refetches `/progress`, and the chip's green ✓ lights up. Same for N52–N100 when those pages exist.
- **Side benefit.** The `/progress` response now also emits a `by_day` row for days 11–20, so the day-progress chip in the Day 11 page header (`X / 5 nodes`) starts showing real counts instead of being suppressed by the `progress.dayProgress &&` guard returning falsy. The advanced-ai index page in the navbar swap (below) gets a richer signal too — once `useNodeProgress` is wired into that page, completed advanced days will show ✓ chips just like Day 1–10.

### Navbar — day chips swap to 11–20 on /advanced-ai pages

- Same nav UX issue the user flagged: on `/advanced-ai` and `/advanced-ai/day-11`, the chip strip was still 1–10 (the beginner set), which felt wrong since the Advanced button itself was highlighted and the user clearly wasn't in the beginner section.
- **[frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx).** New module-level constants `BEGINNER_DAYS = [1..10]`, `ADVANCED_DAYS = [11..20]`, `ADVANCED_LIVE = new Set([11])`, and a small `dayHref(day, advanced)` helper that maps to `/day-XX` (zero-padded) for beginner or `/advanced-ai/day-X` (no zero-pad, matches the actual route layout) for advanced. Inside the component, derived `inAdvanced = pathname.startsWith('/advanced-ai')` flips both the desktop and mobile chip lists.
  - **Desktop chip strip** — iterates `inAdvanced ? ADVANCED_DAYS : BEGINNER_DAYS`. Days that are live render as the existing `<Link>` with the active/done/idle palette intact. Days that are not in `ADVANCED_LIVE` (currently 12–20) render as a non-clickable `<span>` with `text-foreground/30 cursor-not-allowed`, `aria-disabled="true"`, and a "Planned" `title` tooltip — visible so the roadmap is legible at a glance, but never blocks the user with a 404 from clicking a route that doesn't exist.
  - **Mobile hamburger** — same swap inside the `Days` section. The section header now reads `"Advanced days"` when `inAdvanced` is true, otherwise `"Days"`. Disabled-chip styling follows the bordered mobile-chip pattern (border-rule + muted text) instead of the desktop's borderless style.
- **No regression to existing behavior** — beginner chips still zero-pad, still use `/day-XX`, still respect `doneDays`. Active-state still uses `pathname === path` for the chip itself (which means hovering over Day 11's chip on `/advanced-ai/day-11` lights the chip filled, same as Day 5's chip lit when on `/day-05`).
- **Verified.** `tsc --noEmit` exits 0. ESLint on `app/components/Navbar.tsx` reports the same two pre-existing `react-hooks/set-state-in-effect` errors (lines 99 and 109 — line numbers shifted from 89/99 because my edit added ~10 lines of derived state above; the errors themselves sit in unchanged `useEffect` bodies for `refetch()` and `refetchProgress()`). Diff: 63 insertions, 15 deletions, single file.

### Docs sync — AI_ENGINEER_ROADMAP_ADVANCED.md + PRACTICE.md aligned with new architecture

Both reference docs were still describing the old layout where Day 11–20 curriculum pages lived at `frontend/app/day-XX/page.tsx` and the practice frontend was the primary advanced surface. Brought them in line with the as-built decision: visual curriculum at `/advanced-ai/day-XX` in the main frontend, `practice/` purely as the optional user-managed Python sandbox.

- **[AI_ENGINEER_ROADMAP_ADVANCED.md](AI_ENGINEER_ROADMAP_ADVANCED.md) — structural rewrites.**
  - Two new pinned notes at the top: "**Where Day 11–20 lives**" (main frontend port 3000, behind the Advanced button at `/advanced-ai`, not in the day-chip nav) and "**About `practice/`**" (optional, user-managed via PRACTICE.md, advanced roadmap does NOT autocreate it).
  - Rewrote the **Project structure additions** block. Frontend tree now shows `frontend/app/advanced-ai/page.tsx` (the Day 11–20 index) plus `frontend/app/advanced-ai/day-11/page.tsx … day-20/page.tsx` (one navigation level deeper than before). `practice/` is now flagged "OPTIONAL hands-on sandbox (user-managed)" with the per-day backend folders collapsed onto one line each to make the optional nature visually obvious.
  - Step 0 heading retitled `Step 0 Addition — Practice folder + venv (OPTIONAL · skip unless you want the sandbox)`. Intro callout reworded: explicit "you do not need it to follow Day 11–20", PRACTICE.md called out as the authoritative manual path, Setup prompt below it framed as a Claude-Code-friendly *template* rather than a required step.
  - Killed a stale claim — the second-line tagline said "Bilingual (EN + ID)" and the description called out "bilingual JSON" in the rules. Removed "bilingual JSON" from the global-rules note since the codebase moved to single-language per the 2026-05-11 policy; left the tagline alone to keep the line cosmetically symmetric with the original roadmap.
  - Bulk-rewrote all **10 per-day frontend prompt headers** from `### Frontend prompt — frontend/app/day-XX/page.tsx` → `### Frontend prompt — frontend/app/advanced-ai/day-XX/page.tsx`, and the matching `Replace frontend/app/day-XX/page.tsx.` instruction line inside each prompt body. 20 string replacements total, all confirmed via `grep` (the only remaining `app/day-1X` matches are inside the `practice/frontend/app/day-XX/` Step 0 block, which is correct — that's where the user's *practice* pages live when they scaffold the sandbox).
- **[PRACTICE.md](PRACTICE.md) — clarifying split between the two surfaces.**
  - New "**What this guide is (and isn't)**" section right after the opener. Two-row table contrasts the **Visual curriculum** (main app at `:3000`, includes Day 11–20 behind the Advanced button, booted by `start-all.ps1`) with the **Practice sandbox** (this guide, `:3001` + `:8001`, user scaffolds manually, VAL repo does NOT auto-create it). Closing paragraph: most learners only need surface #1, this file is purely for the optional hands-on side; `start-all.ps1` does not start anything in `practice/`.
  - Rest of PRACTICE.md left alone — the port references (3000/8000 = VAL, 3001/8001 = practice) and the day-by-day install steps were already consistent with the as-built layout. Grepped `frontend/app/day` / `practice frontend` / `3000` / `8000` / `3001` / `8001` to be sure; no stale lines remained.
- **No code touched.** This is a docs-only sync. `tsc --noEmit`, `eslint`, and backend imports are unchanged from the previous entry.

### Day 11 relocated to `/advanced-ai/day-11`; practice/ reverted to user-managed

- User pushback on the previous Day 11 entry — two architectural decisions were wrong: (1) **practice/ should be created manually by the user**, not scaffolded by me; PRACTICE.md exists specifically to guide that process, and (2) **Day 11 isn't a "Day 1–10 sibling"** — it's already advanced content, so it doesn't belong in the main day-chip navbar.
- **Reverted: practice/ wholesale.** `rm -rf practice/` — including the minimal Coming Soon placeholder I'd added earlier in the session, the `practice/frontend/` Next.js scaffold, the per-day-11 page, the `app/components/ModelSwitcher.tsx` + `Markdown.tsx` copies, `node_modules/`, and `package-lock.json`. The folder is back to not existing on disk; the user will scaffold it on their own schedule via PRACTICE.md.
- **Moved: Day 11 page** from `frontend/app/day-11/page.tsx` (which never landed in main; I had it briefly in `practice/frontend/app/day-11/`) to its final home at [frontend/app/advanced-ai/day-11/page.tsx](frontend/app/advanced-ai/day-11/page.tsx). Same content (5 node tabs N51–N55, FactCard grids per engine, shared `AskBlock` calling `/day11/ask`, Compare panel on N55 with SVG radar chart + feature matrix), but ported to Tailwind classes and reusing the main frontend's shared components (`ModelSwitcher`, `Markdown`, `useNodeProgress`) instead of inline-styled copies. Relative imports adjusted to `'../../components/...'` for the extra path depth.
- **Promoted: `/advanced-ai` page** from a single "Coming soon" stub to a real **Day 11–20 index** in [frontend/app/advanced-ai/page.tsx](frontend/app/advanced-ai/page.tsx) — a 2-column grid of 10 cards, one per day. Day 11 is the only `status: 'live'` card (its card is a `<Link>` to `/advanced-ai/day-11`, accent border); Day 12–20 are `'planned'` (muted opacity, no link, "planned" badge in the corner). Header text points the user at PRACTICE.md for the optional hands-on sandbox.
- **Reverted: Navbar Advanced button** from a plain `<a href="http://localhost:3001">` back to a Next.js `<Link href="/advanced-ai">` on both the desktop and mobile-menu variants in [frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx). Active-state classnames restored, using `pathname.startsWith('/advanced-ai')` (not `=== '/advanced-ai'`) so the button stays highlighted while the user is on `/advanced-ai/day-11` or deeper. Navbar `DAYS` array stayed at 10 — Day 11+ is reached only via the Advanced button now, not the day-chip nav.
- **Reverted: backend CORS** in [backend/main.py](backend/main.py) — `allow_origins` back to `["http://localhost:3000"]` only. The Day 11 page is served from the main frontend on port 3000 now, so the previous `:3001` entry isn't needed (and shouldn't grant blanket access to a practice app that the user might choose never to run).
- **Reverted: `start-all.ps1` / `stop-all.ps1`** to their pre-conversation shapes — dropped the conditional `practice/frontend/` launch, the `Stop-Port 3001` + `Stop-Port 8001` lines, and the related comments. Per PRACTICE.md, the user manages practice servers in their own terminals (port 3001 frontend, port 8001 per-day backend). Both files re-verified clean by `[Parser]::ParseFile()` AST check.
- **Kept: `backend/routers/day11.py`** and its `main.py` mount + `DAY_TOPICS[11]` entry. The `/day11/ask` and `/day11/compare` endpoints stay live; the relocated frontend page calls them at `http://localhost:8000/day11/...` from the same origin (port 3000), so no CORS exception is needed.
- **Verified end-to-end.** Backend import succeeds with `/day11/ask` + `/day11/compare` still mounted and CORS = `['http://localhost:3000']` only. Frontend `tsc --noEmit` exits 0; `eslint app/advanced-ai/page.tsx app/advanced-ai/day-11/page.tsx` clean (the 2 pre-existing `react-hooks/set-state-in-effect` errors in `Navbar.tsx` lines 89/99 are unchanged by this session). Both PowerShell scripts AST-parse clean.

### Day 11 — Vector DB Landscape (N51–N55)

- First day from the **advanced roadmap** (`AI_ENGINEER_ROADMAP_ADVANCED.md`). The curriculum page initially lived in the **practice frontend** on port 3001 (where the navbar's Advanced button briefly pointed), not in the main VAL frontend on 3000. The AI endpoints still live in the main backend on port 8000 — the practice frontend just called across origins to `localhost:8000`, which I authorized below in CORS. (**Both decisions were later reverted — see the entry above this one.**)
- **Backend `routers/day11.py` — two endpoints**, single-language per the 2026-05-11 policy. Shared `_usage()` / `_fail()` helpers in the same shape as Day 4–10. Both endpoints route through the existing `provider.py` runtime switcher, so the user can pick OpenAI / Groq / DeepSeek / Gemini per request just like every other day.
  - `POST /ask` — JSON `{question, provider?}`. System prompt is `BEGINNER_SYSTEM_PROMPT` extended with "Today's topic is vector database engines: FAISS, ChromaDB, Qdrant, Weaviate, Pinecone. Focus on when to choose each, their trade-offs, and how they differ." Plain text response (`response_format` left default so it streams natural prose, not JSON). Returns `{question, answer, prompt_tokens, completion_tokens, total_tokens, latency_ms, provider, model}`.
  - `POST /compare` — JSON `{provider?}`. Asks the model to score all five engines 1–5 across `managed / filter_support / scale / cost_efficiency / setup_ease`, plus `type` (`embedded | self-hosted | managed`) and a short `best_for` phrase. `response_format={"type": "json_object"}` so we get strict JSON back. `_normalize_engine()` defensively clamps scores to 1–5, validates `type` against the enum, and truncates `best_for` to 120 chars. Returned engines are re-indexed by name and emitted in the **canonical order** (FAISS → ChromaDB → Qdrant → Weaviate → Pinecone) regardless of the order the model returned them — with a zero-score placeholder for any engine the model dropped. Returns `{engines, axes, prompt_tokens, completion_tokens, total_tokens, latency_ms, provider, model}`.
- **`backend/main.py` plumbing.** Added `day11` to the router imports and to `_day_routers`, added `11: "Vector DB Landscape"` to `DAY_TOPICS` so the OpenAPI tag is generated. **CORS now allows both `http://localhost:3000` and `http://localhost:3001`** so the practice frontend can hit the main backend without a preflight rejection — every existing endpoint (`/providers`, `/tokens`, `/day01..day10/*`, `/progress`) becomes reachable from 3001 as a side effect.
- **Practice frontend shared components** under [practice/frontend/app/components/](practice/frontend/app/components/). The practice app is Tailwind-less by design (per the "minimal placeholder" choice), so these are inline-styled versions of the main frontend's helpers.
  - `ModelSwitcher.tsx` — same contract as the main app's (props `{selected, onChange, disabled}`), fetches `http://localhost:8000/providers` on mount, renders a dark-styled `<select>` with `provider · model` options. Mount-gate via `mounted` boolean so SSR doesn't reach for `fetch`.
  - `Markdown.tsx` — tiny stripped-down version of the main app's parser: paragraphs, bullets (`-` / `*`), `**bold**`, and `` `code` ``. No tables, no headings, no streaming caret — enough to render the `/day11/ask` model output cleanly.
- **Practice frontend layout + entry.**
  - `app/layout.tsx` — added a sticky dark navbar (matches VAL's `bg-gray-900` look via inline styles) with: an accent dot + "VAL · Practice" wordmark linking to `/day-11`, a 10-chip Day 11–20 nav (only Day 11 is real for now; the rest will be valid links once those pages exist), and a dashed "← VAL (Day 1–10)" link back to `http://localhost:3000` on the right.
  - `app/page.tsx` — Server Component `redirect('/day-11')` so hitting `http://localhost:3001/` lands on Day 11 directly. Replaces the previous Coming Soon placeholder; the placeholder copy was its own commit and is no longer reachable.
- **Practice frontend `app/day-11/page.tsx` — five node tabs N51–N55**, same shell shape as Day 5–10 (header + tab nav, per-tab body, all five panels rendered simultaneously with the inactive ones hidden via `display: none` so per-tab state survives a tab switch). Each tab has the same anatomy: `SectionHeader` (engine name + one-sentence vibe), a `FactCard` grid (4 cards per engine, color-coded by engine), a shared `AskBlock` (model switcher + 3 engine-specific presets + textarea + result card via the shared `Markdown`), and a closing `ExplainerBlock`. Per-engine color palette: FAISS blue, ChromaDB green, Qdrant violet, Weaviate amber, Pinecone coral.
  - **N51 FAISS** — facts: Born 2017 / Embedded / Three index types (Flat, IVF, HNSW) / No filters. Explainer: "the 'just a library' superpower".
  - **N52 ChromaDB** — facts: One-line setup / Metadata filters / Built-in embedder / Scale ceiling. Explainer: "fastest path from notebook to RAG".
  - **N53 Qdrant** — facts: Rich payload filters / Single binary / HNSW + PQ / Managed option. Explainer: "the pragmatic middle ground".
  - **N54 Weaviate** — facts: Hybrid by default (BM25 + vector) / Modular vectorizers / GraphQL API / Generative modules. Explainer: "when keywords still matter".
  - **N55 Pinecone + Compare** — facts: Zero ops / Serverless tier / Real-time updates / Compliance built-in. Plus the live **Compare panel**: button hits `/day11/compare`, renders a hand-drawn SVG **radar chart** (5 axes, 5 polygons one per engine in the engine palette colors, 5 grid rings, axis labels positioned around the perimeter with anchor flipped left/right based on side) next to a **feature matrix table** (engine | type | 5 dot-bars per axis | best_for). The dot-bars are 5 6-px dots, filled in the engine color up to the score and hollow in `rule` gray after. Explainer: "paying to skip the operations team".
- **JSX-attribute gotcha repeated, fixed.** Hit the same Day 10 papercut: JSX `attr="..."` doesn't honor backslash escapes, so `body="… [\"rag\"] …"` triggered a parser error at `\"`. Fixed by switching inner double quotes to single quotes inside that one body string. Cheaper than the `body={"..."}` JS-expression rewrite I used on Day 10, and visually closer to the surrounding cards.
- **Verified end to end.** `python -c "from routers import day11"` succeeds, `main.app.routes` mounts `/day11/ask` + `/day11/compare`. Practice frontend `tsc --noEmit` exits 0. Smoke-tested by booting `next dev --port 3001` in the background — server ready in 2.2s, `GET /` 307-redirects to `/day-11`, `GET /day-11` returns 200 in 1.7s. Tore the server down with `Stop-Process` on whoever held port 3001 after the test.
- **Progress catalog already covers N51–N55** — `backend/routers/progress.py` was populated with the full 50-node catalog when Day 1–10 was built, so the new endpoints integrate with the existing `/progress` + `/progress/{node_id}` machinery without any catalog edit. The Day 11 page doesn't wire up the mark-complete button yet (the practice frontend doesn't ship the `useNodeProgress` hook) — that's deferred until we decide whether to share progress state across the two frontends.

### `practice/frontend/` — minimal Coming Soon placeholder

- The previous changelog entry wired the Advanced button to `http://localhost:3001` and taught `start-all.ps1` to launch a Next.js dev server in that folder. But the folder didn't exist on disk — the advanced roadmap's Step 0 scaffold prompt hasn't been run yet — so the script silently skipped step 5 and the button hit a connection-refused page. User asked for the minimum that makes the button actually go somewhere; full Step 0 scaffold (navbar, 10 day-XX routes, health checks against :8001) is deferred until they run the prompt.
- **New files** under [practice/frontend/](practice/frontend/):
  - `package.json` — Next.js 16.2.6 + React 19.2.4 + TypeScript 5, pinned to the **exact** versions the main `frontend/` uses so npm doesn't have to re-resolve and a future Step 0 scaffold can layer on without version churn. No Tailwind, no ESLint — those come with the real scaffold.
  - `tsconfig.json` — same shape as the main frontend. On first `next dev`, Next auto-amended `"jsx": "preserve"` → `"jsx": "react-jsx"` and appended `.next/dev/types/**/*.ts` to `include` (its normal first-boot behavior); kept as-is.
  - `next.config.ts` — one-liner: `{ reactStrictMode: true }`.
  - `app/layout.tsx` — minimal root layout, sets `<html lang="en">`, loads `globals.css`, declares page metadata.
  - `app/globals.css` — matches the main VAL theme's dark background (`#0c0a09`) and foreground (`#f5f5f4`); no Tailwind, just resets and a system-font stack.
  - `app/page.tsx` — single inline-styled "Day 11–20 experiments are *coming soon*" landing. Includes a "next step" callout pointing at the Step 0 prompt in `AI_ENGINEER_ROADMAP_ADVANCED.md` and a `← Back to VAL` link to `localhost:3000`. Inline styles instead of Tailwind so this file is self-contained — drop it as-is when Step 0 generates the real UI.
- `npm install` ran in the new dir (27 packages, no errors). Smoke test: backgrounded `next dev --port 3001`, `curl localhost:3001/` returned 200 with the rendered HTML in 2 seconds; killed cleanly afterwards.
- **`.gitignore` deliberately untouched.** The advanced roadmap's Step 0 prompt instructs `/practice/` to be gitignored as a per-user sandbox, but this placeholder is meant to be the *shared* fallback that ships in the repo so the Advanced button works for anyone who clones without running Step 0. The per-day experiment folders that Step 0 creates (`practice/day-11/backend/` etc) are still expected to be gitignored when added — just not the shared `practice/frontend/` shell.
- Verified: `tsc --noEmit` in the new dir exits 0; `start-all.ps1`'s `Test-Path practice\frontend` branch now resolves to true so step 5 will spawn the third PowerShell window on the next run.

### `start-all.ps1` — em-dash inside string literal broke dot-source

- User reported `. .\start-all.ps1` failing with `The string is missing the terminator: "` at line 107 char 83, plus `Missing closing '}' in statement block` at line 94. False-alarm dismissal in the prior changelog entry was wrong — my AST-parser test loaded the file via `[Parser]::ParseFile()` (which honors UTF-8), but **dot-sourcing from an interactive PowerShell session reads with the legacy default codepage** (Windows-1252 on most English Windows installs).
- **Root cause.** The line I added — `Write-Step "Practice frontend not found at practice\frontend\ — skipping."` — contained an em-dash (`—`, U+2014, UTF-8 bytes `E2 80 94`). Decoded as Windows-1252, those three bytes become `â€"` — and the trailing byte `0x94` is `”` (U+201D RIGHT DOUBLE QUOTATION MARK). PowerShell's lexer **treats curly quotes as equivalent to straight `"` inside expandable strings**, so the parser saw `"Practice frontend not found at practice\frontend\ ”` as a closed string, then read ` skipping.` as bareword tokens, then `."` re-opened a new string that ran until EOF — hence the cascade of errors blaming lines 94 / 107.
- **Fix.** Replaced all three em-dashes in [start-all.ps1](start-all.ps1) (one in the string literal at line 95, two in comments at lines 68 + 87) with ASCII hyphens. The comments would probably have been tolerated by the parser, but I converted them too for consistency — and because PowerShell occasionally surprises you in strings that *aren't* obviously strings.
- **Verified by running the same code path the user did.** `Get-Command -Syntax .\start-all.ps1` (which loads via the default codepage, same as `.` dot-sourcing) now succeeds; AST parse also clean. [stop-all.ps1](stop-all.ps1) had no non-ASCII characters to fix.
- **Forward rule:** plain ASCII in `.ps1` is the safest default. If a future script genuinely needs Unicode characters in string literals, save the file as UTF-8 **with BOM** so the PowerShell engine auto-detects the encoding instead of falling back to Windows-1252.

### Navbar Advanced button → practice frontend on :3001

- The navbar's "Advanced" button used to point at the in-app `/advanced-ai` route (a "coming soon" stub). Per `AI_ENGINEER_ROADMAP_ADVANCED.md`, the Day 11–20 curriculum lives in a separate Next.js app under `practice/frontend/` running on port 3001. Rewired both buttons (desktop nav + mobile hamburger menu in [frontend/app/components/Navbar.tsx](frontend/app/components/Navbar.tsx)) from a Next.js `<Link href="/advanced-ai">` to a plain `<a href="http://localhost:3001">` — `Link` is for in-app client-side routing, so it can't cross to a different origin. Removed the now-dead `pathname === '/advanced-ai'` active-state branches on both, since clicking the button always navigates off-origin and the active state would never trigger. Same-tab navigation (no `target="_blank"`); if the practice app isn't running, the browser will surface a clear connection-refused page rather than us silently swallowing the click.
- The orphan `/advanced-ai` page still exists on disk for now — harmless, can be deleted later if nothing else links to it.

### `start-all.ps1` / `stop-all.ps1` — practice ports 3001 + 8001

- The dev-stack scripts previously knew only about ports 8000 (VAL backend) and 3000 (VAL frontend). Now that the Advanced button hops to `localhost:3001`, the scripts need to bring that surface up and tear it down too.
- **[start-all.ps1](start-all.ps1).** New step 5 after the existing frontend launch: if `practice/frontend/` exists, spawn another PowerShell window running `npm run dev -- --port 3001` from that directory. If the folder is missing (which is the current state — the advanced roadmap's Step 0 scaffold prompt hasn't been run yet), the script prints a one-line skip notice pointing at `AI_ENGINEER_ROADMAP_ADVANCED.md` and continues without erroring. Final summary block now lists the practice URL only when started, and adapts the closing hint between "two" and "three" spawned windows depending on whether the practice frontend came up. Header comment updated to mention the new behavior.
- **[stop-all.ps1](stop-all.ps1).** Added `Stop-Port 8001` (practice backend, in case the user has a per-day FastAPI on 8001) and `Stop-Port 3001` (practice frontend) between the existing 8000 / 3000 stops. `Stop-Port` already no-ops when nothing's listening, so the new calls are safe even before the practice folder exists. Header comment updated to enumerate all four ports.
- **`.bat` wrappers untouched.** `start-all.bat` and `stop-all.bat` just delegate to the `.ps1` files via `powershell -ExecutionPolicy Bypass -NoProfile -File`, so the new behavior flows through them automatically.
- Verified: both PowerShell files AST-parse cleanly via `[Parser]::ParseFile(...)` (which respects the file's UTF-8 encoding — my first quick parse-check used `Get-Content -Raw | [scriptblock]::Create(...)` which decoded as Windows-1252 and reported a false-positive "missing terminator" on the em-dashes I added; the AST parser, like the .bat wrapper's `-File` invocation, decodes correctly).

### `AI_ENGINEER_ROADMAP_ADVANCED.md` — outer prompt fence promoted to 4 backticks

- The Markdown preview for the advanced roadmap was scrambling everything after the "Step 0 — Setup prompt" section. Root cause: the outer prompt block at line 99 opened with a triple-backtick fence, then nested another triple-backtick `​`​`bash` block on line 165 for the practice README setup. Markdown's CommonMark fence rules don't allow same-length nesting — the inner closing fence at line 187 terminated the outer block early, line 439 re-opened a "new" block, and everything from then on rendered as alternating code / prose in the wrong order.
- **Fix.** Promoted only the outer pair (line 99 and line 439) from three backticks to four. Inner triple-backticks at lines 165 / 187 now render as literal text inside the outer code block — which is what the user wants, since the whole block is a prompt to paste verbatim into Claude Code and the nested `​`​`bash` is part of the prompt's literal content. Two more indented triple-backticks at lines 330 / 344 (the per-day README template) were already safe — they're inside the outer block as content lines, so markdown swallows them.
- **Verification.** Wrote a small Python depth-tracker that walks every `^\s*​`​`+` line, matches lengths, and reports any unclosed fences. Pre-fix: imbalance (the inner `​`​`bash` looked like a fresh open). Post-fix: all fences balance, no orphans. Every other prompt block in the file was already clean (no inner unindented fences) so nothing else needed touching.

### Day 10 / N49 — gpt-image-1 swap

- DALL-E 2 is gone for many OpenAI accounts; live `/day10/generate-image` started erroring with `BadRequestError: The model 'dall-e-2' does not exist (code: invalid_value)`. Migrated the endpoint to `gpt-image-1`, the current default image model.
- **Backend `routers/day10.py`.** `_IMAGE_MODEL` → `gpt-image-1`. `_ALLOWED_SIZES` swapped to gpt-image-1's set (`1024x1024`, `1024x1536`, `1536x1024`, `auto`) — the smaller DALL-E 2 sizes (256, 512) are not supported. New `_ALLOWED_QUALITIES = {low, medium, high, auto}` and per-tier `_QUALITY_COST` map (`low $0.011 / medium $0.042 / high $0.167`). `GenerateRequest` gained `quality: str = "low"` and the default size moved to `1024x1024`. The call site adds `quality=quality` to `images.generate(...)`. Response now handles `b64_json` (gpt-image-1's default — no URL field) by wrapping it as `data:image/png;base64,…` so the `<img>` renders inline; the previous `getattr(item, "url", ...)` path is kept as a fallback in case the SDK / model ever changes its mind. `cost_usd` in the response now reflects the selected tier; the response also echoes `quality` for the UI footer.
- **Backend `token_tracker.py`.** Added `"gpt-image-1": {"flat": 0.011}` to the OpenAI rates block. The tracker only supports one flat rate per model so this **pins navbar cost accounting to the "low" tier we default to**; calls at medium/high will under-count in the global counter (acceptable for a demo — the per-call `cost_usd` in the response is still accurate). A code comment on the rate captures the trade-off.
- **Frontend `app/day-10/page.tsx`.** `IMAGE_BADGE` → `openai · gpt-image-1`. New `ImageSize` / `ImageQuality` types and a `QUALITY_OPTIONS` array with inline `$cost` labels (`low · $0.011`, …). `GenerateImage` gained a `quality` state and a quality button row matching the existing size-button row styling (violet active state). The footer line now reads `provider · model · size · quality · $cost/image · latency`, the loading text says "waiting on gpt-image-1…", and the explainer was rewritten to talk about quality tiers instead of DALL-E 2's flat rate.
- Verified: backend import clean, `tsc --noEmit` + `eslint app/day-10/page.tsx` both exit 0.

### Day 10 / N49 — vision URL pipeline now server-fetches the image

- Live `/day10/describe-image` against the original Wikimedia preset URLs failed with OpenAI's misleading `BadRequestError: You uploaded an unsupported image. Please make sure your image has one of the following formats: ['png', 'jpeg', 'gif', 'webp']`. Root cause is not the format — OpenAI's downstream image fetcher gets blocked / hands back HTML on hosts that User-Agent-gate (Wikimedia's `upload.wikimedia.org` thumb redirector is the classic offender; quick `httpx` probe with even a friendly UA returned 400 + HTML).
- **Backend fix.** New `_fetch_image_as_data_url(url)` helper in `routers/day10.py`. Uses `httpx.Client(follow_redirects=True, timeout=15.0)` with a real `Mozilla/5.0 (compatible; ValBot/1.0; +<repo>)` UA and `Accept: image/*`. Validates `Content-Type` against `_ALLOWED_IMAGE_MIMES = {image/png, image/jpeg, image/gif, image/webp}`, enforces the existing 4 MB cap, then base64-encodes into `data:{mime};base64,…`. Maps network failures to 502 ("could not fetch image: …"), non-200 status to 502 ("image host returned HTTP X"), wrong content-type to 400 with the allowed list, oversize to 413. `describe_image_url()` now calls `_fetch_image_as_data_url(url)` and hands the data URL to the existing `_run_vision()`, so the URL and upload paths both reach OpenAI as data URLs through the same code path. Pulled `httpx` into the module imports (already in `requirements.txt`).
- **Frontend fix.** Swapped the two Wikimedia preset URLs for three reliable-against-bots hosts: `https://picsum.photos/seed/val-cat/640/480`, `https://picsum.photos/seed/val-mountain/640/480`, and an Unsplash photo. Quick probe confirmed both hosts return `200 image/jpeg` to a generic UA. Also took an IDE diagnostic on `max-h-[420px]` → `max-h-105` (Tailwind 4 canonical class).
- Verified: backend import clean (`/describe-image`, `/describe-upload`, `/generate-image`, `/moderate`), `tsc --noEmit` + `eslint app/day-10/page.tsx` both exit 0.

### Day 10 — MCP + Safety + Multimodal (N46–N50)

- **Backend `routers/day10.py` — four endpoints**, all OpenAI-only because Groq / DeepSeek / Gemini's OpenAI-compatible APIs don't uniformly support vision multipart, image generation, or moderation (per the spec's "no provider switcher — vision / image gen are OpenAI only" note). Shared `_openai_client()` returns a fresh `openai.OpenAI(api_key=OPENAI_API_KEY)` per call; `_fail(label, e)` wraps every call in a 502 with the underlying exception class. Vision uses `gpt-4o-mini`, image gen uses `dall-e-2`, moderation uses `omni-moderation-latest` — the chat and image models are already in `token_tracker.RATES` so cost is tracked accurately; the moderation model is missing from `RATES`, which means `_calc_cost` returns 0.0 (correct — the moderation endpoint is free).
  - `POST /describe-image` — JSON body `{image_url, question?}`. URL must be `http(s)://` (validated, else 400). `_run_vision(payload, question)` is the shared helper that builds the chat-completions request with the multimodal content-block shape (`[{type: "text", text}, {type: "image_url", image_url: {url}}]`), tracks tokens, and returns `{description, tags, raw, prompt_tokens, completion_tokens, total_tokens, latency_ms, provider, model}`. `_split_description_tags()` peels a trailing `TAGS: a, b, c` line out of the model output into a lowercased keyword array (max 8) so the UI can render them as chips.
  - `POST /describe-upload` — multipart `file` + optional `question` form field. 4 MB cap, content-type must be `image/*`; the upload is base64-encoded into a `data:{mime};base64,…` URL and handed to the same `_run_vision()` so both paths share one code path and one response shape.
  - `POST /generate-image` — JSON body `{prompt, size="512x512"}`. Validates size against `{256x256, 512x512, 1024x1024}`, clamps prompt to 1000 chars, calls `client.images.generate(model="dall-e-2", prompt, size, n=1)`, tracks one image (`prompt_tokens=1` triggers the flat-rate branch in `token_tracker._calc_cost`). Returns `{prompt, size, image_url, latency_ms, cost_usd: 0.0018, provider, model}`. The returned DALL-E URL is signed and expires ~1 hour after generation, which the UI surfaces in the explainer.
  - `POST /moderate` — JSON body `{text}` (max 4000 chars). Calls `client.moderations.create(model="omni-moderation-latest", input=text)` and normalizes the per-category `categories` and `category_scores` attribute bags into a sorted list of `{category, flagged, score}` rows (sorted by score desc). Tracks the call at 0 tokens / $0. `_as_dict(obj)` is a defensive normalizer that tries `model_dump()`, then `dict()`, then a `dir()` walk — covers the various shapes the OpenAI SDK has returned across minor versions.
- **Frontend `app/day-10/page.tsx` — five node tabs N46–N50** replacing the "Coming soon" stub, same shell as Day 5/6/7/8/9 (header + day chip, `NODES` nav with ✓, mark-complete wired to `useNodeProgress(10)`, `lg:-mx-24 xl:-mx-40` widening, all five panels stay mounted with the active one toggled via `hidden` so the vision panel preview + the generated DALL-E image survive a hop to N47 and back). Shared `SectionHeader` / `ErrorBox` / `Footer` / `ExplainerBlock` / `StaticBadge` / `SubmitButton` helpers; the page is OpenAI-only across the board, so there is no `ModelSwitcher` anywhere — each node renders a `StaticBadge` (`openai · gpt-4o-mini` blue for vision, `openai · dall-e-2` violet for image gen, `openai · omni-moderation-latest` coral for moderation).
  - **N46 MCP** — 4-card role strip (Host → Client → Server → Resource), then a 2-col side-by-side of a `tools/call` JSON-RPC request (blue) and the corresponding result envelope (green), then a 3-col comparison table "Raw function calling vs MCP" (where tools live, discovery, sharing, transport). No backend call — entirely static curriculum content.
  - **N47 Safety** — 4-card pillar grid (Honesty / Harmlessness / Helpfulness / Privacy) above a moderation form with 3 sample presets (benign / borderline / clearly unsafe). On submit, a top "flagged | safe" pill (red or green), then a top-6 category-score list rendered as `CategoryBar` rows (green ≤10%, amber ≤flagged, red if flagged). Footer surfaces provider/model.
  - **N48 Multimodal** — a single overview table of 5 modalities (Text↔Text grey / Image→Text blue / Text→Image violet / Audio↔Text amber / Video→Text coral) listing input, output, and color-tinted example-model chips. No backend call.
  - **N49 Vision + DALL-E** — two stacked subsections sharing the page-level state. **Describe** has a URL/upload mode toggle (URL preset chips with two Wikimedia images, upload preview via `FileReader.readAsDataURL`, optional question input), a 2-col result layout (input image left blue, description + tag chips right green via the shared `Markdown` component). **Generate** has 3 prompt presets, a 3-button size selector (256/512/1024), and a violet result card with the generated image, the prompt as a caption, and `provider · model · size · $0.0018/image · latency` in the footer. `ImagePreview` uses a raw `<img>` with an inline ESLint-disable for `@next/next/no-img-element` because the DALL-E URL is dynamic and signed — would defeat `next/image` optimization, and the Wikimedia presets aren't in `next.config.js`'s `remotePatterns`.
  - **N50 Dev tools** — 4-card comparison (Claude Code coral / Cursor blue / Aider amber / Gemini CLI violet) listing shape, good-for, trade-off. Below: a green "You built this with…" callout pointing at `CHANGELOG.md`, then a "50 / 50 — roadmap complete" closer.
  - Every node closes with an `ExplainerBlock` (tools-once-hosts-many, classifier-is-a-separate-model, the-content-block-is-the-trick, vision-is-just-another-content-block, generation-is-fire-and-forget, the-meta-lesson).
- A few JSX-string gotchas surfaced during typecheck — JSX string attributes don't honor backslash escapes (`body='it\'s'` parses as `it\` + a stray `'`), and `react/no-unescaped-entities` flags raw `"` inside JSX text. Fixed by switching one ExplainerBlock body to `body={"...it's..."}` (JS expression with double-quoted string, no escape needed) and replacing the literal quotes in one help line with `&ldquo;` / `&rdquo;`.
- Verified: backend import + route list (`/describe-image`, `/describe-upload`, `/generate-image`, `/moderate`), frontend `tsc --noEmit` and `eslint app/day-10/page.tsx` both exit 0. Progress catalog (`backend/routers/progress.py`) already carries N46–N50 and the router was already registered in `main.py`; no change needed. Roadmap is now 50/50.

### Day 9 — AI Agents (N41–N45)

- **Backend `routers/day09.py` — three endpoints**, all reusing `provider.py` for runtime-switchable model selection; single-language per the 2026-05-11 policy. Three tools (`calculate`, `get_weather`, `search_web`) shared between the single- and multi-agent flows. `calculate` is the same AST allow-list `_safe_arith` Day 3 / Day 4 use (`+ - * / ** %`, numbers only); `get_weather` is a deterministic mock seeded by city name; `search_web` is a tiny keyword-indexed canned snippet store (~8 rows on RAG / agents / embeddings / pgvector / Depok / LangGraph / OpenAI Agents SDK / CrewAI) with a generic-snippet fallback — no network call, so demos are reproducible without an API key for a real search provider. `_run_agent_loop()` is the shared loop primitive: takes `client + model + system_prompt + user_question + max_iterations + endpoint + prov + optional label`, runs up to 8 iterations of `chat.completions.create(..., tools=_TOOLS, tool_choice="auto")`, appends each tool result back as a `role: "tool"` message, records every assistant thought / tool call / tool result as a typed `step` (`user | thought | tool_call | answer`), tags steps with the optional `agent` label, accumulates token usage, calls `track()` once at the end, and returns `{answer, steps, iterations, prompt_tokens, completion_tokens, tools_used, latency_ms}`.
  - `POST /agent` — body `{question, provider?, max_iterations=5}`. Wraps `_run_agent_loop` with the user-facing `_AGENT_SYSTEM` prompt ("always call calculate for math, get_weather for weather, search_web for facts"). Returns the full trace plus `total_tokens` and `tools_available` so the UI can render the catalog the model actually sees.
  - `POST /multi-agent` — body `{topic, provider?}`. Three-stage pipeline using the same client/model: (1) **Researcher** = `_run_agent_loop` with `_RESEARCHER_SYSTEM` ("call search_web at least once, output 3–5 bullet facts"), `label="researcher"`; (2) **Critic** = single `response_format={"type":"json_object"}` call returning `{verdict: "ship" | "needs_more", feedback: "..."}` (server-validates the enum, clamps feedback to 240 chars); (3) **Writer** = single plain call with a templated system prompt that pastes the researcher's facts + critic verdict + feedback. Per-agent latency and token counts are returned individually plus a rolled-up `total_*` so N44 can show the per-stage cost without re-summing.
  - `GET /tools` — describes the three-tool catalog (name + description + example args) for the UI to render statically without round-tripping.
- **Frontend `app/day-09/page.tsx` — five node tabs N41–N45** replacing the "Coming soon" stub, same shell as Day 5/6/7/8 (header + day chip, `NODES` nav with ✓, mark-complete wired to `useNodeProgress(9)`, `lg:-mx-24 xl:-mx-40` widening, all five panels stay mounted with the active one toggled via `hidden` so trace results survive tab switches). Shared `SectionHeader` / `ErrorBox` / `Footer` / `ExplainerBlock` / `ProviderRow` / `SubmitButton` helpers; tool-color map (`calculate` blue / `get_weather` amber / `search_web` violet) and `KindBadge` (user grey / thought violet / tool_call blue / answer green) drive the consistent palette. `StepTimeline` + `StepRow` is the reusable step renderer used by N41, N42, N43, and N44's collapsible researcher trace — each tool call shows args + result side-by-side as wrapped mono JSON, errors swap the result border to coral, the final answer is rendered through the shared `Markdown` component.
  - **N41 What are agents** — 5-step "agent loop" strip (Goal → Think → Act → Observe → Decide) above a tiny live agent form (default `"What is 24 * 7?"`, `max_iterations=3`) so the user can watch the loop run end-to-end in one or two iterations.
  - **N42 Use cases** — four scenario cards (customer support / research analyst / travel planner / data sidekick), each with a colored left border, an italic preset prompt in a dashed box, and a one-click "run this case" button. Hits `/day09/agent` with the preset; renders the same trace below.
  - **N43 Tools** — three-card tool catalog (name + signature + description + example) above the main form. Submit-time `max_iterations` slider (1..8), 3 preset prompts, then a 3-stat summary row (iterations / tools used / tokens · latency) above the full step trace.
  - **N44 Multi-agent** — pipeline strip (1·Researcher / 2·Critic / 3·Writer) above the form, then on submit a 3-col result row: researcher facts (violet), critic verdict + feedback (amber, with the verdict embedded in the column title), writer answer rendered through `Markdown` (green). Collapsible "▸ show researcher trace" below reuses `StepTimeline`. Footer surfaces summed prompt + completion tokens across all three agents.
  - **N45 Frameworks** — four comparison cards (Raw OpenAI tool calls / OpenAI Agents SDK / LangGraph / CrewAI), each with vibe + good-for + trade-off + a ~7-line Python snippet in a mono code box. No backend call; pure curriculum content closing the day.
  - Every node closes with an `ExplainerBlock` (why-the-loop-matters, the-shape-is-always-the-same, the-model-never-runs-the-tool, specialization-is-the-trick, pick-the-smallest-thing-that-fits).
- Verified: backend import + route list (`/agent`, `/multi-agent`, `/tools`), frontend `tsc --noEmit` and `eslint app/day-09/page.tsx` both exit 0. Progress catalog (`backend/routers/progress.py`) already carries N41–N45 and the router was already registered in `main.py`; no change needed.

---

## 2026-05-22

### Repo housekeeping — .gitignore, .env.example, monorepo fold

- New root `.gitignore` covering secrets (`.env`, with a `!.env.example` allow-rule), Python (`__pycache__`, `.venv/`, `*.py[cod]`, `*.egg-info/`, `*.sqlite3`, `*.db`), Node/Next.js (`node_modules/`, `.next/`, `out/`, `build/`, `dist/`, `*.tsbuildinfo`, `next-env.d.ts`, npm/yarn debug logs), and editor/OS files (`.vscode/`, `.idea/`, `.DS_Store`, `Thumbs.db`). Verified via `git check-ignore` that `.env`, `frontend/node_modules`, and `backend/__pycache__` are excluded.
- New root `.env.example` mirroring `.env` with all four provider keys, `DEFAULT_PROVIDER`, per-provider model overrides, `OPENAI_EMBEDDING_MODEL`, and `DATABASE_URL` — values replaced with `xxxx`/placeholder patterns so the file is safe to commit. The `!.env.example` exception in `.gitignore` keeps it tracked.
- **Fixed broken embedded submodule.** `frontend/` was checked into the parent repo as a gitlink (mode `160000`) with no `.gitmodules` — leftover from `create-next-app` initializing its own `.git` inside the directory. GitHub rendered it with the submodule arrow icon and none of the frontend's actual files were tracked by the parent. Resolved by `git rm --cached frontend`, deleting `frontend/.git` (only loss: a single boilerplate "Initial commit from Create Next App" — no remote, no other commits), and `git add frontend` so the files re-enter the parent repo as normal tracked files.
- **Security note flagged separately to the user:** the live `.env` was pasted into chat while building `.env.example`, exposing real API keys for OpenAI / Groq / DeepSeek / Gemini. Recommended rotating all four; the new `.gitignore` prevents future leakage but doesn't un-expose the keys already shown.

### Day 6 — tab-switch state persistence fix

- Day 3/4/5/6 rendered the active node with `{activeId === 'N26' && <NodeInspector />}` — which unmounts every *other* node and destroys their local state. Submitting an N26 inspection then hopping to N27 and back wiped the previous result; user flagged this on Day 6.
- Fix in [frontend/app/day-06/page.tsx](frontend/app/day-06/page.tsx): render all five node components always, toggle visibility via Tailwind's `hidden` class on a wrapping `<div>`. Components stay mounted, their `useState` values (`result`, `text`, `query`, etc.) survive tab switches. Trade-off: all 5 forms exist in the DOM after first render — cheap because no node auto-fetches, every API call is submit-gated.
- Day 7 and Day 8 were built with the same persistence pattern from the start. Day 3/4/5 still have the unmount-on-switch behavior and could be retrofitted on request.

---

## 2026-05-19

### Day 8 — RAG (N36–N40)

- **Backend `routers/day08.py` — seven endpoints**, embeddings always OpenAI via `_embed_batch()`, generation runtime-switchable via `provider.py`, all writes into the existing `rag_chunks` table. Single-language responses per the 2026-05-11 policy (the frozen `{answer_en, answer_id}` shape stays in the roadmap as spec only). `_chunk(text, size=500, overlap=50)` walks the text in `size − overlap` steps; `_insert_chunks(source, chunks, vectors)` deletes any prior rows for that source then bulk-inserts via `executemany`, so re-uploading a filename replaces the old chunks; `_retrieve(query_vec, top_k)` runs the same `SELECT id, source, chunk_index, text, 1 − (embedding <=> %s::vector) AS score … ORDER BY embedding <=> %s::vector LIMIT %s` shape Day 7 introduced. DB-down still raises a 503 with the start-Postgres hint instead of crashing.
  - `POST /upload` — multipart `.txt` (UTF-8, 256 KB cap). Returns `{source, chunks_stored, tokens_used, chunk_size, chunk_overlap, embed_provider, embed_model, preview}` with the first 3 chunks for instant feedback.
  - `POST /upload-text` — JSON `{source, text}` body for the paste-text path (no multipart needed). Same chunking + insert as `/upload`.
  - `POST /retrieve` — JSON `{question, top_k=5}`. Embeds + retrieves only, no LLM. Powers N39 in isolation so the user can see retrieval quality before any generation tokens are spent.
  - `POST /ask` — JSON `{question, provider?, top_k=5}`. Embeds + retrieves + generates with a strict RAG system prompt: "Use ONLY the context below. If the answer is not in the context, reply exactly: 'I don't have that in the provided documents.'" Returns `{answer, sources, embed_tokens, gen_tokens, gen_prompt_tokens, gen_completion_tokens, gen_provider, gen_model, latency_ms}`.
  - `POST /compare` — JSON `{question, provider?, top_k=5}`. Runs the LLM twice with the *same* user question: once with a plain "answer from your own knowledge" system prompt, once with the RAG context-only prompt. Returns both sides plus latency + per-side token counts so N37 can render the difference side-by-side.
  - `GET /sources` — aggregated source list with chunk counts.
  - `DELETE /sources/{source}` — drops all chunks for one source so the UI can clean up demo data.
- **Frontend `app/day-08/page.tsx` — five node tabs N36–N40** replacing the "Coming soon" stub, same shell as Day 5/6/7 (header + day chip, `NODES` nav with ✓, mark-complete wired to `useNodeProgress(8)`, `lg:-mx-24 xl:-mx-40` widening). Sources list is lifted to the page (`useState<SourceItem[]>` + a stable `refreshSources` + `deleteSource`) and passed into N36 (read-only), N37/N39/N40 (just the `sourceCount` for empty-state hints), and N38 (read + mutate). All five panels stay mounted with the active one toggled via `hidden` — your N40 answer survives a hop to N38 and back. Shared `SourceList` (per-row delete) and `ChunkList` (score bars + source/index header + score percent) components handle the repeated UI.
  - **N36 What is RAG** — 5-step pipeline strip (Index → Embed query → Retrieve → Augment → Generate) plus the live indexed-sources list.
  - **N37 vs Fine-tune** — RAG vs Fine-tuning comparison cards (Where / Update / Cost / Time / Privacy) above a live "ask with vs without RAG" demonstrator: same question through `/compare`, plain LLM in amber on the left, RAG-grounded answer + collapsible sources in green on the right. ModelSwitcher chooses the generator for both columns.
  - **N38 Implementing** — tabbed `paste text` / `upload .txt` (max 256 KB). Two paste presets (rag-notes / pizza-recipe) so the same demo answers a relevant question on N40 and a clear "off-topic" refusal. After a successful upload, the response renders an indexed badge plus the first 3 chunks inline; the lifted `SourceList` below has per-row delete.
  - **N39 Retrieval** — query + `top_k` slider (1..10) + 3 presets. Hits `/retrieve` only and renders the chunks with the same `ChunkList` component used everywhere else. Footer shows it's an embed-only call ("no LLM call"). Empty-state hints if nothing is indexed or no chunks match.
  - **N40 Generation** — query + `top_k` slider + `ModelSwitcher` for generation. Hits `/ask`, renders the answer through the shared `Markdown` component, plus a collapsible `▸ show N sources` block. Footer shows the full cost picture: `embed: openai · gen: {prov}/{model} · {embed_tokens}+{gen_tokens} tok · {latency_ms}ms`.
  - Every node closes with an `ExplainerBlock` (RAG-is-runtime-knowledge, when-to-RAG-vs-fine-tune, why-chunks-not-docs, retrieval-has-no-opinion, system-prompt-is-the-trick).
- Mount-time `useEffect` follows the Day 7 lint-clean pattern — inline async IIFE with a `cancelled` flag so the first `setState` fires post-`await`, satisfying `react-hooks/set-state-in-effect`. The page-level `refreshSources` callback (used by button clicks and the post-upload hook) is the same function reference passed to `SourceList`.
- Progress catalog (`backend/routers/progress.py`) already carried N36–N40 and the router was already registered in `main.py`; no change needed. Verified: backend import + route list (`/upload`, `/upload-text`, `/retrieve`, `/ask`, `/compare`, `/sources`, `/sources/{source}`), frontend `tsc --noEmit` and `eslint` both clean.

---

## 2026-05-18

### Day 7 — Vector Databases (N31–N35)

- **Backend `routers/day07.py` — four endpoints**, OpenAI embeddings only, all going through pgvector on the existing `documents` table (`id TEXT PK, text TEXT, embedding vector(1536)`). Reuses Day 6's `_embed_one`/`_track` shape; embeddings serialize to a `'[v1,v2,...]'` literal and cast via `%s::vector` in SQL — no `pgvector.psycopg2` adapter dep added. DB-down case raises a 503 with a clear "start Postgres + run init_db.py" message instead of crashing.
  - `POST /ingest` — body `{id, text}`. Embeds + upserts (`ON CONFLICT (id) DO UPDATE`) so re-ingesting the same id replaces the row. Returns `{id, dimensions, tokens_used, provider, model}` per the frozen spec contract.
  - `POST /search` — body `{query, top_k=5}` (clamped to 1..50). Embeds the query, runs `SELECT id, text, 1 - (embedding <=> %s::vector) AS score FROM documents ORDER BY embedding <=> %s::vector LIMIT %s` — pgvector's `<=>` is cosine distance, `1 - dist` gives a similarity in [-1, 1]. Returns `{results: [{id, text, score}], tokens_used, …}`.
  - `GET /documents` — lists every stored row (`id`, `text`, `has_embedding`) in id order. Used by the frontend to render a live "what's in the DB right now" panel on N31 and N34.
  - `DELETE /documents/{id}` — single-row cleanup; 404s if id is unknown. Not in the frozen spec but added so the N34 panel can prune demo rows without touching SQL.
- **Frontend `app/day-07/page.tsx` — five node tabs N31–N35** replacing the "Coming soon" stub, same shell as Day 5/6. Document list is lifted to the page (single `useState<Doc[]>` + a `refreshDocs` callback + a `deleteDoc` callback), passed down to N31 (read-only) and N34 (read + mutate). All five node panels stay mounted with the active one toggled via the `hidden` class — same persistence pattern Day 6 adopted earlier, so an N35 search result survives a tab switch back from N34.
  - **N31 What is a DB** — 5-step pipeline strip (Text → Embed → Store → Query → Rank, each a color-coded left-border card) plus the live `DocList` so the user can see how the row count changes as they ingest.
  - **N32 Embed models** — 4-card comparison: `text-embedding-3-small` (highlighted as "used here"), `text-embedding-3-large`, Cohere `embed-multilingual-v3`, Sentence Transformers `all-MiniLM-L6-v2`. Shows dims / pricing / access / note + an explainer on why models are not interchangeable across an index.
  - **N33 Platforms** — 4-card comparison: pgvector (highlighted), Pinecone, Weaviate, and a grouped Qdrant/Milvus/Chroma card. Explainer pushes "start with pgvector if you already run Postgres".
  - **N34 Indexing** — id input + text textarea + 4 presets; on submit POSTs `/ingest` and refreshes the doc list. Success card shows `id — N dims · X tok · openai` in green. `DocList` below has per-row delete buttons calling `DELETE /documents/{id}`.
  - **N35 Search** — query input + `top_k` slider (1..min(20, max(docCount, 10))) + 3 query presets. Renders top-1 in green, the rest in blue, each with a percentage score and `ScoreBar`. Empty-state hints when the DB is empty or the query returns zero rows; closes with an explainer on the SQL fragment that powers it.
  - Every node closes with an `ExplainerBlock` (vector-DB-is-SQL-with-a-vector-column, models-can't-mix, start-with-pgvector, embed-once-query-many, the entire `ORDER BY <=> LIMIT k` recipe).
- **Lint pattern fix.** Initial mount fetch used `useEffect(() => { refreshDocs(); }, [refreshDocs])` matching the existing `useNodeProgress` shape — which trips `react-hooks/set-state-in-effect` because `refreshDocs` synchronously sets `docsLoading=true` inside the effect body. Refactored to an inline async IIFE inside the effect with a `cancelled` flag, so the first setState fires post-`await` (no longer synchronous in the effect body). `refreshDocs` stays available for button-driven refreshes from N31 and the post-ingest hook in N34. Triggers an HMR "useEffect dep array changed size" console warning across the edit; a hard refresh clears it.
- Progress catalog (`backend/routers/progress.py`) already carried N31–N35 and the router was already registered in `main.py`; no change needed. Verified: backend import + route list (`/ingest`, `/search`, `/documents`, `/documents/{doc_id}`), frontend `tsc --noEmit` and `eslint` both clean.

---

## 2026-05-17

### Day 6 — Embeddings (N26–N30)

- **Backend `routers/day06.py` — five endpoints**, OpenAI embeddings only via `get_embedding_client()` and `OPENAI_EMBEDDING_MODEL` (no provider switcher per the frozen Day 6 spec). The spec lists only `/embed` and `/search`; added `/classify`, `/recommend`, and `/anomaly` so N28–N30 are hands-on, matching the established Day 3/4/5 pattern of one endpoint per node. Shared `_embed(inputs)` helper returns vectors + total tokens in a single batch call; `_cosine(a, b)` reuses Day 2's numpy formulation; every call is `track()`-ed under `day="day06"` via `_track(endpoint, tokens)`.
  - `POST /embed` — body `{text}`. Returns `{embedding, dimensions, tokens_used, provider, model}` per the frozen contract — single-text inspector.
  - `POST /search` — body `{query, documents[], top_k=3}`. Batches `[query, ...docs]` in one embedding call, cosine-ranks docs against the query, returns `{results (top_k), all_results, tokens_used, …}`.
  - `POST /classify` — body `{text, labels[]}`. Zero-shot classification by embedding the text + every candidate label and picking the closest. Returns ordered `scores[]` plus `top_label` / `top_score`. Requires ≥2 labels.
  - `POST /recommend` — body `{seed_index, items[], top_k=3}`. Embeds the whole catalog in one call, returns nearest neighbours to the seed (the seed itself excluded). Validates `seed_index` and rejects empty items.
  - `POST /anomaly` — body `{items[]}` (≥3). Embeds, takes the mean vector as centroid, scores each item by `1 − cos(item, centroid)`, flags items at ≥ `mean + 1.5·σ` as anomalies. Returns sorted `results[]` with the threshold, mean, and σ surfaced so the UI can show why an item tripped.
- **Frontend `app/day-06/page.tsx` — five node tabs N26–N30** replacing the "Coming soon" stub, same shell as Day 5 (header + day-progress chip, `NODES` nav with ✓, mark-complete wired to `useNodeProgress(6)`, `lg:-mx-24 xl:-mx-40` widening, shared `SectionHeader`/`ErrorBox`/`Footer`/`ExplainerBlock`/`PresetRow`/`SubmitButton` plus a new local `StaticEmbedBadge` and `ScoreBar`). No `ModelSwitcher` anywhere on the page — every node renders a static `openai · text-embedding-3-small` badge instead.
  - **N26 Inspector** — text input + presets; `VectorPreview` renders the first 32 dims as a centered bar chart (positive bars up green, negative bars down coral, normalized to local max) plus the first 8 raw floats in a mono grid. Hover any bar for the exact value.
  - **N27 Search** — query input + multi-line documents textarea + 3 query/doc preset pairs. After response, all docs render in cosine order; the top-`k` (3 by default) get the emerald accent, the rest are muted — so the visual cut between "matched" and "didn't" is immediate.
  - **N28 Classify** — text presets + editable label chips with add-by-Enter and per-chip delete (min 2 enforced). Result shows a top-label badge plus a ranked similarity bar per label (winner green, rest blue).
  - **N29 Recommend** — pre-seeded catalog of 8 items, each row click-to-set as seed (violet pill `★ seed`), add/remove items inline. Response renders the seed in a violet block above the green "you might also like" list with score bars.
  - **N30 Anomaly** — multi-line items textarea, 2 presets (off-topic review + suspicious login). Result lists items in distance order with green normal rows and red `anomaly` badge + red bar when above threshold. A muted header line surfaces centroid dims, mean, σ, and threshold so the math behind the flag is visible.
  - Every node closes with an `ExplainerBlock` (meaning-as-coordinates, meaning-vs-letters, labels-are-sentences, nearest-neighbour cold start, outliers-far-from-centroid).
- Progress catalog (`backend/routers/progress.py`) already carried N26–N30 and the router was already registered in `main.py`; no change needed. Verified: backend import + route list (`/embed`, `/search`, `/classify`, `/recommend`, `/anomaly`), frontend `tsc --noEmit` and `eslint` both clean.

---

## 2026-05-16

### Day 5 — AI Models (N21–N25)

- **Backend `routers/day05.py` — four endpoints**, single-language per the 2026-05-11 policy (the roadmap's frozen spec proposed an Ollama-vs-active `/compare`; replaced with a real multi-provider compare since the project already ships four providers — no Ollama dependency). Shared `_usage()` / `_fail()` helpers; `_kind()` maps `groq`/`deepseek` → `open`, `openai`/`gemini` → `closed`. Every model call is `track()`-ed under `day="day05"`.
  - `POST /pretrained` — body `{question, provider?}`. `response_format={"type":"json_object"}`; the model answers from frozen weights and self-classifies the question as `parametric` / `time_sensitive` / `private_data` with a one-line `why` (server-validated enum). Teaches the training-cutoff gap that motivates RAG/tools.
  - `POST /compare` — body `{question, providers[]}`. Loops each known provider, times it with `perf_counter`, and returns a `results[]` of `{provider, model, kind, answer, latency_ms, tokens, error}`. Per-provider failures are caught into the `error` field (one dead provider doesn't 502 the batch); unknown providers filtered out.
  - `POST /ask` — body `{question, provider?}`. Plain single-answer + `kind` + `latency_ms`, reused by the N23/N24 "ask one live" panels.
  - `POST /finetune-vs-prompt` — body `{task, examples[{input,output}], test_input, provider?}`. Two calls: a base call (instruction + test input only) vs a "simulated fine-tune" that injects the examples as prior user/assistant turns. Returns `{base, tuned, …}` so the UI can diff weight-baked vs runtime-conditioned behavior.
- **Frontend `app/day-05/page.tsx` — five node tabs N21–N25**, same shell as Day 4 (header + day-progress chip, `NODES` nav with ✓, mark-complete wired to `useNodeProgress(5)`, `lg:-mx-24 xl:-mx-40` widening, shared `SectionHeader`/`ErrorBox`/`Footer`/`ExplainerBlock`/`ProviderRow`/`PresetRow`/`SubmitButton`/`Badge` and the `VIZ` palette). Model answers render through the shared `Markdown` component instead of a duplicated parser.
  - **N21 Pre-trained** — question + presets (one parametric, one time-sensitive, one private-data); shows a color-coded `knowledge_type` badge + `why` above the answer.
  - **N22 Closed vs open** — multi-select provider chips (default `openai`+`groq`, tinted by `open`/`closed` kind), 2-col result grid with closed=amber / open=green top border, per-card latency + tokens or inline error.
  - **N23 / N24** — a shared `ModelFamily` explorer: clickable static model cards (GPT-4o/Claude/Gemini; Llama/Mistral/DeepSeek) with vendor/license/access/context detail, plus a live "ask one" form hitting `/day05/ask` through the `ModelSwitcher`.
  - **N25 Fine-tuning** — editable task + add/remove example rows + held-out test input; 2-col base (grey) vs simulated fine-tune (violet) with combined token footer.
  - Every node closes with an `ExplainerBlock` (frozen knowledge, rent-vs-own weights, closed=rent, open=own, fine-tune vs prompt trade-off).
- Progress catalog (`backend/routers/progress.py`) already carried N21–N25 and the router was already registered in `main.py`; no change needed. Verified: backend import + route list, frontend `tsc --noEmit` and `eslint` both clean.

### Day 5 / N21 — knowledge timeline visual

- New `KnowledgeTimeline` block added below the existing N21 chat answer, gated on `result` and keyed on the question so it remounts per submission. Fade-in via a `visible` state flipped on a 30ms `setTimeout` (opacity 0→1, translateY 8px→0, 400ms ease) — same reveal pattern as Day 4's `RoleVoiceVisual`. Full-width single column; the N21 form, `knowledge_type` badge, answer box, token footer, and "Pre-training is frozen knowledge" `ExplainerBlock` are untouched.
- **Vertical timeline.** A green-left-border "before training cutoff" zone, an amber centered "✂ Training cutoff — weights frozen here" divider (flex line + label + line), and a red-left-border "after cutoff" zone. Each item is a `TimelineItem` (colored dot + text + uppercase pill, color-tinted bg/border at `1F` alpha). Below: a "How to fill the gap" 2-col grid (`GapCard` — blue RAG, violet Tools/function-calling) separated by a top border, then a green-left-border 💡 insight note.
- **First pass shipped hardcoded example items; replaced with question-derived classification.** `splitClauses()` splits the asked question on `,` / word-bounded `and` / `?` / `;`, trims, drops empties. `classifyClause()` lowercases and checks time-sensitive keywords first (`recent`, `latest`, `today`, `price`, `who won`, …), then private keywords (`my`, `our`, `company`, `internal`, `private`), else `parametric`. Parametric clauses render in the green zone (`parametric` pill); time-sensitive → red (`hallucination risk`), private → red (`never seen`). Empty-state notes: "No stable facts detected in this question" (green) / "No time-sensitive topics detected" (red). No hardcoded items remain.
- **Defensive fix — `question is undefined` runtime TypeError.** When the live `/day05/pretrained` response omitted `question` (stale `--reload`'d backend, or any path returning no echo), `splitClauses` threw on `.split` of `undefined`. Hardened: `splitClauses` accepts `string | null | undefined` and coerces via `(question ?? '')`; `KnowledgeTimeline`'s prop type widened to match; the call site falls back to the in-scope user-typed `question` state (`result.question ?? question`) for both the `key` and the prop, so the timeline still reflects what was asked even if the API drops the field. `tsc --noEmit` + `eslint` clean after each change.

---

## 2026-05-15

### Day 4 / N16 + N18 — visualizers, raw toggle, role fan-out, markdown

- **N16 — schema + extraction visualizer.** Added `StructuredVisualizer` then promoted it to the primary display: the old unstructured/structured side-by-side grid (and its now-dead `SCHEMA_KEYS` / `sentimentColor` helpers) was removed. 3-column layout `grid-cols-[1fr_32px_1fr]` (12px gap): LEFT a code-style schema box (muted `{ }` braces, teal mono key + violet type pill + muted description per row) above a prose-input box populated from the actual submitted textarea; CENTER a vertically-centered `extract →` arrow hidden until reveal; RIGHT six `OutField` blocks (`name`/`category`/`price_range` mono, `sentiment` color-coded badge, `key_features` blue pills, `summary` muted 1.5 line-height) reading the live API response, then a green-left-border 💡 insight. Refs-only animation (`sectionRef` / `proseRef` / `arrowRef` / `fieldRefs[6]` / `insightRef`, timers in `timersRef`, cleared on unmount): section 30ms → prose 150ms → arrow +400ms → fields 280ms apart → insight exactly 400ms after the last field. Component is keyed by a `runId` counter so it replays per extraction; whole section gated on `result`.
- **N16 — "Show raw output" toggle.** Below the visualizer + token `Footer`, a muted mono `▸ Show raw output` / `▾ Hide raw output` button expands a wrapped mono code box showing `result.raw`. Default collapsed, `showRaw` resets on each submit — same disclosure pattern as N14's raw-message-trace. "Why a schema beats parsing prose" `ExplainerBlock` kept directly below.
- **N18 — role voice comparison visual.** New `RoleVoiceVisual` below the existing comparison, gated on `result`, keyed by a `roleRunId` counter, fade-in via a `visible` state (opacity 0→1 + translateY 8→0, 0.4s). Title "Same task — three different roles" / subtitle. Each card: role badge pill colored by the actual role identity (`ROLE_META`: `security_engineer` red `#E24B4A`, `kindergarten_teacher` teal `#1D9E75`, `unix_manpage` blue `#378ADD`; no-role grey `#9CA3AF`; custom violet), 120-char excerpt with a color-matched left border, and four `TraitBar`s (Formality from avg alpha-word length, Length from word count, Jargon = matched-tech-words/total×100, Analogy = analogy-phrase-count×20 — all capped 100, pure JS string ops). Trait values held in React state, computed in an effect on mount.
- **N18 — backend role fan-out for 3 cards.** The visual needed three voices but `/day04/role` returned only `plain` + `roled`. Extended `role_control` to also call a second contrasting preset (`second_key` = first `ROLE_PRESETS` key ≠ the selected one), `track()`-ed, and return a 3-item `results[]` (`role_key`, `role_name`, `answer`, tokens) = `[no-role, selected role, second role]`. `plain` / `roled` / `role_system` retained so the existing comparison columns and system-prompt strip stay untouched. Frontend `RoleResponse` gained `results`; `RoleVoiceVisual` now maps all three via `voiceFromResult()` into a `md:grid-cols-3` grid (results[0]→No role, [1]→Role 1, [2]→Role 2, colored by role name).
- **N18 — markdown rendering.** API answers contained raw markdown showing literal symbols. Added dependency-free `renderInline()` (regex tokenizing `**bold**` → `<strong>`, `` `code` `` → styled inline `<code>`) and `MarkdownText()` (line-based block parser: `#`/`##`/`###` headings, consecutive `- `/`* ` lines → one `<ul>`, blank lines split `<p>` paragraphs). Applied to both `RoleColumn` answers and the comparison-card excerpts (the excerpt `<p>` became a `<div>` since block-level markdown can't nest in `<p>`).
- **N18 — fenced code blocks.** Extended `MarkdownText` with a fenced-block branch checked **first** in the line loop (before blank/heading/bullet/paragraph) so ```` ```lang ```` … ```` ``` ```` is consumed before inner bold/heading patterns can match — renders `<pre><code className="font-mono language-<lang>">` with `var(--paper)` bg, `0.5px solid var(--rule)` border, 6px radius, 10/12px padding, 12px font, `overflow-x:auto`, `white-space:pre`. Inline code restyled to `var(--paper)` bg, 1px/5px padding, 3px radius, 12px. (Spec's `--color-background-secondary` / `--color-border-tertiary` tokens don't exist in this project's `globals.css`; used the real `--paper` / `--rule` equivalents.)

### Day 4 — Prompt Engineering Advanced (N16–N20)

- **Backend `routers/day04.py` — five endpoints**, single-language per the 2026-05-11 policy (the roadmap's original bilingual `{response_en, response_id}` shape is frozen as spec, not implementation). Shared `_usage()` / `_fail()` helpers; every model call is `track()`-ed under `day="day04"`.
  - `POST /structured` — body `{description, provider?}`. System prompt declares a fixed six-key schema (`name, category, price_range ∈ budget|mid|premium|unknown, sentiment ∈ positive|negative|neutral, key_features[2–5], summary`) with `response_format={"type":"json_object"}`. Validates/normalizes enum fields server-side, clamps `key_features` to 5, returns parsed object + the `raw` JSON string + tokens.
  - `POST /system` — body `{question, provider?}`. Runs the **same user question** through three system-prompt variants (`none` baseline / `concise` terse-expert / `eli5`) and returns `results[]` of `{id, label, system, note, answer, tokens}` so the UI can diff behavior at fixed input.
  - `POST /role` — body `{task, role, provider?}`. `role` is a `ROLE_PRESETS` key (`security_engineer` / `kindergarten_teacher` / `unix_manpage`) or free-text. Makes two calls — plain (no system) vs roled (role as system) — returns `{plain, roled, role_system, …}`.
  - `POST /react` — body `{question, provider?}`. Classic text-format ReAct loop (not the tool-calling API): system prompt constrains output to `Thought:` / `Action:` / `Action Input:` or `Final Answer:`. Up to 6 iterations with `stop=["Observation:"]`; `_parse_react()` line-scans each step, the server executes `calculator` (AST allow-list eval, same `_safe_arith` as Day 3) or `lookup` (small `_FACTS` table with fuzzy contains + first-principles fallback), appends `Observation:` and re-prompts. Returns an ordered `steps[]` of `{type: thought|action|observation|answer, …}` plus `answer`, `iterations`, summed tokens.
  - `POST /temperature` — body `{prompt, temperatures[], provider?}`. One call per temperature (clamped to `[0,2]`), returns `results[]` of `{temperature, response, tokens}`.
- **Frontend `app/day-04/page.tsx` — five node tabs N16–N20**, same shell as Day 3 (header + day-progress chip, `NODES` nav with ✓, mark-complete wired to `useNodeProgress(4)`, `lg:-mx-24 xl:-mx-40` widening). Local helpers `SectionHeader` / `ErrorBox` / `Footer` / `ExplainerBlock` / `ProviderRow` / `PresetRow` / `SubmitButton` / `Badge`; shared `VIZ` palette (`green #1D9E75, blue #378ADD, violet #7F77DD, amber #FAC775, coral #F5C4B3`).
  - **N16 Structured** — 2-col "Unstructured input → Structured output". Each schema key renders as a colored left-border block; `key_features` as `Badge` chips, `sentiment` as a color-coded badge (green/red/grey).
  - **N17 System** — an "identical user message" callout pinned above a 3-col grid; each column shows the variant label, its system prompt in a mono box (`— (none)` for the baseline), the answer, and per-call tokens. Loading state renders 3 placeholder columns.
  - **N18 Role** — role-preset pills (violet active state) + task presets; the resolved `role_system` shown in a violet-left-border strip; 2-col `RoleColumn` "No role" (grey) vs "With role" (violet).
  - **N19 ReAct** — tool palette badges (calculator/lookup), then an `<ol>` of step nodes colored by type (thought violet / action blue with tool chip + mono input / observation amber / answer green 1.5px bold). Refs-only staggered reveal: `stepRefs[]` flipped via `setTimeout` (120ms + i·280ms) pushed into `timersRef`, kicked off in `requestAnimationFrame` after the response; cleanup clears timers on unmount.
  - **N20 Temperature** — a blue→amber→coral gradient spectrum bar with `0.0 focused / 0.7 balanced / 1.4 wild` labels, then a 3-col grid of per-temperature cards (top-border tinted by temp band) with response + tokens; placeholder cards while sampling.
  - Every node closes with an `ExplainerBlock` articulating the underlying mechanic (schema-as-contract, system prompt as leverage, role as behavior switch, reason→act→observe loop, temperature as the creativity dial).
- Progress catalog (`backend/routers/progress.py`) already carried N16–N20; no change needed for the mark-complete button. Backend router was already registered in `main.py`.

### Navbar — responsive on mobile with a days hamburger menu

- The sticky-header day nav had `hidden md:flex`, so on screens < 768 px the 10 day links disappeared entirely. Mobile users had no way to switch days from the navbar.
- **Final shape on mobile (< sm).** `● VAL  [☰]                  [76,680 tok · $0.0223]`. The hamburger sits in the left group right after the `VAL` wordmark (the natural primary-nav slot). Tapping it opens a panel beneath the sticky header with two sections:
  - **Days** — `grid-cols-5 gap-1.5` so the 10 day chips lay out as 2 rows of 5. Same active/done coloring as the inline chips: active+done = filled accent; active+not-done = filled foreground; not-active+done = accent outline with `✓`.
  - **More** (separated by a top border) — stacked `Usage` and `Advanced` links matching the desktop chip styling.
  - Tapping any link calls `setMenuOpen(false)`; a route change also auto-closes via the existing `useEffect` on `pathname`.
- **On `sm+`** the inline day nav comes back (`hidden ... sm:flex` on the `<nav>`) and `Usage` / `Advanced` re-appear inline in the right group (`hidden ... sm:inline-block`). The hamburger button itself is `sm:hidden` so it disappears on desktop.
- **Layout knobs that made everything fit.** Outer flex container went from `gap-6 px-6` to `gap-3 px-3 sm:gap-6 sm:px-6` to recover mobile horizontal room. Left wrapper got `min-w-0 flex-1` so its inline day nav (on `sm+`) can `overflow-x-auto` without pushing the right group off-screen; scrollbar hidden via `style={{ scrollbarWidth: 'none' }}`. VAL link, hamburger, and each day chip all carry `shrink-0` so nothing collapses to illegibility.
- **Iterations along the way.** First attempt removed the `hidden md:flex` outright and relied on horizontal scroll — visible but only ~2 days fit before the right-side controls. Second attempt tried a hamburger that contained `Usage` + `Advanced`; corrected after feedback so the hamburger holds days instead. The hamburger was initially grouped with the right-side controls, then moved to sit immediately after `VAL`. Next fix: `Usage` + `Advanced` were still inline on mobile and overlapping the hamburger because the right group couldn't fit at < 380 px — moved them into the hamburger panel's new "More" section, so the mobile inline navbar shrinks to just `VAL + ☰ + token button`.
- **Scroll trap fix.** With the panel sitting inside `sticky top-0`, an opened menu would extend the sticky height and on short viewports (landscape phone) effectively own the visible area — the user reported that page scroll wouldn't engage until they had scrolled the menu content to its end. Two changes resolved it: (a) the panel's inner container got `max-h-[calc(100vh-3.5rem)] overflow-y-auto overscroll-contain` so it's bounded by viewport-minus-header and won't chain-scroll the page, and (b) a `useEffect` (gated on `menuOpen`) attaches `mousedown` + `touchstart` listeners on `document` that close the menu when the target is outside both `menuRef` (the panel) and `menuButtonRef` (the hamburger). Listeners are cleaned up when the menu closes or the component unmounts. Result: any tap on the page area dismisses the menu and the user can scroll immediately; taps inside the menu (including the day chips and Usage / Advanced links, which already had `onClick={() => setMenuOpen(false)}`) keep working as before.

### Project rename — AI Engineer Roadmap → VAL (Visual AI Learning)

- Renamed the user-facing brand from "AI Engineer Roadmap" to **VAL — Visual AI Learning**. Long form ("VAL — Visual AI Learning") used in browser metadata, FastAPI `title`, layout footer, top-level `AI_ENGINEER_ROADMAP.md` H1, and `CHANGELOG.md` intro. Compact form ("VAL") used in `Navbar.tsx` sticky-header wordmark. `usage/page.tsx` "Roadmap progress" relabeled to "Learning progress" to match.
- Touched: `frontend/app/layout.tsx`, `frontend/app/components/Navbar.tsx`, `frontend/app/usage/page.tsx`, `backend/main.py`, `AI_ENGINEER_ROADMAP.md`, `CHANGELOG.md`, `start-all.ps1`, `stop-all.ps1`.
- **Intentionally left alone** so nothing breaks: the `ai-engineer-roadmap/` folder name and all path references in docs, the `AI_ENGINEER_ROADMAP.md` filename and the constant references that point at it, and the internal `"service": "ai-engineer-roadmap"` health-check identifier in `backend/main.py`.

### Day 1 — interactive visuals + always-on 2-column layout

- Each of N1–N5 now has its own static educational visual that sits **side-by-side** with the ask form rather than stacked below the answer. The page no longer reveals visuals only after a successful response — they are always mounted.
- **Page shell.** New 2-column grid under the existing header / tab strip / active-node title: `<div className="grid grid-cols-1 items-start gap-6 md:grid-cols-2">`. Left column holds the form (provider switcher, textarea / term input, ASK button), inline error, "thinking…" placeholder, and the `(hasResult || isStreamingActive)` answer section (thinking disclosure, `AnswerCard`, token footer). Right column always renders `<VisualN{1..5} />` based on `activeId`. Below `md` the columns collapse to a single column with ask first, visual underneath.
- **Removed the old reveal flow.** `streamDone` derivation, the `FadeIn` wrapper, and the `streamDone && <FadeIn key={activeId}>…</FadeIn>` block were deleted — visuals are always-on now. `VisualSection` lost its `border-t border-rule pt-6` since visuals are no longer beneath the answer.
- **Shared palette** lives in a single `VIZ_COLORS = { green #1D9E75, blue #378ADD, violet #7F77DD, teal #14B8A6, amber #FAC775, coral #F5C4B3 } as const`. Pill / tinted-bg backgrounds use the color with `1F` alpha suffix appended (~12%); active-state tints use `14` (~8%).
- **N1 — Three flavors of an AI person.** Three role cards (AI Engineer / ML Engineer / AI Researcher), each a flex row with a 2-px left border in the role color and 10 px / 14 px padding. Card body holds an uppercase color-tinted label (10 px), a bold one-line title (13 px), a one-line body (12 px), and a muted tools footer (11 px). Below the stack: a green-left-border 💡 strip — "You are here → AI Engineer. You use what ML Engineers and Researchers build." (After the initial 3-card top-border grid was too cramped in the narrow right column, this was rebuilt as the stacked-row variant.)
- **N2 — A typical week.** 4 weekday cards (Mon/Tue/Wed/Thu) with `borderLeft: 2px` per-day color (teal / amber / violet / coral) and a one-sentence task. Below: a matching 4-pill row (Integrate / Build / Optimize / Deploy) — pill background uses the same column color at 12% alpha, full rounded. Inner grid is `grid-cols-2 lg:grid-cols-4` so the right column shows 2×2 at md and a single row of 4 at lg.
- **N3 — 6-month learning path.** 6 month nodes with top-border color accents (teal → amber → violet → coral → blue → green). Each card has `minHeight: 80` so all six line up uniformly. Inner grid is `grid-cols-3` (no `lg:` upgrade) so it always renders as 2 rows of 3 — Months 1-2-3 on row 1, Months 4-5-6 on row 2. Below: an 8-px gradient bar with the same 6 stops in left-to-right order and a "VAL covers months 2–4" caption. (Initial `lg:grid-cols-6` was reverted after months in a single row crushed the labels.)
- **N4 — Two roles, side by side.** 2-card grid: AI Engineer (green top border, 5 bullets) vs ML Engineer (blue top border, 5 bullets). Below: an always-visible "Where they overlap" pill row (6 pills: Python, APIs, Cloud deployment, Data handling, Evaluation, Fine-tuning) — all green-tinted background, green border + text.
- **N5 — Click a term to expand.** 6 term cards in a 2-or-3-column grid (LLM, Token, Inference, Embedding, RAG, AGI). `useState<TermId>('llm')` drives the active card — green border + 8% green tint background + green label. Detail panel below has a 2-px green left border, the term label, mono-uppercase short subtitle, the analogy paragraph, and an "Example:" footer. All copy authored from the spec; no API call.

### All day pages — wider layout for ≥lg / xl screens

- Each `<article>` across `day-01`…`day-10` now breaks out of `main`'s `max-w-3xl` with `lg:-mx-24 xl:-mx-40` so the header, tab strip, active-node title strip, and any 2-col grid inside all widen together at larger viewports (≈ +192 px at `lg`, ≈ +320 px at `xl`). Mobile (< `lg`) is unaffected.
- Day-01 specifically went through a column-ratio detour — `md:grid-cols-2` → `md:grid-cols-[1fr_1.4fr]` → back to `md:grid-cols-2` (50/50). The final form keeps the columns at 1fr / 1fr and instead pushes overall page width wider via the `<article>` breakout above, giving the right-column visuals more horizontal room without changing the column ratio.
- Day-04 through day-10 are still "Coming soon" stubs — the breakout classes go on their `<article>` so their headings sit at the same width as content days.

### Layout footer — wider then centered

- `frontend/app/layout.tsx` footer ("● VAL — Visual AI Learning · Editorial edition") got `pt-10` for breathing room below the now-wider article. After a brief detour widening the footer with the same `lg:-mx-24 xl:-mx-40` so it tracked the article width, it landed on `text-center` instead: the footer stays at `mx-auto max-w-3xl` and centers its content within that, regardless of how wide the article above grew.

---

## 2026-05-14

### Day 3 / N15 — Prompt-caching visualizer, raw run rows collapsed

- New `CacheVisualizer` mounts below the existing run-results block and keys on a fresh `runId` counter (incremented on every successful `/day03/cache` submit) so each run replays the animation from a clean DOM. Section fades in via `sectionRef` (opacity 0→1, translateY 8→0, 500ms) before any per-run animation starts.
- **Two-column layout (gap 20px).** LEFT — "Token processing": one box per actual run from `result.runs` with header `Run 0N — cold start` for i=0 and `Run 0N — cache hit ✓` for i>0, latency rendered tabular-nums in `#22c55e` when faster than run 1 and `#9CA3AF` otherwise. 28-px-tall flex bar with three segments — cached `#1D9E75`, new prefix `#FAC775`, output `rgba(181,212,244,0.5)` — whose widths animate from 0% to `seg/(prompt+completion)*100%` over 600ms ease-out. Per-run text line `{cached} cached · {new} new · {output} output` with the cached number tinted green when > 0. Status line under each box (⚠️ for run 1, ✅ for run 2+). Below all boxes: animated speed-up badge `X.XXx faster` gated on `speedup > 1`, plus a four-item `CacheLegend` (cached / new prefix / output / uncached prefix).
- RIGHT — "How caching works": a 10-column token-flow grid sized at `min(run1.prompt_tokens, 80)` cells. On run 1, every cell fades in with amber background 28ms apart; on run 2, cells `0..min(run2.cached_tokens, cellCount)` flip to green 22ms apart. Caption "Each cell = one token. Green = cached on Run 02." Followed by a static "What the provider saves" paragraph, a cost-impact card comparing uncached `$0.150` (100% width amber) vs cached `$0.0375` (37% width green, "−75%" label), and a `💡` insight note at the bottom (12 px, left-border 2px muted) about placing stable content at the start of the prompt.
- **Refs-only animation engine.** `sectionRef`, `runRefs[]`, three bar-segment ref arrays per run (`cachedBarRefs` / `newBarRefs` / `outputBarRefs`), `pillRefs[]`, plus single refs for the speedup badge and insight note. A local `sched(delay, fn)` helper pushes each `setTimeout` into `timersRef` so the effect's cleanup function clears every in-flight timer. Cumulative sequence: section fade (30ms) → run-1 box reveal (300ms) → run-1 bar fill (600ms) → amber pills 28ms apart → 500ms gap → run-2 box reveal → 200ms later bar fill → 500ms gap → green pills 22ms apart → any further runs sequentially → speed-up badge (400ms after) → insight note (400ms after that).
- **Raw run rows collapsed.** The original `<ol>` of `CacheRunRow`s — which already displayed `{run.index, latency_ms, prompt+completion, cached, full answer text}` — now lives behind a `▸ show raw results (N runs · full answer text)` toggle (`showRawResults` default false, reset on submit). Same disclosure pattern used by N14's raw-message-trace toggle. Cache-prefix card, speed-up badge, provider note, and the `provider · model · N runs` `Footer` all stay above the toggle; "Why caching matters" `ExplainerBlock` is untouched.

### Day 3 / N14 — Agent loop visualizer, raw trace collapsed, initial-load polish

- **Backend.** `POST /day03/agent` now emits two new fields alongside the existing payload: `loop_trace[]` and `tool_anatomy`. The trace is built by walking the same iteration loop and emitting (a) `model_thought` entries whenever `msg.content` is non-empty (stripped), (b) `tool_call` entries `{iteration, tool, input}` before each handler dispatch, and (c) `tool_result` entries `{iteration, tool, result, is_error, error?}` after each handler returns — `is_error` derived from `"error" in result`. `tool_anatomy.last_call` and `last_result` capture the most recent round-trip as `{type: "tool_use", id, name, input}` and `{type: "tool_result", tool_use_id, content}` so the frontend has one canonical example for the right column. All existing fields (`timeline`, `iterations`, `tools_used`, token counts, etc.) untouched.
- **Frontend visualizer.** `AgentLoopVisualizer` renders below the message trace, keyed by `result.question` so it remounts on each submission. 2-column grid: LEFT walks `loop_trace` + a synthesized `{type: "final_answer", content: result.answer}` node tacked on at the end; `LoopNode` colorizes per type — model_thought violet `#c4b5fd` italic, tool_call left-bordered by tool color (calculator `#B5D4F4`, get_weather `#9FE1CB`, web_search `#FAC775`) with input JSON in a `<pre>`, tool_result green when ok / red `#ef4444` on `is_error` with the error message inline, final answer 1.5px solid emerald with bold text. Animated `↓` connectors render between every pair of adjacent nodes. RIGHT shows `AnatomyBox` for `last_call` and `last_result` as flat key:value rows (teal keys `#9FE1CB`, amber values `#FAC775` by default, green `#22c55e` overridden for the `content` key), plus a static "💡 Key insight" callout ("The model never runs code itself. It outputs JSON, your server executes it, and feeds the result back.").
- **Refs-only animation engine.** `sectionRef`, `nodeRefs[]`, `arrowRefs[]`, `timersRef[]`. `useEffect` keyed on `result` clears any pending timers, schedules `setTimeout`s that flip `style.opacity` / `style.transform` directly on each ref — no `useState` drives the reveal. Cadence: node N appears 300ms after node N-1, arrow N (between N and N+1) appears 150ms after node N. Initial node CSS state is `opacity: 0; transform: translateX(-8px); transition: opacity 320ms, transform 320ms`; arrows are `opacity: 0; transition: opacity 280ms`.
- **Raw message trace collapsed.** The original `<ol>` of `TimelineRow`s (user / assistant / tool blocks with the existing 320ms staggered reveal still wired via the parent's `revealed` counter) now lives behind a `▸ show raw message trace (N entries)` toggle — `showRawTrace` state default false, reset on every submission. Matches the same disclosure pattern used for N12's "show the actual prompts the model saw". `AgentSummary` (iterations / tools used / prompt / output tokens) stays visible above the toggle.
- **Initial-load polish.** `ToolPalette` ("Available tools" chips) is now gated on `loading || result` so the badges only appear once RUN AGENT is clicked; on a fresh page load only the form shows. The "What just happened" `ExplainerBlock` is gated on `result` and wrapped in a new generic `FadeIn` helper (opacity 0→1 + translateY 8→0, 400ms ease) so it slides in after the response. Loop visualizer + raw-trace toggle + agent stats all stay gated on `result` so nothing leaks pre-run.

### Day 3 / N12 — Zero-shot vs Few-shot visual

- New `ZeroFewShotVisual` sits below the existing side-by-side `ClassifyColumn`s. Stays hidden until the user clicks RUN COMPARISON: `showVisual` state defaults false, flips true after both `runOne('zero_shot')` and `runOne('few_shot')` resolve in `Promise.all`. On first reveal the section fades in (opacity 0→1, translateY 8→0, 400ms ease) via a `visible` state + 30ms `setTimeout`, mirroring the `PromptAnatomy` reveal pattern in N11.
- 2-column grid. LEFT shows the prompt-sent-to-model breakdown: system block (teal `#9FE1CB`, "Classify sentiment as Positive, Negative, or Neutral."), three labeled `example N` blocks (green `#22c55e`) that are hidden in `zero` mode and slide in 150ms apart in `few` mode (opacity / maxHeight / transform / margin co-transitioned over 300–350ms), user block (amber `#FAC775`) carrying the actual `submittedReview` captured at submit time (falls back to `FALLBACK_REVIEW` if the visual is mounted before any classification runs). Two token-info rows below: "Prompt tokens 29 | 214" and "Extra cost — | +185 tok" — values swap by mode.
- RIGHT is a mode-specific explainer: in `zero` mode a neutral-bordered card explains the model is relying on training-time priors; in `few` mode an emerald-bordered card with two `TradeoffBar`s (Accuracy 92% with `+12%` green tag, Token cost 75% with `+3×` amber tag) — bars animate `width` over 400ms ease-out.
- Mode toggle pills (zero-shot / few-shot) above the grid switch via `setMode`; example reveal animation lives in a `useEffect` keyed on `mode` that clears `timersRef.current`, then if `mode === 'few'` schedules three timeouts 150ms apart to flip individual entries of `revealedExamples[]`. Cleanup clears any pending timers on unmount. Helpers: `PromptPart` (colored-left-border content block, optional emerald `accent` flag) and `TradeoffBar` (label + percent + accent tag, animated width fill).
- Always-visible insight strip at the bottom (left-border emerald-500, paper bg, 💡 prefix): "The only difference between zero-shot and few-shot is the examples in the prompt. Same model, same weights — just more context."

### Day 3 / N11 — PromptAnatomy interactive builder

- New `PromptAnatomy` block sits *below* the existing N11 `StreamingExplainer`, so the chat answer, provider switcher, and free-text question are all untouched. Two-column grid (`grid-cols-1 md:grid-cols-2 gap-6`) — left = toggles + quality meter, right = live preview blocks.
- **Five toggleable components** in a single `PROMPT_COMPONENTS` array (`id, color, label, desc, content`): `system` `#9FE1CB`, `instructions` `#FAC775`, `examples` `#CECBF6`, `format` `#F5C4B3`, `constraints` `#B5D4F4`. Default state: `system` + `instructions` ON, the rest OFF. Each `ToggleRow` is a 36×20 iOS-style pill — track is a `<button>` whose background animates between `#10b981` (on) and `rgb(64,64,72)` (off) over 200ms, with an absolutely-positioned 16×16 white thumb that animates `left: 2 ↔ 18`. `aria-pressed` + `aria-label` set for accessibility.
- **Quality meter** uses a fixed lookup table `QUALITY_TABLE` keyed by active-count: `0→0%`/`1→20%`/`2→40%`/`3→62%`/`4→80%`/`5→100%`, each with a one-line description from "Empty — the AI has nothing to work with" to "Excellent — fully engineered prompt". Fill bar transitions `width` over 400ms ease-out; percent rendered tabular-nums.
- **Live preview blocks** — each `PreviewBlock` wraps in an outer `<div>` whose `opacity`, `maxHeight` (0↔240), `transform: translateY`, and `marginBottom` all transition together (300–350ms) so toggling produces a clean slide-in/out with no layout jump. Inner block has a 2px colored left border, an 8%-alpha tinted background built via template literal `${color}14`, an uppercase label in the component color, and a `<pre>` to preserve the embedded `\n` (used in the `examples` content's good/bad answer pair).
- **Empty state** — when `activeCount === 0`, a dashed-border placeholder ("All components off — the AI has nothing to work with 😅") renders above the now-invisible preview stack. All other times it's absent from the DOM rather than just hidden.
- All state is local React (`Record<ComponentId, boolean>` + `toggle()` setter). No refs, no animation libraries — pure CSS transitions on width / max-height / opacity / transform / margin / background-color.

### Day 3 implementation — Prompt Engineering, all five nodes

- **Backend `routers/day03.py` — four endpoints**, all using the streaming + single-language pattern's structured-JSON variant (per the 2026-05-11 policy update, the original bilingual `{result, reasoning_en, reasoning_id}` shape in the day prompt is frozen as the original spec, not the implementation):
  - `POST /day03/classify` — body `{review, technique: "zero_shot"|"few_shot", provider?}`. Returns `{sentiment, confidence, reasoning, prompt_used, prompt_tokens, completion_tokens, …}`. The few-shot system prompt is built by concatenating the zero-shot prompt with three labeled examples (positive / negative / neutral) so the model can see the desired JSON shape and the reasoning style.
  - `POST /day03/cot` — body `{question, technique: "direct"|"cot", provider?}`. Returns `{answer, steps?: string[], …}`. Direct system prompt forbids reasoning; CoT system prompt requires 3–6 step entries before the final answer. `response_format={"type":"json_object"}` on both.
  - `POST /day03/agent` — body `{question, provider?}`. OpenAI-compatible function-calling loop with three tools: `calculator` (AST-based safe-eval, allow-list of `+ - * / ** %` and unary; no names, attributes, or calls), `get_weather` (deterministic per-city `random.Random(city.lower())` so the same city always returns the same weather), `web_search` (mock results). Loop runs up to 5 iterations of `model → tool_calls → tool results → model`; usage summed across all iterations and `track()`-ed once at the end. Returns a full `timeline[]` of `{role, content?, tool_calls?, result?}` entries so the frontend can render the round-trip.
  - `POST /day03/cache` — body `{prompt, provider?, runs: 2-5}`. Sends the same `_CACHE_KB` (~1800-token system prefix, eight sections covering LLMs / tokens / embeddings / RAG / agents / caching / fine-tuning / evaluation) as the leading message on every run, then the user prompt. `_extract_cached_tokens()` reads `usage.prompt_tokens_details.cached_tokens` (OpenAI) or `usage.prompt_cache_hit_tokens` (DeepSeek), returns 0 for Groq/Gemini. Response includes `cache_supported: bool` and a per-provider `note` explaining what the user is or is not seeing.
- **Frontend `app/day-03/page.tsx` — five tabs N11–N15**, same shell as Day 2 (header with day-progress chip, NODES nav with ✓ on completed, mark-complete button wired to `useNodeProgress(3)`). Helpers `SectionHeader` / `ErrorBox` / `Footer` / `Stat` are local-to-file (intentionally not extracted to a shared module yet — duplication is fine across pages until we have more than three day pages doing this).
  - **N11 Basics** — reuses `StreamingExplainer` against `/day01/ask/stream`, default question covers system/user messages + the four levers.
  - **N12 Zero-shot vs Few-shot** — three review presets + free-text. Hits `/classify` twice in parallel (`Promise.all`), renders side-by-side `ClassifyColumn`s with sentiment badge (color-coded — emerald/accent/rule), confidence bar, reasoning. Collapsible "▸ show the actual prompts the model saw" reveals both system prompts so the user can literally diff the few-shot examples.
  - **N13 Chain of Thought** — three reasoning puzzles (bookstore-customer arithmetic, classic water-jug, painters-walls-hours trap). Hits `/cot` twice in parallel; `DirectCard` shows just the answer; `CoTCard` reveals steps at 380ms cadence via a `setTimeout` chain held in `revealTimerRef`, each step transitioning `opacity` + `translateX(-6px → 0)` over 350ms. Final-answer block (emerald border) goes from 0.3 opacity to 1 once all steps have appeared.
  - **N14 Function calling** — three presets + free-text. Tool palette chips ("calculator ∑", "get_weather ☁", "web_search ⌕") render up top so the user knows what's available. After the response: 4 summary `Stat` cards (iterations / tools used / prompt+output tokens) then a `TimelineRow` per entry revealed at 320ms cadence. Rows are colored by role: user (muted), assistant (accent red left border, shows `→ call <name>` plus pretty-printed args JSON), tool (emerald left border, pretty-printed result JSON).
  - **N15 Prompt caching** — three presets + a 2–5 runs selector. After the response: prefix-size callout (chars + estimated tokens), big emerald "Speed-up X.XX×" badge (first vs last run latency) gated to only show when speedup > 1, then a `CacheRunRow` per run with a red latency bar AND an emerald "cached / total prompt tokens" mini-bar inside it. Note line at the bottom carries the provider-specific caveat from the backend.
- Every interactive node ends with an `ExplainerBlock` that articulates *why* the demo matters in plain English (few-shot anchoring, "thinking on paper", model→tool loop, prefill caching). The `BEGINNER_SYSTEM_PROMPT` voice is only used inside chat responses; these blocks are author-written so they can be precise about implementation details.
- Progress catalog in `backend/routers/progress.py` already had N11–N15 entries (no backend change needed for the mark-complete button to work).

### Devex + global polish — launcher, encoding, navbar persistence, usage chart

- **One-click launcher.** Added `start-all.bat` + `start-all.ps1` at the repo root. The `.bat` is a thin shim — double-click from Explorer and it runs the `.ps1` with `-ExecutionPolicy Bypass -NoProfile`. The PowerShell script does four sequential phases: (1) probes `docker info` and, if the daemon isn't up, launches Docker Desktop from `$env:ProgramFiles\Docker\Desktop\Docker Desktop.exe` and polls every 2 s for up to 2 minutes until it's ready, printing dots while waiting; (2) `docker inspect`-checks the `ai-roadmap-pg` container — if missing, prints the canonical `docker run …` command with the `ai-roadmap-pg-data` volume + `pgvector/pgvector:pg16` image and exits; if stopped, runs `docker start`; (3) spawns a new PowerShell window for the backend (`uvicorn main:app --reload --port 8000`) with auto-detection of `backend\.venv\`, `backend\venv\`, or `backend\env\` activate scripts; (4) spawns another window for the frontend (`npm run dev`). Closing the two child windows stops the servers; closing the launcher does not. Companion `stop-all.bat` + `stop-all.ps1` identify the dev servers by which process is listening on port 8000 / 3000 (via `Get-NetTCPConnection`) and `Stop-Process -Force` them, then `docker stop ai-roadmap-pg`.
- **UTF-8 mojibake purge in `day-01/page.tsx`.** Browser was rendering tabs as `ÅŒ N1 AI ENGINEER` and the submit button as `ASK Â†'` — every special character mangled. Root cause: at some point the file went through a UTF-8 → cp1252 → UTF-8 round-trip and the literal multi-byte glyphs got stored as their cp1252 misreading, then re-encoded as UTF-8. The file was still valid UTF-8 but its content was no longer the characters intended (`→` U+2192 stored as the 8-byte sequence `C3 A2 E2 80 A0 E2 80 99`, which decodes as `â†'`; CSS `uppercase` on the button then made `â` into `Â`). Diagnosed via a Python byte-level scan of all `.tsx`/`.ts`/`.css` under `app/` looking for the `C3 A2 E2|C2|C5` triple-byte mojibake prefix — only `day-01/page.tsx` was affected, with 11 sequences across 7 distinct corrupted glyphs: `✓`×2, `→`×1, `…`×4, `–`×1, `○`×1, `▾`×1, `▸`×1. Fixed via byte-level `data.replace(mojibake_bytes, real_utf8_bytes)` for each pattern, written back in binary mode so no editor / shell re-encoding could re-corrupt them. Post-fix scan confirms zero double-encoded sequences remain anywhere in `app/`.
- **Token counter persists across backend restarts.** `_totals` in `backend/token_tracker.py` was in-memory only, so any uvicorn `--reload` (or full backend restart) zeroed the navbar counter even though every call had been written to Postgres `token_usage` via `_persist`. Added `_rehydrate_from_db()`: groups `token_usage` by provider, replaces `_totals.by_provider` buckets and rolls up `total_tokens` / `total_cost_usd` / `call_count` from `SUM()` aggregates. Guarded by a module-level `_rehydrated_ok` flag and called lazily from `get_summary()` — so the first navbar `/tokens` fetch after restart triggers the rebuild, and retries every call until the DB pool returns a real connection (matches the existing 5s reconnect in `db.py`). Also updated `DELETE /tokens/reset` to wipe `token_usage` rows, otherwise rehydration would resurrect totals after every reset.
- **Navbar dropdown panel travels with the sticky header.** Previous structure: `<><header sticky>…</header>{open && <div>panel</div>}</>` — the panel was a sibling of the sticky header in document flow, so as soon as the user scrolled the header glued itself to the viewport top while the panel stayed pinned to the top of the body. Restructured: removed the fragment wrapper and moved the conditional panel *inside* `<header>`, after the inner h-14 flex toolbar. Flipped the panel's `border-b` to `border-t` so the rule sits between toolbar and panel rather than under the panel. Now the entire sticky element (toolbar + optional dropdown) follows the viewport.
- **Usage page Daily chart bars were invisible.** Y-axis tick labels were rendering at the correct vertical positions but the chart area below them was empty. Root cause: the bars container was `flex h-44 items-end` — `items-end` aligns children to the bottom of the cross axis but doesn't stretch them. Each bar wrapper was `<div class="flex-1">` with no explicit height, so it shrink-wrapped to its content height. Its child set `height: ${heightPct}%`, but percentage height needs a *resolved* parent height — the wrapper had none, so the percentage resolved to 0, the wrapper's content height was therefore 0, the wrapper itself was 0. Circular zero, no bars. Fix: `items-end` → `items-stretch` so wrappers fill the 176px container, plus `h-full flex-col justify-end` on each wrapper so its inner percentage-height stack stays anchored to the bottom. Added `minHeight: 2` for non-zero days so very small values still show a hairline rather than disappearing.

### Day 2 / N8 — PCA scatter retired, SimilarityCircle in its place

- **PCA polish first, then replacement.** Scatter plot got 60px margins, min-max normalization squeezed into `[0.1, 0.9]` (dots fill 80% of plot area), 4×4 grid + faint center axes, 12px dots with 2px white ring, label pills outside each dot, dashed connection lines from query to others (opacity = cosine score, clamped). Then ripped out entirely in favor of a **SimilarityCircle**: query at canvas center, others orbit at distance `(1 − score) × 180` (clamped to 230 to stay on-canvas), angles evenly distributed starting at 12 o'clock with an O(n²) iterative collision-avoidance pass (`+20°` per overlap, up to 10 passes). Three concentric rings r=60/120/180 with white-pill labels at the bottom so they never collide with dots. Dot color tiers: `≥0.3` teal `#1D9E75`, `≥0.1` gray, `<0.1` light gray. Connection lines teal for high / gray for low, dashed, opacity-scaled. Two-line labels (24-char truncated sentence + score in muted) with text-anchor flipped by angle quadrant and canvas-bound clamp. `pca2d` / `topEigenvector` / `normalize` helpers deleted.
- **Heatmap reveal animation hardened.** Was twitching and stopping early because `useEffect` deps were `[totalCells, vectors]` — `vectors` reference instability + 30s poll from `useNodeProgress` re-rendered the whole tree and restarted the loop. New: parent `NodeEmbeddings` owns an `embedKey` counter that increments only after a successful `setResult`; passed as the sole effect dep. Timer handle moved into `useRef`, and the recursive `setTimeout` chain stores each handle so cleanup can cancel any in-flight tick.
- **Scatter hover tooltip rewritten ref-only.** Previous `useState` + conditional render caused re-render twitching on every mouseenter/mouseleave. Now: one always-mounted `<div>` with `opacity:0` + 150ms transition, mutated via `tooltipRef.current.style` and `textContent` on `onMouseEnter`/`onMouseLeave`. Side-flip via `left`/`right` swap so tooltips on the right half of the canvas grow leftward.
- **Render decoupling via `React.memo`.** `VectorPreview` is now a thin composer; `HeatmapSection` and `SimilarityCircle` are memoized children. Animating `revealedCount` inside `HeatmapSection` no longer triggers PCA/Circle re-renders. `embeddings`/`scores`/`sentences` references are stable across parent renders (they live on `result`), so memo's shallow compare skips.

### Day 2 / N9 — "How inference works" visualizer

- New `InferenceVisualizer` sits below the existing inference results without touching the form, runs selector, provider switcher, latency stats, or runs list. Six step boxes reveal sequentially when **Run** is clicked (each hidden via `opacity:0` + `translateY(8px)`, revealed by direct style mutation): **1 Tokenize** (regex split → pastel `CHIP_PALETTE` chips), **2 Token IDs** (now real — see below), **3 Embedding lookup** (six random floats + `×1536`), **4 Transformer layers** (counts `Layer 1/12 → 12/12` at 120ms cadence with a progress bar), **5 Logits → sampling** (3 random candidates seeded by the prompt's last word, descending probability bars), **6 Output (live)** which streams the *real* API answer word-by-word at 35ms.
- Imperative API via `useImperativeHandle(controllerRef, () => ({ start, streamAnswer, reset }), [])` so the parent triggers animation without any React state for timing. All `setTimeout` handles collected in `timersRef`; new `start()` clears the previous run before scheduling. `step6ReadyRef` + `pendingAnswerRef` reconcile the two clocks — if the API responds before step 6 has revealed, the answer queues and drains on reveal.
- **Real token IDs from the backend.** Extended the existing `POST /day02/tokenize` (used by N7) with three new fields: `tokens`, `ids`, `note`. For `openai`: `tiktoken.encoding_for_model("gpt-4o-mini")` returns the actual BPE IDs and per-id decoded pieces. For other providers: regex split + `random.randint(1000, 50000)` with a note "Simulated IDs — {provider} uses its own tokenizer format, not tiktoken". Top-level `try/except ImportError` so the server still boots if `tiktoken` isn't installed (graceful degrade). `tiktoken==0.8.0` added to `requirements.txt`.
- Frontend step 2 calls `/day02/tokenize` with `{text: prompt, provider}` when `start()` fires. Shows "Fetching real IDs…" placeholder until response arrives, then swaps in real IDs and writes the `note` into a muted italic line below. Race control via `idsRequestIdRef` counter — stale responses are ignored if a newer Run started.

### Day 2 / N10 — "How vector search works" demo

- Visual RAG-retrieval walkthrough added below the existing N10 chat answer. 3-column grid (`2fr_40px_2fr_40px_3fr` after fixing initial query-box overflow): Documents → `embed →` → Vector DB slots → `search →` → Query + Result. Document cards have solid borders; DB slots use dashed borders; matched slots and their source docs get an emerald-500 border + 10% emerald tint via inline `style.borderColor` / `backgroundColor`. Result box has a permanent 1.5px emerald border. Insight note below the grid quotes the "retrieval part of RAG" framing.
- **Animation: refs + async/await + `setTimeout`, zero `useState` for timing.** Phases: docs reveal (180ms apart) → 400ms gap → embed arrow + slot fills (220ms apart) → 500ms → search arrow + query box → 600ms → highlight top-2 docs+slots → 400ms → populate + reveal result box → 400ms → reveal insight. `runIdRef` counter is bumped on every Run/Reset; after every `await sleep(...)` the async task checks `myId !== runIdRef.current` and bails, so Reset cleanly kills any in-flight animation.
- **Cross-node handoff via `sessionStorage`.** `NodeEmbeddings` persists `{texts, vectors, similarity_ranking}` under `day02-embed-result` after a successful embed. `VectorSearchDemo.loadDemoData()` reads it on each Run click; if at least 5 saved entries (query + 4 docs) and top-2 matches resolve to doc indices, the user's own sentences/vectors/scores drive the demo. Otherwise falls back to the hardcoded reference set ("Cats are popular…", "RAG combines search…", etc.) with scores 0.912 / 0.887 from the image. Vector previews formatted as `[a.aa, b.bb, c.cc … ×N]`.
- Query box overflow fix: `whiteSpace: 'normal'` + `wordBreak: 'break-word'` on the query text + vector preview, plus `minHeight: 88` to fit two lines without animation jitter. Result rows reflow as inline content (number, sentence, emerald score) so the score rides at the end of the wrapped sentence instead of being pinned to the row's right edge.

---

## 2026-05-13

### Beginner voice + domain context

- System prompt promoted to a single shared constant `BEGINNER_SYSTEM_PROMPT` in `backend/provider.py` (reused by `/day01/ask` and `/day01/ask/stream`, which is also what Day 2's N6/N10 explainers POST to).
- Voice rewritten for absolute beginners: "explain like to a curious 10-year-old," with six numbered rules — one-sentence plain answer first, real-life analogy, concrete mini-example, define jargon inline, ≤ 2 sentences per paragraph, never assume prior knowledge.
- Added a domain-context preamble so the model never wanders out of the AI/LLM frame: RAG = Retrieval-Augmented Generation (not Red Amber Green), tokens = LLM tokens (not auth tokens), models = AI/ML models (not fashion/3D). All questions are interpreted in the AI-engineering context by default.

### Flow diagrams

- **New chat contract: `{answer, diagram}`.** For naturally step-by-step topics (RAG pipeline, agent loop, function calling, multi-agent flow, vector DB query) the response includes an optional `diagram: { title, steps: [{id, label, sublabel?}], connections: [[fromId,toId], …] }`. For conceptual topics it's `null`.
- Streaming endpoints don't ask the streaming call to produce JSON — the prose still streams as plain text. After the stream finishes, the backend issues a small non-streaming JSON-mode call (`provider.generate_diagram(question, answer)`) and emits the validated diagram as `event: diagram` just before `event: done`. Token usage for that pass is tracked separately under `endpoint="/ask/stream/diagram"`.
- `useStreamingAsk` extended with a `diagram` field and a handler for `event: diagram`. `AnswerCard` accepts an optional `diagram` prop and renders `<FlowDiagram>` below the prose. Day-1 page threads it through its per-tab `Snapshot` map; Day-2 `StreamingExplainer` passes it from the hook directly.
- New `frontend/app/components/FlowDiagram.tsx`: pure SVG, no chart lib. 140×64 rounded boxes, 5 pastel colors cycled, label + optional sublabel centered. Up to 4 boxes per row, wraps to next row; same-row connections are straight arrows, cross-row use an elbow path (down → across → down). Single arrowhead marker, shared by all connectors.
- Reveal animation: boxes fade in one by one left-to-right, top-to-bottom (120ms cadence; 200ms ease-out opacity + `translateX(-8px)→0`). Connections fade in only after both endpoint boxes have appeared. SVG uses attribute `transform` for absolute position and a nested `<g>` for the CSS reveal transform — splitting them avoids the SVG-attribute vs CSS-`transform` conflict.
- Overflow fix: SVG now uses `width="100%"` with a natural-size `viewBox` and `maxWidth: width`, so wide diagrams scale down to fit narrow cards instead of clipping the rightmost box.

### Day 2 / N8 Embeddings — explainability + viz

- Backend `/day02/embed` now returns the raw `vectors` (full 1536-dim arrays) and the original `texts` alongside the existing `similarity_ranking`. Lets the frontend visualize embeddings without a second round-trip.
- Frontend `VectorPreview` added under the ranked results: heatmap of the first 24 dimensions per sentence (blue=negative → light gray → red=positive, scaled to absolute max across all visible cells), plus a 2D PCA scatter projecting 1536D → 2D in the browser. PCA is implemented manually — mean-center, power iteration for the top eigenvector, deflate, second power iteration — no numerical library.
- Plain-English explainer blocks: "What is a vector?" above the heatmap; "Why are they close or far apart?" above the scatter with the top- and bottom-ranked sentences and scores templated in from the live response; "💡 Key insight" box after the scatter naming the query and best-match sentence dynamically. Removed raw numbers next to heatmap rows in favor of a `★ Query` / `Other` badge (per-cell values still surface via tooltip).
- Scatter improvements: 48px min margin all sides, post-projection min-max normalization to fill the plot area, dashed 3×3 gridlines inside the plot, 10px-radius dots labeled with `★`/`1`/`2`/`3` inside (full sentence text in SVG `<title>` tooltip), red/gray legend, fixed 280px container with `overflow: hidden`.
- Heatmap reveal animation: cells fade in left-to-right, row by row, at 18ms cadence with a 220ms ease-out opacity transition. Driven by a `revealedCount` state ticker scoped to each fresh embed response.

---

## 2026-05-11

### Policy change — chat responses are now single-language + streamed

- Replaces the original bilingual `{answer_en, answer_id}` JSON contract. The model mirrors whichever language the user wrote in. Day pages no longer show two side-by-side cards — one answer card that fills in live as tokens arrive.
- Backend: new `stream_chat()` iterator in `provider.py` (yields `thinking` / `answer` / `usage` chunks; `thinking` only fires on providers exposing `reasoning_content`, e.g. DeepSeek-Reasoner). Endpoints expose SSE variants with `event: thinking | answer | done | error`.
- Frontend: new `useStreamingAsk` hook (manual SSE parsing over `fetch().body`) and a shared `AnswerCard` + in-repo markdown renderer at `frontend/app/components/Markdown.tsx` — no `react-markdown` dependency. Handles `**bold**`, `*italic*`, `` `code` ``, `#`/`##`/`###` headings, `-` bullets, and pipe-table syntax. Tolerates unclosed delimiters during streaming (a half-typed `**Pra` stays literal until the closing `**` arrives). A blinking caret marks the live tail.
- **For future day routers (3, 4, 5, 8, 9, 10):** the original day-by-day prompts in `AI_ENGINEER_ROADMAP.md` still describe the bilingual JSON shape — that's frozen as the original spec. New implementations should follow the streaming + single-language pattern instead.

### Navbar progress

- Navbar day numbers now show a red `✓` next to days where all 5 nodes are completed (active-day variant inverts to white-on-accent). Hook `useNodeProgress.toggle()` dispatches a `progress:changed` window event after a successful POST; Navbar listens for it so the checkmark appears the instant you mark complete — no page reload, no 30s wait for the polling tick. Also refetches on pathname change.

### Day 1 — per-tab streaming state

- `day-01` page keeps a `Record<nodeId, Snapshot>` of answers so switching N1↔N2 preserves each tab's result. One stream is allowed in flight at a time; switching tabs no longer aborts the stream (it keeps writing into its origin tab's snapshot), and a pulsing dot on the tab shows where the live stream is running. Ask buttons on other tabs disable with a "Streaming on N1…" label.

### Day 1 — streaming endpoint

- `POST /day01/ask/stream` added alongside the original bilingual `/ask`. SSE pipeline with `event: thinking | answer | done | error`; original `/ask` kept untouched for backward compat.
- `day-01` page switched to streaming via `useStreamingAsk`. Collapsible "Thinking" panel appears only when a provider emits reasoning tokens (DeepSeek-Reasoner).

### Day 2 / N7 — compare experiment (reverted)

- Built then **removed** a "Compare all providers" feature on N7 Tokens. Backend kept `POST /day02/tokenize/compare` (parallel) and `POST /day02/tokenize/compare/stream` (SSE — `init` event with text/char/word/pieces, one `result` event per provider as it returns, `done` at end) so the API surface is there if needed later. Frontend reverted: the per-provider tokenizer difference is real and interesting, but presenting it usefully would require 4 stacked result blocks (mostly duplicated) or a switching dropdown — both felt worse than the single-provider view. Left as a known unimplemented direction.

### Day 2 / N7 — tokens viz redesign

- **N7 Tokens** redesigned. The old "Visual split" wall of grey boxes didn't communicate what each metric meant. Replaced with three side-by-side **inline** views of the same input text, OpenAI-tokenizer-style:
  - **Chars view** — every character gets its own colored background; spaces remain bare so they read naturally.
  - **Words view** — whitespace-separated groups get colored backgrounds; the actual spaces stay between them.
  - **Regex pieces view** — words AND punctuation each get a piece, source whitespace preserved by walking the original text and reinserting gaps between consumed pieces.
- 6-color cycling palette so adjacent units never blend. "Real tokens" stays as a callout because we don't have actual token boundaries from any provider's API.

### Day 2 / N6 + N10 — switched to streaming

- `BilingualExplainer` (which still POSTed to `/day01/ask`) replaced with `StreamingExplainer` using the new `useStreamingAsk` hook + shared `AnswerCard`. N7 (Tokens), N8 (Embeddings), N9 (Inference) untouched — they don't produce prose answers.

---

## 2026-05-10

### Per-node completion tracking (added later that day)

- Per-node completion tracking added (persisted to Postgres):
  - New `node_progress` table (`node_id` PK, `day`, `completed`, `completed_at`).
  - New `backend/routers/progress.py` with the full 50-node catalog hardcoded for label lookup.
  - `GET /progress` returns all 50 nodes with completion status + per-day counts + total. `POST /progress/{node_id}` toggles (or accepts `{completed: true|false}` body for explicit set).
  - Tagged "Progress" in `/docs`.
- Frontend: shared `useNodeProgress(day?)` hook in `app/components/useNodeProgress.ts`. Day 1 / Day 2 pages now show:
  - A `[ ]`/`[✓]` indicator on each node tab (inverted colors when the tab is active so the checkmark stays visible).
  - A "mark complete / completed" button next to the active section header.
  - A `X / 5 nodes` chip in the page eyebrow row.
- `/usage` page header gained a `X / 50 nodes` chip plus a roadmap-wide progress bar and ten mini Day-1..Day-10 bars.
- React 19 hydration: `suppressHydrationWarning` + explicit boolean coercion on the mark-complete buttons (same class of issue we hit on `ModelSwitcher` earlier — resolved without deferring render).

### Postgres in Docker + /usage filter bar + stacked-bar chart

- Postgres now runs in Docker via `pgvector/pgvector:pg16` container `ai-roadmap-pg` (volume `ai-roadmap-pg-data`). Resolved a port-5432 collision with a native Windows PostgreSQL 18 service by stopping it (`Stop-Service postgresql-x64-18`).
- `db.py` connection pool now retries every 5s instead of latching to `None` after a failed first attempt — so the backend auto-recovers when Postgres comes up later.
- `/usage` rebuilt with filter bar + chart:
  - Range chips (Today / 7d / 30d / 90d / All), provider toggle chips (with strikethrough when off), and a `<input type="date">` for picking a single day. Filters compose; "clear" returns control to the range chips.
  - Stacked-bar chart (pure CSS, no chart lib) — Y-axis quartile ticks + gridlines, hover tooltips per bar, x-axis labels auto-thin when many days, providers stack with their legend colors.
  - Reactive top stats and per-provider % share row.
  - Daily fetch bumped to `days=365`, monthly to `months=12`; filtering happens client-side.

### Day 2 — `/day02/*` endpoints implemented

- `POST /day02/embed` implemented per spec (OpenAI cosine ranking).
- Added `POST /day02/tokenize` for N7 (regex-split visual + real `usage.prompt_tokens` from a tiny chat call so the count matches the model's own tokenizer).
- Added `POST /day02/inference` for N9 (1–5 runs, latency + tokens-per-second per call + averages).
- Frontend rebuilt as 5 node tabs (N6 LLMs · N7 Tokens · N8 Embeddings · N9 Inference · N10 Vector DBs). N6 + N10 reuse `/day01/ask` for bilingual explainers. N7 wraps each piece in a hairline tag. N9 shows a latency bar per run.

### Day 1 — `/day01/ask` implemented

- `POST /day01/ask` implemented per spec. Hardened the system prompt and added a `_to_text()` coercion so structured replies (some providers return nested objects) get flattened to plain strings before reaching the frontend.
- Frontend rebuilt as 5 node tabs (N1 AI engineer · N2 Roles · N3 Learning path · N4 vs ML · N5 Terminology). Each tab pre-fills a focused question that's still editable; N5 has a free-form term input.
- `ModelSwitcher` defers render until after mount → fixed a React 19 hydration mismatch caused by browser extensions touching the `<select>` before hydration.

### Initial scaffold

- Backend scaffolded: `provider.py`, `token_tracker.py` (with Postgres persistence + connection pool), `main.py` with tagged OpenAPI sections, `init_db.py` (now also creates `token_usage` table), 10 router stubs.
- Frontend scaffolded on Next.js 16 + React 19 + Tailwind v4 — not Next 14 as the prompt assumed. App Router + TypeScript intact.
- Editorial dark palette (paper/ink + red accent), Inter + Source Serif 4 fonts. Server-Component root layout with the navbar split into `app/components/Navbar.tsx` (client). Provider rows with zero usage are hidden from the dropdown.
- Added `/usage` page + `GET /usage/daily?days=30` + `GET /usage/monthly?months=6` so token spend is visible day-by-day and month-by-month (requires Postgres reachable).
